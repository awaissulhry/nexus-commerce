# Variants page — FINAL PASS prompt (VP.F), 2026-09-11

Launch one session on the most capable model available, from the repo root, and paste the block below as the
first message. VP.F owns every path the five wave lanes owned plus the frame and the DS; the wave lanes are
closed. Nothing is committed until the Owner says so.

---

```
You are VP.F — the FINAL PASS on the Variants page. Five lanes (VP.1–VP.5) built it and closed; you own ALL of their paths now, plus the studio frame and the design system. Your job is one thing: make the four approved states pixel- and behaviour-identical to the design and to each other, then prove it with numbers. AAA quality, zero inconsistency, nothing left "for a later lane".

READ FIRST, in this order, before touching a file:
1. docs/2026-09-11-variants-page-spec.md — the approved build spec (§2 chrome measurements, §3/§4 anatomy, §5 contracts, §7 verification, §9 verbatim copy).
2. The canvas it names (four artboards: Variants · shared product, Variants · eBay projection + mapping, Product navigation, Generate combinations). The canvas is the visual contract; where the spec and the canvas differ, the canvas wins; where either is silent, the Information sheet's existing behaviour wins (one vocabulary, never a third).
3. docs/vp2-contracts.md — the FINAL API shapes (§6a: nullable readiness/completeness rule).
4. docs/pes-claims.md — rulings at the TOP (newest first; #794, #753, #169, D9 #214), then the five VP lane sections at the bottom (VP.3 at ~line 974, VP.4/VP.2/VP.5/VP.1 near the end, VP.5's END-OF-WAVE table last). Every open item and every "Owner's call" in those sections is now YOURS to close, with the defaults below.
5. docs/2026-09-01-product-edit-studio-layout.md §1b (nothing above the grid may push it down), §2.10, §5; the CH.1 sheet-chrome rule in `_studio/sheet/SheetToolbar.tsx`'s header.

RULES: claim `VP.F` in the ledger (session name, date, paths) before editing; one ledger section, numbers only, appended as you go. Build on the DS — a missing piece is added to `apps/web/src/design-system/**` with `.nds-*` styles, light AND dark (`scripts/check-dark-alias-scope.mjs`), contrast measured. Nothing is committed or pushed. Before ANY write, establish which database `:3000 → :8091` answers for (VP.2/VP.3 measured a LOCAL Docker `nexus_development`, `Product.version` 54 vs Neon 51 — re-measure, do not inherit); if it is prod, rehearse on the XAVIA family only (GALE-JACKET cmokmy3a40078pm0p1fvnu523, eBay·IT account cmr4aaqb00025nz016k18rup9, MISANO, AIREON, XRI01) and announce the fixture in the ledger BEFORE the write. A claim must match its measurement; a write's response is not what it wrote — read back after a delay. Run every gate BARE for its exit code. The spec and canvas ARE the approval; proceed on the defaults below and STOP only for: a write outside the XAVIA family, a non-additive migration, or a data correction on a real product (report it, do not apply it).

METHOD — measure, fix, measure again:
A. BEFORE: at 1440×900 signed in, on GALE-JACKET, capture the four states (master · variants; eBay·IT · variants; eBay·IT with the mapping dock open; master with Generate combinations open) plus the navigation drawer. For each: every §2 band height, every control height/type/radius, column widths, row order, identity-band contents, copy verbatim, console errors. Put the table in the ledger first.
B. FIX everything in the list below and anything else the before-table shows. Prefer the sheet's existing mechanism over a new one; delete every stub, fixture path and page-local copy the lanes left behind.
C. AFTER: the same table, plus a FUNCTIONALITY MATRIX — every control on both states (axis chip, Add axis, Generate combinations, Add variant ▾ and each menu item, each toolbar chip, Customise, Export, Import, ⋯ items, every checkbox, every editable cell, pin/reset, Edit mapping, every dock control, Save mapping, Cancel, nav items) → what it does → which endpoint it calls → how you verified it (read-back, inject, screen) → result. A control with no row in that matrix does not exist.

DEFECTS MEASURED ON THE LIVE BUILD (2026-09-11, 1440×900) — each with the rule that closes it:
D1. Master state: an info banner "Axis values are read-only on this page" (82px) sits between the toolbar and the grid. §1b: nothing above the grid may push it down. Default: axis cells are EDITABLE (canvas), and the write must land where the channel pushes READ (VP.2 measured: the sheet's `attr_color`/`attr_size` write lands in `categoryAttributes.color` while the eBay push reads `categoryAttributes.variations`). Make ONE write path through `masterWrite` that updates what the pushes read; prove it with a read-back of both stores. Only if that is genuinely impossible today: cells read-only, banner GONE, the reason as a footer note + cell tooltip.
D2. Toolbar chips print "5 columns / 0 columns / 2 columns". The unit on this page is VARIANTS (rows): `Excluded somewhere 5`, `Duplicate combinations 2`. Fix the unit rule in `SheetToolbar`/the ViewChip contract (a chip declares its noun), so the Information sheet keeps "columns" and this page says rows.
D3. "no views · no import" render as inline text in both toolbars. The canvas has Customise · Export · Import · ⋯ and no notes. Import is IN the design: build it (reuse `_studio/import/` with a variants template: axis values + inclusion per coordinate; dry-run then apply, like the sheet's). Views: absent silently on this page; the reason lives on a disabled ⋯ item ("Saved views — fixed column set on this page"), never as bar text.
D4. Readiness pill shows 9% / 11% on every row (completeness incl. optional attributes) where the Information sheet shows 76% / 73% for the same rows. ONE number per row across surfaces: the identity band renders the same `CompletenessPill` value the sheet renders on the same coordinate (master state = master, channel state = that channel), from VP.2's split `readiness`/`completeness` fields. Never two numbers for one product.
D5. Identity band differs between the two states: master has thumb + pill + row ⋯, eBay has an empty thumb box, no pill, no ⋯, and the parent's secondary line shows the product name. Both states: IdentityBand identical — thumb, SKU mono, secondary = shared axis values ("Nero · XS") with the ⚠ glyph on BOTH states when the shared value is suspect, pill, ⋯ with the row verbs; parent secondary "Parent · 20 variants".
D6. Row order differs: master by axis value order, eBay alphabetical by SKU (3XL, 4XL, 5XL, L …). Both states: parent first, then `axes[].valueOrder`, then SKU. And `valueOrder` is missing XXS (Taglia reads "9 values" while XXS variants exist) — derive the order for values absent from the stored order (append, sorted by the size/colour canon), never drop them.
D7. Master shows the two XXS variants as "Nero · XS" / "Giallo · XS" (the shared Taglia bag holds "XS" on those rows — VP.2's "overwritten axis bag"), which is why "Duplicate combinations 2" and "18 of 18 combinations" appear for 20 variants. Do NOT rewrite product data. Report the two rows with evidence and the exact fix; show the ⚠ on both states; make the band honest: "18 of 18 combinations exist · 0 missing · 2 duplicates" (the duplicates count belongs on the band when non-zero).
D8. Pinned semantics: the eBay grid shows a pin on EVERY cell and the dock says "Nero · 10 pinned" while the chip says "Pinned values 2". Define ONE rule: pinned = the channel value DIFFERS from the resolved shared value; equal = inherited (link glyph, no tint) regardless of how eBay stores it. The chip counts variants with ≥1 pinned cell; the dock's Values table shows per value: shared · on eBay · included, and a one-line "same as shared · N included" for an axis with no differences (canvas §4.4.2).
D9. Mapping dock: the Values section is a long vertical list that pushes Listing split below the fold; the lock banner is at the top; up/down arrow buttons instead of drag. Match the canvas: sections in order Variation specifics → Values (compact tables) → Listing split → lock banner; all visible at 900px without scrolling for a 2-axis family; drag handles (keep the arrows as the keyboard path, not as visible buttons). Panel fills the 420 track.
D10. Listing column repeats the same ItemID on all 20 rows; parent Included cell says "The listing". Canvas: children show the state word only; the ItemID lives on the parent row; parent Included = "—".
D11. Column headers: "Inclu…" truncates at 90 (header carries the ⋮); "WooCommerce · GLOB…" truncates; Shopify/Etsy carry a "· GLOBAL" suffix the canvas does not. Rule: the market suffix only when the channel has more than one market; every header fits its width with the ⋮ visible; Included ≥ 100.
D12. Scope bar disagrees with the grid: on the master state the chips read "Amazon · Not set up" and "eBay · Not set up" while the projection columns show Listed on both; on the eBay state the eBay chip reads "Blocked 100%". The chips and the projection cells must read ONE readiness source for the current market/account on every state; no market or account is silently defaulted.
D13. Frame copy varies by scope: "Autosave on" / "Choose listing destinations" on master vs "Nexus draft autosave" / "Review listing information" on eBay. One save-state vocabulary and one action label per scope, taken from the Information sheet's strings; the Variants page introduces none of its own.
D14. Generate combinations: clicking the button opened nothing (measured: no modal, no message, no console error). Make it open the §3.4 dialog: dryRun → summary → create; created children are DRAFT and excluded on every coordinate. Verify Add variant ▾ and each of its items the same way.
D15. Toolbar wraps to two rows at 1440 and three at 1280 (VP.1/VP.3/VP.5 all measured ~1670px of children in 1660). One row at 1280 and above: shorter chip labels, the search slot as a flexible width, the absent notes gone (D3).
D16. `Needs a value` tone: warning (`missing`), not danger — `readinessMeta(state,'row')` decides; no local colour map anywhere on the page.
D17. Two studio-chrome buttons a tier small: `ClassificationDialog.tsx:53` and `SchemaStatus.tsx:32` (`size="xs"` → `sm`).
D18. Gates: `check-layout-v2.mjs` §2.3 forbids the app top bar while the approved canvas draws it — update the assertion to the canvas (top bar present, 56). `check-control-census.mjs` waits for `.nds-tab.on` on panel surfaces the studio no longer renders and runs as `anon` so 5 of 12 surfaces never measure — fix the readiness selector and run signed in (the studio's other probes show how). Both gates must exit 0 on one clean run, or name a pre-existing failure with its owner and the number.
D19. `_studio/variants/channel/dock/sections.tsx` raised the raw-primitive ratchet by one — replace the raw control with the DS one.
D20. Delete every fixture/stub path the lanes left in `_studio/variants/**` (channel/fixtures.ts, any `meta.source` fallback that reads the catalogue instead of `/studio/family`), and the `'variation-order'` / `VariantOrderTab` remnants (`types.ts:43`, `PresentationTab.tsx:26`, `images/types.ts:137/204` if they are not the images homonym).

DEFAULTS FOR THE OPEN DECISIONS (proceed on these; the Owner overrules in the ledger):
- Relationships stays a placeholder this pass; Shopify "Product family" stays under Shopify.
- Import is built (D3), not held.
- Listing split is rendered and HELD with its reason until alias creation is measured live (PES.5-ii).
- Data fixes on real products are reported, never applied.

DONE means, and only means: the AFTER table shows every state at the §2 numbers, controls all 28px on the tier, copy verbatim to §9/the canvas, identical identity band and row order on both states, zero console errors signed in; the functionality matrix has a verified row for every control; `npx tsc --noEmit -p apps/web/tsconfig.json`, api tsc, `npx vitest run _studio` + api vitest, `check-ag-grid-import-boundary`, `check-editor-open`, `check-control-census` (all 12 surfaces measured), `check-layout-v2`, the raw-primitive ratchet and `check-dark-alias-scope` all exit 0 bare on one clean run of the finished tree; the ledger holds the before/after tables and the matrix; nothing committed. Then say "100% done" and stop.
```

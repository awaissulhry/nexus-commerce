# Session 03 follow-up — finish product-specific eBay Presentation

Workspace: `/Users/awais/nexus-commerce`. Continue the existing Session 03 and preserve your mapping/theme increment. This authorizes implementation of the remaining product Presentation behavior. Read the coordination README, PRODUCT-CONTEXT-ADDENDUM.md, your handoff and the final integration receipt in handoff-01.md. Inspect the current dirty tree, applicable AGENTS.md, DESIGN.md and relevant design-system source before edits. Do not reconstruct from Git HEAD.

## Outcome and current evidence

Product → eBay → Presentation must select/customize the theme assignment, preview the buyer result, order axes and values, and restore inheritance for the explicit product/family, account, market and listing. Reuse the existing theme editor, ordering controls, canonical mapping/resolution and writes. Shared library/rule editing is explicitly broader; saving a product customization must not modify a shared definition or another product. Keep the task sidebar and single scope selector. Do not add channel branches or begin main Listings/Channels navigation.

The coordinator applied your source-metadata patch and extended the source/reset labels and rule link. `PresentationTab.tsx` now labels shared rules and their wider impact. Preserve those edits. It also no longer mounts `VariationValueOrderModal`; its order action is disabled because the real cockpit write and order application still select product+market without complete account/alias/CAS. Both this UI restriction and server-side standing-order activation restriction must stay until the actual supporting path passes verification. Re-enabling a button alone is not the task.

## Implement

1. Record your start and exact owned consumer files in `handoff-03.md`. Take copies before edits. Trace order/theme reads, writes, preview, effective resolution, family membership, Inventory/Trading payloads, background operations and per-listing result stamps. Identify all entry points to the consumer you change; retain unrelated behavior.
2. Complete product-specific ordering with the existing `_variationAxes`, `_axisValueOrder` and legacy compatibility semantics. Carry validated account, market and primary/alias listing identity through reads and writes; use observed target versions and atomic conflict protection. Show whether the operation affects the parent, chosen variants or the destination family. Ordering cannot create variants, rewrite shared axes/SKUs or reorder grid rows.
3. Use `resolvePresentationOrder` consistently across product preview and the affected publishing/order consumers, extending the existing resolver where necessary. Preserve explicit property-level customizations over applicable rules; reset removes the override and restores the current rule/default. Test equal-priority conflicts and new variants/values. Do not add a frontend-only precedence calculation.
4. Make execution destination- and version-safe. Parent lookup, family membership, account credentials, payload construction and result stamping must agree on the same destination. Bind reviewed inputs and rule/target versions; reject stale work, retry safely and report per-destination partial failures. Never let an old completion stamp claim newer content was submitted. Marketplace publication remains a separate reviewed action. Verify with dry-run/mocked adapters, never real listing publication.
5. Finish the product theme path's remaining account/alias consumers: usage/staleness, preview, gallery/presentation preferences and description push/result scope where used by Presentation. Keep the reusable editor visibly shared; the preview product is not an update target. Preserve `allowPublish=false` for the embedded shared editor. Guard drafts against Back/programmatic navigation as well as close/Escape, using established patterns.
6. Restore the product ordering action only after its supported scopes are wired and tested. Propose the exact Studio capability change to the coordinator; do not remove the whole primary-only guard based on one working endpoint. Enable standing order activation only after all applicable consumers and reviewed activation/invalidation semantics agree. If a consumer remains unsupported, retain its precise restriction and state the limitation in the handoff.

## File ownership for this follow-up

Retain ownership of canonical mapping/impact/presentation-rule and theme modules from your original handoff, plus `PresentationTab.tsx`, its feature modules and DescriptionStudio. You may now directly edit these eBay consumer areas, transferred from the original Session 04 allocation for this bounded work:

- Web `apps/web/src/app/products/ebay-flat-file/VariationValueOrderModal.tsx` and new extracted reusable feature content, with their focused tests. Read DS controls before composing them.
- API `apps/api/src/routes/ebay-cockpit.routes.ts`, limited to the relevant presentation/order endpoints; `ebay-description-push.routes.ts`; related focused tests.
- API `apps/api/src/services/ebay-variation-order-apply.service.ts`, `ebay-description-push.service.ts`, `ebay-publish.service.ts`, `ebay-publish-gate.service.ts` and their focused tests, limited to required destination/version/resolution changes.
- API `apps/api/src/services/listing-wizard/ebay-publish.adapter.ts` and its tests. Additional exclusively eBay payload/order modules discovered through these imports may be included after recording their exact paths in your handoff, provided they are not a shared coordinator or Session 02 file.

Session 02 now owns listing-preset UI/defaults and wizard draft/preset routes; do not edit those areas. Both sessions must send changes to `services/listing-wizard/{channels,submission.service,channel-publish.service}.ts` and shared destination contracts to the coordinator. Keep core Studio scope/save/sheet files, listing-ID deep-link resolution, core writers, shared packages, DS, schema, registration, permissions and lockfiles coordinator-owned. Prepare exact current-baseline patches with hashes, record interface needs early and continue independent feature work. Do not make competing edits to shared files.

## Verification and delivery

Use isolated fixtures with two families, two accounts in one market, another market, primary and alias listings, shared family membership, inherited and customized themes/order. Assert exact affected listings and no changes to the other product/account/market/alias, shared variants or reusable definitions. Cover source/reset parity, current rule changes, stale saves/reviews/jobs, partial failures/retries and family membership changes. Compare canonical preview and the actual mocked Inventory/Trading payload ordering; a helper-only test is insufficient.

Run fresh focused API/Web tests, types, tokens/conformance and browser tests for product selection, assignment, reorder/save/reset, reload, scope switching during requests, draft navigation, keyboard, narrow layouts and light/dark. Respect the README's browser sequence: Session 02 first, then your explicit lease; continue non-browser work while it owns the browser. Use only isolated fixtures for writes. Preserve grid files and performance. Any shared DS extension needs coordinator integration plus exports/catalog/changelog/DS-GAPS and a Factory mirror.

Append a dated follow-up receipt to `handoff-03.md`: changed paths, exact supported destinations, persistence/resolver/job contracts, fresh test evidence, browser lease/result, remaining restrictions and current-baseline integration patches. Clearly distinguish product assignment, shared definition/rule activation and publication. Do not edit other sessions' handoffs or the coordination README, and do not call the whole product phase complete from an isolated feature result.

# Session 5 prompt — integrated accessibility, performance and scale verification

Workspace: `/Users/awais/nexus-commerce` (or the coordinator's verified snapshot of its current uncommitted source).

Read `docs/catalog-redesign-sessions/README.md`, repository instructions and `docs/2026-09-06-catalog-workspace-redesign.md`. Start a read-only baseline audit now if useful; reserve final verification and fixes until handoffs 01–04 are integrated. Respect feature-file ownership and coordinate browser use. This task includes fixing demonstrated gaps after integration, not merely issuing a checklist.

The latest navigation decision is mandatory: only product editing has secondary navigation. Preserve the current shared grid and new `/channels/mapping` system. UI fixes must use Nexus DS, relevant source inspection, semantic tokens and coordinated Factory/catalog/changelog/DS-GAPS updates. Do not claim AAA from a few screenshots or passing unit tests.

## Verify and fix

1. Establish a reproducible baseline against the current pre-change snapshot, including uncommitted files. Record environment, dataset, viewport and operation timings. The earlier grid geometry and focused test evidence in the implementation record are useful references, not substitutes for current measurements.
2. Verify secondary-navigation geometry in product Studio: toggle cell/divider end at the subheader border; full-width content below; overlay follows actual global header/primary-nav size; desktop is undimmed; panel scrolls internally. Measure main content/grid x, y, width and height before/after open and close. Confirm no secondary sidebar/toggle in Products Next or standalone catalog/libraries.
3. Test keyboard and assistive semantics: collapsed content absent from focus order; accessible names/current route; visible unobscured focus; Escape/collapse/outside dismissal and restoration; dismissal clicks do not activate underlying controls; one navigation surface open; narrow-screen drawer conventions; reduced motion. Test nested editor/menu/drawer interactions and unsaved draft protection, including browser Back.
4. Audit responsive light/dark presentation at representative desktop, tablet and narrow widths plus zoom. Measure actual text/control/focus contrast against the applicable WCAG AAA criteria, including shared tokens and legacy theme-editor internals. Existing dark info text was measured at 6.51:1: that is not a 7:1 normal-text AAA pass. Fix demonstrated issues through shared DS where appropriate; list any unmet criteria rather than declaring blanket certification.
5. Preserve grid editors, variant expansion, keyboard movement, paste, selection, sorting/filtering, saved views, column customization, imports/exports, save/conflict feedback and scope isolation. Compare controlled before/after interaction latency, render work, query counts and memory using representative isolated thousands-product/variant data. Test multiple channels/markets without eager resolution of all destinations.
6. Verify end-to-end consistency: shared facts → mappings/defaults → partial overrides → effective source → explicit reset; two accounts on one market; rule/category overlap and new eligibility; one-file shared/override import; stale preview/edit/job conflicts; large bounded scans; interrupted/partially failed jobs and retries. Browser-check beginner and bulk workflows through review using dry runs or test adapters, never live publication.
7. Run appropriate focused tests, API/web/Factory type checks, token generation checks, DS token/conformance/parity guards and production builds against the integrated snapshot. Inspect scripts first; coordinate unique build-output paths so dev sessions remain intact. Do not run migrations/seeds/live verification scripts against the working catalog to satisfy this task.
8. Separate pre-existing/unrelated guard failures from introduced failures using saved starting content. Factory previously had 320 remaining token violations; re-establish that fact on the current snapshot rather than assuming any new failure is unrelated. Type checks and source parity alone do not prove runtime performance or accessibility.

## Ownership and final report

Own reproducible test/benchmark/audit artifacts and `handoff-05.md`. During parallel feature work, report defects to the owner without editing that owner's files. After integration, fix demonstrated defects with explicit file ownership, coordinated DS mirrors and targeted regression tests. Avoid broad restyling or replacing grid infrastructure.

Report what changed and was retained, routes/components/domain behavior affected, exact test/build/browser evidence, comparable performance measurements, unmet criteria, unrelated guard failures and any unfinished workflow. Recheck fixes, not every unrelated area repeatedly. The final verdict must distinguish implemented-and-verified, implemented-but-unverified, and unfinished; a partially wired workflow is unfinished.

# NAF.C/D/E(+H) — Director+Critic, Control Room, Promotion Machinery, Entity Graph — Pull-Forward Plan

> **STATUS: APPROVED 2026-08-06 with defaults (Q1 vendor relaxation recorded, Q2 weekly council, Q3 H in-cycle with cut line, Q4 B window untouched) — implementation in progress.**
> **For agentic workers:** REQUIRED SUB-SKILL: superpowers:subagent-driven-development. Steps use checkbox (`- [ ]`) syntax.
> **Spec:** `docs/AGENT_FLEET.md` Parts 0–3 + Part 7 (control surface) + Part 8.2 (Control Room panels) + Part 12 §§ C, D, E, H. Operator directive 2026-08-06: *"Continue with what we can build right now… Phase E to J, we'll try to do as much as we can"* — this plan is the buildable subset, staged, with the non-buildable remainder explicitly deferred with reasons.
> **Predecessors:** Phases A/B shipped and verified; the B 14-sweep window runs in parallel and is NOT blocked by this work (analysts stay dark until the operator enables them — unchanged).
> **Verified this session:** two exploration passes over apps/web (Control Room tab recipe, DS conventions, fetch idiom) and apps/api (approval gate, blast radius, pins, protected terms, self-competition, change ledger, dry-run inventory). file:line citations below.

**Goal:** Everything from C–J that is genuinely buildable today, in dependency order: the Director + Critic pipeline (C, dark, PROPOSE), the Control Room fleet UI (D — the first web surface), the promotion machinery (E, dark where execution-dependent), and the entity graph (H). Phases F, G, I, J are deferred with stated reasons — F in particular is a *data-gated promotion act*, not a build.

**Architecture:** Additive on A/B. C reuses the EXISTING approval gate by registering three preview-only ads tools (no `execute` — approving queues intent, nothing can reach Amazon by construction until F adds executors). D extends the existing ads Control Room page as a `fleet` tab following that page's own documented conventions. E hooks exemplar minting into the fleet's approval-decide route and computes scorecards nightly inside the existing sweep. H is a self-contained Postgres graph. No existing service signature changes anywhere.

---

## Verified state (do not re-derive)

| # | Fact | Consequence |
|---|---|---|
| V1 | `approval-gate.service.ts` accepts only REGISTERED tool names (`runOrQueueTool` resolves `getTool`/`resolveToolPolicy`, :38-47); the registry has **zero advertising tools**. A tool's `handler` is its dry-run preview; `execute` is the real write; **preview-only tools (no `execute`) short-circuit `decideApproval` to `status:'approved'` without executing** (:144-153). | C registers three preview-only ads tools. "Nothing reaches Amazon" becomes structural, not procedural. Executors are Phase F's one job. |
| V2 | `requestApproval` mints a synthetic AgentRun (:98-108); `runOrQueueTool(name, args, ctx, agentRunId)` links to a real run. | The director's plan items queue via `runOrQueueTool` with the DIRECTOR's run id — the decision timeline stays coherent. |
| V3 | **Approvals never auto-expire**: `expiresAt` is written (:78) and read nowhere; no expiry job exists. A failing `execute` reverts to `'pending'` indefinitely (:161,176). | The council job expires stale fleet approvals itself (D6). |
| V4 | `blast-radius-guard.ts` is PURE, in `services/ads-core/`: `evaluateBlastRadius(BlastInput, UNATTENDED_THRESHOLDS)` returns every breach; input is aggregate counters. | The critic folds plan items into `BlastInput` in code and hands the verdict to the LLM as evidence — and hard-fails the plan on breaches regardless of what the model says (L1). |
| V5 | Authority pins are campaign-level columns + pure helpers (`pinDenial`, `dimensionsForWrite`); protected terms are an inline WHITELIST query + `normaliseTerm` matching inside the write gate (ads-write-gate.ts:304-337, fires only on negation); no standalone exports. | The critic's deterministic pre-checks replicate the documented query/matching exactly (cited), never invoking the whole gate. |
| V6 | `detectSelfCompetition` (pure, caller supplies targeting) and `detectKeywordConflicts` (injectable prisma, excludes same-ASIN overlaps) are complements; their ranking keys are deliberately aligned after the 45%-disagreement incident. | `no_self_competition` uses BOTH; the critic must not invent a third ranking (ACR.2.5 lesson). |
| V7 | Dry-run inventory: `previewBidOptimization` (real diff), `previewHarvest` (candidate qualification), `suggestBids` (proposed values); **no dry-run exists for creating a negative or graduating a keyword**. | C builds per-item previews in the tool handlers from: candidate metrics + existing-negative check (`isNegative` only) + protected-terms + pin denial + blast contribution. |
| V8 | `ads-changes.listChanges` is the unified windowed change reader (both ledgers, deduped); **no post-change metric comparator exists anywhere**; `ads-retention.job.ts` prunes both ledgers. | E builds the outcome-attribution reader on `listChanges` + `AmazonAdsDailyPerformance` windows; lookback bounded by retention constants (checked at E build time). |
| V9 | Control Room tabs: URL-param + `<Link>`, literal tuple in `ControlRoomClient.tsx` (:68-75, :187-202); new tab = 4 edits + one file + `acr-*` styles in `control-room.css`. Fetch idiom: plain `fetch(getBackendUrl()…)` — auth + CSRF ride the global `installAuthFetch` wrapper. | The fleet tab is cheap to mount and follows the page. |
| V10 | `GuardrailGrid.tsx:22-25` documents that this page **deliberately avoids the DS DataGrid** (dark-cards-in-light-shell defect; own `acr-*` classes, light-only). | D follows the page's own convention (D-D2) — the "tables use shared DataGrid" house rule yields to this page's decided exception, which is itself an approved ACR design decision. |
| V11 | `@xyflow/react` is installed and used in `_canvas/`; `AiModelsClient` already renders the `agent-fleet-*` feature keys with per-feature model pinning. | The fleet map reuses xyflow; model pinning UI (control #20) needs zero new work. |
| V12 | Web has ZERO references to `/api/agent/fleet/*`; all 11 fleet routes exist and are RBAC-covered. | Clean slate; D adds ~4 routes (charter policy PATCH, approvals list/decide/bulk). |
| V13 | `tokens:check` is red on main (TECH_DEBT #62: 28 hand-added `--nds-rail-*` vars live only in generated `tokens.css`); pre-push doesn't run it; D's acceptance requires it green. | D folds the #62 fix in: port the rail vars into `css-vars.ts` + add `tokens:check` to `.githooks/pre-push` in the SAME commit (the operator's own recorded directive). |
| V14 | Prod model prefs: global → `sonnet-4-6`; `agent-fleet-analyst` pinned `haiku-4-5`. Gemini quota-dead; local provider unreachable from Railway. | The critic-vendor question (spec: "different vendor than the director") cannot be satisfied with a working second vendor today — Q1. |

## Decisions (call out if you disagree)

| # | Decision | Rationale |
|---|---|---|
| C-D1 | **Three preview-only ads tools** registered in a new `services/agents/tools/ads-propose.tools.ts`: `create-negative-keyword`, `graduate-keyword`, `set-target-bid`. Each `handler` = deterministic dry-run preview (entity state now, what would change, protected-terms/pin/existing-negative verdicts, blast contribution); **no `execute`**; `requiresApprovalDefault: true`. | V1/V7. Approving in Phase C records the decision (and mints the E exemplar) but executes nothing — F adds executors behind its own gate. The write-path defects V8/V9 from the B plan stay quarantined. |
| C-D2 | **Director** consumes open findings (14d window) → `DirectorOutput` → one `AgentPlan` row; plan items queue as `AgentApproval` rows via `runOrQueueTool(tool, args, ctx, directorRunId)` ONLY after the critic passes them. `amazon-ads-director` charter: `autonomyCap 'PROPOSE'`, feature `agent-fleet-director` (unpinned → global sonnet-4-6), council cadence via the existing sweep extended to `runFleet('council')` weekly at first (Q2). | Spec Phase C. Sonnet-tier for the one integrative judgment per council; analysts stay Haiku. |
| C-D3 | **Critic = code first, model second.** Deterministic pre-checks computed in `plan-critic.service.ts` (pins V5, protected terms V5, blast radius V4, self-competition V6, evidence freshness, dedupe-vs-open-findings) → handed to the `plan-critic` LLM as evidence for the 12-check `CriticOutput` → **any deterministic hard-fail forces `block` regardless of the model's verdict** (the model can only add blocks, never remove one). | L1: code does the math. The critic's value-add is judgment on the soft checks; the hard ones were never its to waive. |
| C-D4 | Fleet graph gains edges: three analysts →(finding)→ `amazon-ads-director` →(plan)→ `plan-critic`. Orchestrator level-walk already handles it. `fleet-selftest` stays edge-free. | Spec 3.3. |
| C-D5 | **Critic vendor: relaxed to same-vendor-different-model** — critic pinned `claude-haiku-4-5` vs director's `sonnet-4-6` — recorded as an explicit deviation from the brief's "different vendor", revisited when a second vendor is fundable (Gemini billing) or reachable (local from prod). | V14: there is no working second vendor today. Different model family is the available diversity. Operator may overrule (Q1). |
| C-D6 | The council run **expires** fleet-tool approvals older than 7 days (`status:'pending'` → `'expired'`) before queueing new ones, and never re-proposes an entity with a pending approval. | V3: nothing else expires them; stale intent must not accumulate (the reclaim-stale-writes lesson from the delivery model). |
| D-D1 | **The fleet UI is a `fleet` tab on the existing ads Control Room** — not a new route. Registered via the 4-edit recipe (V9); `acr-*` classes in `control-room.css` (V10); plain-fetch idiom. The spec's alternative (`/marketing/agents`) is rejected per the operator's standing extend-don't-add rule. | Decision the spec explicitly asks Phase D to write down. One cockpit, not two. |
| D-D2 | Six panels mapped to the tab: **(1) fleet map** — xyflow, node colour = autonomy, badge = open findings; **(2) agent drawer** — charter, dial (cap-enforced), budgets, last runs/findings, cost; **(3) decision timeline** — finding → plan → critic → approval rows, expandable to the AgentStep trace, **reading BOTH step shapes** (AgentStep for fleet, `AgentRun.steps` Json for ACP copilot — the recorded D12 obligation); **(4) approval inbox** — fleet-scoped, per-item decide with preview, **reject-all-from-agent**, reject requires a one-line reason (feeds E exemplars); **(5) cost ledger** — per charter/tier/day from AgentStep sums, reconciling to `AgentRun.costUSD`; **(6) brief** — renders the E auditor's output when it exists, honest empty state until then. | Part 8.2, adapted to the tab. |
| D-D3 | **New routes (4):** `PATCH /agent/fleet/charters/:key` (enabled + autonomyLevel, clamped to `autonomyCap` server-side; version-matched), `GET /agent/fleet/approvals`, `POST /agent/fleet/approvals/:id/decide` (wraps `decideApproval`; **reject requires `reason`**; mints the E exemplar), `POST /agent/fleet/approvals/reject-all` (`{charterKey, reason}`). All under the existing `/api/agent/` RBAC mapping. | The missing operator controls (#4 dial, #15 inbox). Server-side cap enforcement is control #5's contract. |
| D-D4 | **TECH_DEBT #62 rides Stage D**: port the 28 rail vars into `tokens/css-vars.ts`, regenerate, and add `npm run tokens:check` to `.githooks/pre-push` — one commit, per the operator's recorded same-commit directive. | V13: D's acceptance ("tokens:check passes") is unmeetable otherwise, and D is the first web work since the directive. |
| E-D1 | **Buildable-now E** ships dark: nightly scorecard computation (deterministic, inside the sweep; execution-dependent fields honestly null until F), `fleet-auditor` charter (daily brief, born OFF), exemplar minting on every fleet approval decision (V-D-D3), exemplar retrieval into director/critic prompts (top-5 by charter, recency-weighted — embedding retrieval stays deferred per A-D6), and the **promotion/demotion state machine**: `promotionEligible` computed per Part 7's ladder; the charter PATCH route refuses `autonomyLevel:'AUTO'` when `promotionEligible=false` (server-side, spec's acceptance); demotion triggers implemented for the computable signals (validation-failure rate >5%, critic double-block) with rollback/acceptance triggers armed but inert until F. | Spec E minus what needs executed actions. The outcome-attribution comparator (V8) is built but returns `null` impact until executions exist — displayed as unknown, never zero (the ACR 0.5 lesson). |
| H-D1 | **Stage H is in scope but LAST, with an explicit cut line**: `GraphEdge` migration, derivation jobs (VARIANT_OF from ProductVariation, TARGETS from campaign structure, COMPETES_WITH/CANNIBALIZES from keyword-conflicts + self-competition, SHARES_INVENTORY from the shared pool), recursive-CTE traversal service (p95 < 50ms acceptance), critic checks upgraded to structural (`no_self_competition`, `inventory_supports_spend`), fleet-map overlay. | Self-contained, deterministic, and it upgrades C's critic. If the operator wants to stop after E, H moves to its own later approval. |
| X-D1 | **Deferred, with reasons:** **F** — the spec says "build nothing new"; it is a promotion act gated on 14 clean sweeps + 30 days of PROPOSE track record + operator sign-off, and it moves real money; jumping it would gut the trust ladder the whole program sells. **G** — needs C–E live at council cadence first (its acceptance is ≥15 analysts ≤€60/mo); the strategist charter alone without scorecard history binds nothing. **I** — valuable but independent; after the ads fleet proves out. **J** — the eval harness needs the B corpus (accumulating nightly once analysts are enabled); A2 already seeded the local provider + bakeoff harness; MCP exposure is its own initiative. | Honest sequencing beats checkbox coverage. |

## Open questions (defaults stated)

1. **Critic vendor (C-D5)** — accept same-vendor-different-model (director sonnet-4-6, critic haiku-4-5) with the deviation recorded? Alternatives: fund Gemini billing (~cents/day at critic volume) for true vendor diversity, or accept and revisit. Default: accept + revisit.
2. **Council cadence** — weekly on the existing sweep infrastructure (Mondays 05:15 UTC, after the analyst sweep) until the B gate passes, then daily per spec? Default: weekly-first.
3. **Stage H** — build in this cycle or park behind its own approval after E? Default: build it (it's the critic's structural upgrade), cut if the cycle runs long.
4. **The B 14-sweep window** continues in parallel and its gate (your 50-findings judgment) still decides whether analysts ever leave OBSERVE — this plan doesn't touch that. Confirm understanding. Default: yes.

## Stages & tasks (each stage: TDD, per-task `--only` commits, tsc+vitest gates; pre-push per stage)

### Stage C — Director + Critic (dark)
- [ ] **C1** `ads-propose.tools.ts` — three preview-only tools + registry hookup; handlers build previews from V5/V6/V7 surfaces; tests per tool (preview shape, protected-term denial, pin denial, existing-negative dedupe).
- [ ] **C2** Contracts already shipped (`DirectorOutput`, `CriticOutput`) — add `director-output`/`critic-output` persistence to the executor: directors persist `AgentPlan` (items, dropped, conflicts, changeBudget, blastRadius), critics annotate the plan (`criticVerdict`, `criticNotes`, status transitions draft→critiqued). Tests.
- [ ] **C3** `plan-critic.service.ts` — deterministic pre-checks (V4/V5/V6) + hard-fail override; `amazon-ads-director` + `plan-critic` charters (dark, caps PROPOSE/OBSERVE); graph edges; `runFleet('council')` path queues approvals for critic-passed items via `runOrQueueTool` + expiry sweep (C-D6). Tests incl. the spec's seeded adversarial case (critic blocks a protected-term negation).
- [ ] **C4** Stage gate: full suites, tsc, drift, pre-push; supervised council run on prod (director+critic manual, charters OFF) verifying: plan with non-trivial drops, critic verdict, approvals with previews, zero Amazon traffic.

### Stage D — Control Room fleet tab
- [ ] **D1** The 4 new routes (D-D3) + RBAC check + tests (cap clamp, reject-reason required, bulk reject).
- [ ] **D2** `FleetTab.tsx` + `acr-*` styles: fleet map, agent drawer, timeline (both step shapes), inbox, cost ledger, brief placeholder. Plain-fetch idiom.
- [ ] **D3** TECH_DEBT #62 commit (rail vars → css-vars.ts + tokens:check → pre-push, one commit).
- [ ] **D4** Stage gate incl. `tokens:check` green + **UI self-verified on live prod via browser before showing** (house rule): every panel rendered against real data, screenshots.

### Stage E — Promotion machinery (dark)
- [ ] **E1** Scorecard computation service + nightly hook in the sweep; outcome-attribution reader (null-honest); tests.
- [ ] **E2** Exemplar minting in the decide route + retrieval into director/critic prompts; tests.
- [ ] **E3** Promotion/demotion state machine + server-side AUTO refusal + `fleet-auditor` charter (dark); demotion seeded test; stage gate.

### Stage H — Entity graph (cut line here if needed)
- [ ] **H1** `GraphEdge` migration + derivation jobs + tests.
- [ ] **H2** Traversal service (recursive CTE, p95 measured on prod data) + critic structural checks + map overlay; stage gate.

## Acceptance mapping (spec → this plan)
C: drops explained (executor validates `dropped[].reason` ≥10 chars — already in the Zod contract) · critic blocks a seeded bad plan (C3 test) · no approval without preview (structural: the tool handler IS the preview) · blast thresholds unattended (C-D3) · nothing reaches Amazon (no `execute` exists).
D: decision reconstructable ≤3 clicks (timeline → expand) · reject-all works (D1) · tokens:check green (D3) · cost ledger reconciles (D2 test).
E: nightly scorecards (E1) · calibration displayed (D2 brief/drawer) · AUTO refusal server-side (E3) · demotion fires in seeded test (E3).
H: p95 <50ms (H2 measured) · critic catches seeded self-competition the pairwise checks miss (H2 test) · zero new databases (Postgres CTEs).

---

## Execution record (2026-08-06, same session)

- **Stage C SHIPPED** — 17a65c8b6 (tools), af607a868 (executor plans), 401150ede (director/critic/council). Gate: supervised council on prod — director $0.194/168s produced a 15-item plan with 2 honestly-reasoned drops; critic (haiku-4-5, C-D5 pin applied via AiFeatureModelPref) returned **block** on three checks (no_double_counting: 9 items already negated/existing; blast_radius_ok: 2 declared conflicts vs unattended limit 0; no_self_competition); zero outbound AD_* rows in the window; charters restored OFF.
  **Open question for the operator:** the director's own declared-and-RESOLVED `conflicts` feed the blast guard's conflicts metric (limit 0 unattended), so any honestly-declared conflict blocks the whole plan. Decide whether resolved conflicts should count before the first live council.
- **Stage D SHIPPED** — f47bdcc76 (routes), 9231031b8 (FleetTab, six panels), a17d40a66 (fleet map onto xyflow per D-D2), ec718622c (TECH_DEBT #62: rail vars ported + tokens:check gates the push). Gate: full pre-push green incl. tokens:check; UI self-verified on live prod against real data (map, cap-enforced dial, AgentStep trace, timeline with the blocked plan expanded, inbox, ledger reconciling the council cost, brief).
- **Stage E SHIPPED (dark)** — daf62eff3 (E1 scorecards, 14d/30d windows, execution-dependent fields null until F), 866b8ee7d (E2 exemplars: minted on every decide, retrieved into prompts), 201ff5fb6 (E3 demotion machine + server-side AUTO refusal + fleet-auditor charter riding the sweep post-scorecards).
- **Stage H SHIPPED** — 43672bbb0. Migration applied to prod; first derivation ran: TARGETS=3285, SHARES_INVENTORY=2530, COMPETES_WITH=68, CANNIBALIZES=28, VARIANT_OF=0 (ProductVariation empty on this catalogue — derivation kept, dormant-honest). Traversal acceptance **PASS**: frontier CTE p50=16.7ms / p95=21.9ms / max=46.9ms server-side over 100 runs on prod volume (the first edge-recursive shape measured p95=759ms and was rewritten). Critic structural advisories live behind the same nightly derivation. **Cut per H-D1:** the fleet-map entity-graph overlay.

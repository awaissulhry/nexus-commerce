# Session prompt — "the cell editor sometimes does not open" (2026-09-03 10:25)

Paste everything below the line into a NEW session in `/Users/awais/nexus-commerce`. It owns the grid EDITOR lifecycle; the other running session (`nexus-commerce-ce`, recalculation) must not touch `columns.tsx`, `FormulaCellEditor.tsx`, the editors directory or `MasterSheet.tsx`'s column-model and edit wiring until this session reports the defect closed.

---

You are the session for one defect, P0: **an operator double-clicks a cell (for example the `name` column on the master sheet of the Product Edit Studio) and sometimes NO editor opens** — no popup, no inline field. The Owner hit it by hand. Lanes hit it twice yesterday and wrongly blamed their probes: PES.3 pressed `=` on a select cell and "the formula editor never opened, keystrokes went into the select panel"; another session pressed `=` on a number cell and got "opened no editor" on the first attempt, cause unestablished. Your job: reproduce it deterministically, name the mechanism from a measurement, fix it, and land a gate so it cannot come back. You report to the Owner. Fix-then-report: one report with mtime + sha1 (say "sha1"), the test run, and the screen readings.

## Standing rules
Nothing committed or pushed (the Owner pushes). The local API on :8091 writes the PRODUCTION database — this defect needs NO writes: open editors and press Escape; if a rehearsal must write, XAVIA family only, announced in your ledger section first, restored by value, verified on two paths after ≥ 8 s. Screenshot BEFORE any computed-style probe. `grep` here is a ugrep wrapper that prints BLANK under bracketed route dirs — use `command grep`; an empty count means "did not run". Design changes go to the Owner; the Owner's rulings below stand.

## Environment
Both dev servers are already running (API :8091 by another session's watcher, web :3000 by `nexus-commerce-ce`); do NOT start your own. If a port is empty for more than 20 s, say so to the Owner rather than restarting. Verify on the wire that the web app calls `localhost:8091`, never `railway.app`. Read `docs/pes-claims.md` rulings #762–#775 (line 966 downward) and the memory index: AG owns Enter/Tab/Escape in a popup editor; `onValueChange` is the only way a React editor's value reaches AG 36; opening a formula cell once ARMED a write (fixed by a `touched` ref + `isCancelAfterEnd`); React inline options re-run the column model.

## Leading hypothesis — MEASURE it, do not assume it
`MasterSheet.tsx` builds `columnDefs` in a `useMemo` keyed on `[identityColumns, attributeColumns, readinessColumns, schemaColumns, viewCtx]`. If any of those changes identity AFTER first paint — the formulas batch landing ~1 s after mount (`exprFor` identity repaint), `formulaWritable` arriving, the persisted column state re-applied once the columns are real (`restoredColumnState`), the landing preset, a chip/view change, the SheetWriter's late flush (~8–15 s) re-reading rows — AG rebuilds the column model, and an editor opening in that window is torn down before it mounts (or a double-click lands on a cell that is being re-created). Also candidates: `cellEditorSelector` returning a component whose `popup` flag differs from the ColDef's `cellEditorPopup`; the `isCancelAfterEnd(!touched)` guard interacting with a double-click that AG treats as start-then-stop; an editor that mounts async (`useLayoutEffect` focus) losing the race to a `refreshCells`. A double-click on a formula-bearing cell vs a plain cell may differ. Instrument it: log `cellEditingStarted`, `cellEditingStopped`, `columnEverythingChanged`/`gridColumnsChanged`, `rowDataUpdated`, and every `columnDefs` identity change with timestamps, then double-click cells of EVERY kind (text `name`, long-text `item_name`, number, select, yes/no, a formula-bearing cell) 20 times each at three moments: immediately after the grid paints, during the first 3 s (formula batch landing), and during/after a save flush. Record which double-clicks produced no editor and what fired in the 200 ms around them. That table is the diagnosis.

## Owner's rulings for the fix
1. **Every open gesture must open an editor, every time:** double-click, Enter, F2, typing a character, typing `=`. If an editor genuinely cannot open (cell not editable), the sheet SAYS so in the cell's own words — never silence.
2. **One consistent rule per column kind, not per mood:** INLINE editing (the cell is the editor, Excel-like, fast) for short text, number, select and yes/no; a POPUP only where the content cannot fit the cell — long text and formulas (the formula editor needs its overlay, completions and preview line). The same column always opens the same way. The Owner's words: "no inconsistencies and anything that limits the workflow or slows it down". If the current wiring differs from this, change it and say so.
3. **A gate so it never comes back:** an automated run (add it to the layout suite as `LAYOUT_ONLY=editor-open`, or a sibling script) that opens the editor 20× on each column kind at the three timings above and asserts the editor's DOM within 250 ms of the gesture, with a positive control and a loud FOCUS-BROKEN check like the existing suite; it fails the pre-push gate on a single miss. Quote it with its argv.
4. No writes to reach this: open, read, Escape. `isCancelAfterEnd` must still discard an untouched edit.

## Report shape
The diagnosis table (gesture × column kind × timing × outcome × what fired); the mechanism named from it; the fix with sha1; the gate's argv and its run; a screen reading per column kind showing the editor open; what was NOT verified, plainly.

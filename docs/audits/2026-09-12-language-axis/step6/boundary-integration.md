# Step 6 boundary integration — checkpoint through item (c)

This is a working integration note, not the final Step 6 §4 record. Items (a) and (b) are complete; item (c) remains at Q-LX6-1. Items (d)–(h) and the final Step 6 gates have not started.

- The design mock and protected Amazon runtime files remain outside this change. `checkpoint-c.json` records the content comparison and the current claimed-file hashes.
- `language-sheet.ts` widens the existing per-language information pipeline. Cells retain their ContentAddress, content version and array clears. Columns carry locale and group by field. Conditional completeness is evaluated against each language's canonical keys with the existing completeness function; missing/validation destinations are then qualified. Non-text findings are counted once.
- `contracts.tsx`, `languages.ts`, `views.ts` and `useSheetColumns.ts` share the URL, preset, working-view and saved-view projection. Qualified keys in the existing view payload encode the selected languages. The pending view waits for the actual matching registry and applies after grid landing. Both hosts use the shared column dispatcher and toolbar.
- `languageChips.ts` counts only localizable cells, using resolved/requested language inequality for missing translations. The same rule registry orders the three S2 chips first in a Languages projection. `useRegisterViewChip` carries their actual cell destinations. The channel loading call uses the stable column registry.
- `languageWrites.ts` groups edits by target language and passes the resulting version to the next group. Both hosts retain the Step 4 router. Channel uncertain-save recovery requests the qualified columns too; master recovery already did so.
- `useCellFormulas.ts` reads each projected language, stores formulas under the qualified UI key, and sends canonical fields plus the target language for preview/save/replace/removal. `formulaColumns.ts` supplies one candidate/reference mapping for both sheets and the drawer. The existing DS selector passes the edited field into those callbacks. The bulk formula field selector also retains the target language. No formula save or provider call was rehearsed; formula reads remained read-only.
- Existing master AI draft reads cover the selected languages and project draft IDs onto exact view columns before indexing, grouping and chip counting. Generation remains absent from this adapter.
- The master bullet-list editor is retained for language-qualified bullet columns; its explicit-clear test keeps `[]` and the address.
- The requested Step 7 channel-scope sheet-column cache change was implemented and tested in item (a); the account remains in the cache key. Grouped market languages and import refusal/address seams are integrated with the Step 7 owner.

Final item (c) screen run: 2026-09-13T00:47:23.154Z–00:48:41.877Z, exit 0. Twenty GALE-JACKET readings at 1440/1728 in light/dark, all visually inspected. Full reload retains Languages and the URL selection; the three S2 chips fit in the viewport. Page errors, fatal React diagnostics and provider gateway/transport attempts are zero. The console also contains HTTP 401/403/404 resource diagnostics, retained in the raw evidence.

Shared product: 576 projected language columns, Needs translation 10752; Amazon DE: 13, Needs translation 53; eBay IT: 2, Needs translation 0. AI drafts/Out of date are 0 on these fixture projections. Belgium nl/fr: no cached OUTERWEAR schema, four structural columns, zero text columns, Requirements incomplete. Its zero counts describe zero eligible projected cells and do not prove translation coverage. The Owner must decide whether that incomplete-requirements screen satisfies the Belgium screen condition before item (d) starts.

No Step 6 business-data write, provider call, production write, push or deploy. The dedicated local test account is used only by the authenticated gate; credentials remain in process memory/environment. Browser and isolated read-only API stopped after the final run. The Owner's temporary 33-row test was reverted by the Owner; the fresh family census is 21, and no corrective data action was taken.

Q-LX6-1 accepted for(c) only. Before(d), missing-contract regression8/8 visually read at1440/1728 light/dark, all row and alias pills —; existing DS EmptyState names Amazon · BE OUTERWEAR and Requirements incomplete → Refresh requirements. `c-missing-tests.json` and `screens-c-missing.json` preserve tests and readings; no provider attempts. Then3 LOCAL CategorySchema fixture rows copied from NL→BE and read back after8.128s; see `belgium-fixture-receipt.json`. This is synthetic local fixture provenance, never a Belgian provider validation or production write.

Item(d) implementation checkpoint (not a Step6 §4 closure): canonical tier/language/requested/provenance/translation fields are declared and produced with the same edit; both mirrors have parity coverage; mapping input review stamps remain intact. Both actual cell renderers use the DS description and mark. A canonical pin tooltip names its answering listing; machine review takes precedence over a computed mark.37 focused API tests and32 focused web tests pass, API Studio and owned renderers type-clean. The13 broader older-fixture failures reproduce with pre-(d) source bytes, recorded in d-before-attribution-tests.json.

The item(d)20-screen retry exited1 because concurrent ProductSheet extraction changed five host/model/style paths during the gate; these screens are diagnostic, not acceptance. Q-LX6-2 asks that session to identify its ownership and announce a source-save hold. Its new adapter now directly consumes describeCellSource; the temporary compatibility export was removed once no callers remained. No extraction file was edited by LX6. No(e)–(h) edits or final browser gates have begun. Current mtime/SHA and the protected-file comparison are in checkpoint-d-pending.json. The local BE fixture remains for the authorized remaining acceptance work.

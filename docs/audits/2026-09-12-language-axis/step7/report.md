# LX Step 7 — §4 report

Recorded 2026-09-13 after the full strict gate. Fixture: XAVIA GALE-JACKET, local development database only.

LX.17–LX.20 implementation is in place. This session stops at the Step 7 gate; it does not close Step 6 or authorize later work.

The catalogue now has a Language selector and resolver-backed title/description columns with ReadinessIndex state and fallback predicates applied before server pagination. It consumes Step 6's describeCellSource and ProvenanceMark. Translate uses the complete page filter, previews counts, rate-card estimates and reach, and has a durable one-operation draft/revert path. Live generation returns the ruling #13 gate before any model or draft write. Imported locale content passes through the Step 4 router; workbook row 2 carries language-qualified keys, and blank ignore/clear is independent per language. The readiness page reads and pages the index, preserves channel/market/account/alias/language, and links to the language in Studio.

Both active Amazon text-publish paths use the shared content builder and languageTag(resolved.language, market). Each market language resolves independently; review preflight names the unreviewed language. DE, Belgian Dutch, Belgian French and UK built-payload tests pass. No publish rehearsal or provider publish call was made. channel-specs/index.ts was already cache-first from the closed work; it was verified, not rewritten. studio-columns.ts now retains account/language/workspace/cache-mode identity without bypassing its cache. Step 6 owns and supplied the requested sheet-columns cache correction.

Implementation anchors: [schema columns](../../../../apps/api/src/services/pim/studio-columns.ts#L50), [publish builder](../../../../apps/api/src/services/pim/amazon-content-payload.ts#L43), [grid predicates](../../../../apps/api/src/services/pim/catalog-language.ts#L14), [Translate](../../../../apps/api/src/services/pim/catalog-translate.ts#L79), [workbook parser](../../../../apps/api/src/services/pim/catalog-workbook.ts#L219), [router consumer](../../../../apps/api/src/services/pim/catalog-transfer.service.ts#L186), [index reader](../../../../apps/api/src/services/pim/listing-readiness.service.ts#L65), and [readiness screen](../../../../apps/web/src/app/products/listing-readiness/page.tsx#L34).

Tests: 203/203 in 18 API files; 22/22 in four web files, including DS provenance and Studio wire parity. The API run includes the Step 2 guard, built payloads, cached schemas, addressed imports, durable import jobs and a 3,000-product Translate preview over bounded reads. Latest permission and readiness focused runs each passed 8/8. Scoped API/web types, raw-primitives, DS conformance, token resolution, and AG import-boundary checks exited 0. The last small presentation changes also passed their scoped types and three presentation guards. An earlier broadened API type check through products.routes.ts exposed three existing rateLimit ambient-option errors outside the grid seam; the owned API roots are clean.

Read/cache gate: 2026-09-12T23:02:59–23:03:00Z, local read-only database, three actual account DE sheet loads returned 21 rows in 551/246/240 ms. All reported database fetchedAt 2026-09-12T14:52:54.095Z. Gateway attempts 0; transport attempts 0.

Write gate: 2026-09-12T23:20:56–23:21:53Z, only local 127.0.0.1:55439/nexus_development, XAVIA GALE-JACKET root cmokmy3a40078pm0p1fvnu523 and child cmokmy0lr0009pm0p9yxk7ho0. One synthetic German AI draft produced machine provenance, no reviewedAt, and the preflight “Review the German (de) title before publishing.” An existing operator pin stayed authoritative. The operation reverted to REVERTED. German/French workbook writes and a German Amazon pin used the router; blank ignore changed zero rows, clear changed one German row and preserved French. Readbacks waited 8211/8181/8191/8235 ms. Exact restoration by value was reread after 8175 ms: Product 21, ProductTranslation 1, ChannelListing 130, ChannelListingTranslation 0, CellFormula 0, OutboundSyncQueue 88 and ReadinessIndex 714 all equal. BulkOperation/audit history was retained, including the first failed rehearsal's partial-revert record. That attempt also restored all seven tables before retry. Provider attempts 0. The later temporary family-count change was the Owner's own reverted test; no corrective write was made.

Screen readings: real Next pages and actual handlers on isolated proxy 4121/API 4122, database enforced read-only, fixed synthetic identity (not an authentication gate). Catalogue language columns and marks were inspected at 1440/1728 in light/dark. Readiness, Translate and blank-policy controls were inspected at 1440/1728/768 in both themes. Ready versus Blocked yielded 0/1 indexed German rows; pagination yielded 25 then 9 rows; Studio links include locale=de or BE/fr/nl coordinates. The final German row is 62 px, its full 66-issue Disclosure opens/closes by Enter/Space, and narrow-table ArrowRight scrolls 40 px. Desktop table fits within its viewport after bounding the long product name. Translate showed 1/0/0 for the readiness fixture, and 17/0/0 matching a later 17-product catalogue search; Generate stayed disabled. Blank policy keyboard selection switched clear and back to ignore. Screenshots and DOM readings are retained.

Scope: source-manifest.json contains full mtime/SHA256 for 45 changed/new source files and before/after comparison. 31 protected mock/flat-file/batch-feed files are unchanged. Two assigned catalogue files were missed by the initial literal-glob snapshot and are explicitly marked as lacking pre-edit bytes; their final hashes are recorded without claiming full pre-edit attribution. No Studio tree, DS or sheet-columns source was edited by LX7. No migration, commit, push or deploy.

The existing full `node scripts/check-editor-open.mjs --strict` exited **0**, 2026-09-13T00:09:22.470–00:26:44.281Z. It passed 240/240 open gestures, 9 geometry readings, 235 contract assertions, 20 parity readings and 3 refused-cell readings, with the negative control held. 0 unintended API writes were armed; 12 expected saves were held and never completed. Wire attribution matched only loopback 4122. No `EDITOR_ONLY` or reduced repetitions were used. Master fresh-measure and optional parity cells were explicitly reported as undeclared/unrendered, not measured as 0/0. The first attempt exited 130 after the audit harness exceeded its JSON string limit; it is not accepted. The final harness records bounded summaries and full response checksums. Application source stayed frozen throughout the accepted run.

**Broader-browser diagnostic, outside LX7 ownership:** the full Studio gate reached 123 blocked `amazonSellerSpec` gateway entries through cold shipping-label lookups in `apps/api/src/services/categories/reference-labels.service.ts:30`. The gateway threw before credentials/provider execution; transport attempts were 0. This is distinct from the required three account-sheet cache loads, whose gateway and transport attempts were both 0. The diagnostic was handed to Step6 in the ledger. Support endpoints outside the harness allowlist were also refused; this is not a blanket zero-console-error or authentication claim. The full refusal list is in [screen-summary.json](screen-summary.json).

Open questions: no unanswered Step7 design decision. Belgium's missing cached OUTERWEAR contract remains an honest incomplete-schema reading; no schema was invented or copied. The live import positive control used DE, while BE-nl/BE-fr publish payload behavior is covered by built-payload unit tests. Step6's own Belgium acceptance question and queued channel correction remain with that session. The shipping-label diagnostic above remains outside this claim. No broader “all browser provider attempts are zero” acceptance is asserted.

Owner-visible readings: [German readiness,1440 / light](readiness-1440-light.png), [German translation estimate,1440 / light](translate-1440-light.png), [German catalogue,1728 / dark](grid-1728-dark.png), and [blank-policy control,768 / light](import-768-light.png). All final screenshots were opened and visually inspected.

Evidence: [source mtime/full SHA256 manifest](source-manifest.json), [test/guard commands and exits](checks.json), [last presentation checks](presentation-checks.json), [203 API tests](api-tests-complete.json), [22 web tests](web-tests.json), [three cached loads](local-read.json), [write phases](write-receipt.json), [exact delayed restoration](write-restored.json), [screen readings](screens-final.json), [compact screen receipt](screen-summary.json), [full strict output](strict-gate.log), [strict timing/hash/exit](strict-gate.json), and [round-trip fixture workbook](fixture-language-workbook.xlsx).


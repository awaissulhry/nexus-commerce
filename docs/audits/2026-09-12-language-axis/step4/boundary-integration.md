# LX.8 write boundaries — Step 4

The changed screen is the **Product Edit Studio**, `/products/:id/edit/studio`, Information sheet, Shared product and channel scopes. This step does not add the Languages view; Step 6 owns it.

| Boundary | Address and persistence | Validation / concurrency / cascade |
| --- | --- | --- |
| `resolveWriteRouting`, `getStudioSheet` | One server routing function emits the source/language/pin address, both acknowledgement answers, reach, and the existing content row version. Server and web hosts inherit `ContentWriteFacts`. | Only `tier=pin && follows=false` is an operator pin. Drift carries no default write address. |
| Master `commitMasterRow`; channel `commitChannelRow` | Echo cell address and content version into `PATCH /products/bulk`; shared edits use Product CAS, pins listing CAS. | Server sentences are retained verbatim. Channel paste/fill events use `channelWriteGate` and the same writer. Cancel mutates the original displayed value and refreshes; no `setDataValue` event or HTTP request. |
| `applyProductBulkEdits` → `applyContentBulk` | Localizable contract fields leave the legacy mutation branch before any write. Addressless writes are refused using the loaded column label. | Same sheet `coerceForShape`; coordinates must match; inherited/drift edits need an acknowledgement. Slot edits combine against one read version; all grouped writes keep CAS. |
| `writeContent` source | Primary text stays on Product columns; localizable custom fields use Product category attributes. | Product CAS; synchronous `master-content.service` cascade and language audit inside the transaction. |
| `writeTranslation` | Non-primary content only in `ProductTranslation[language]`, with authored/source hash, review stamp, native clear markers and row version. | Product + translation CAS. Every write/review/remove calls the same-language cascade inside the transaction. No legacy JSON mutation. A draft queues no content publication. |
| `writeContent` pin | `ChannelListingTranslation[listing, language]`; explicit clears remain authored values. Reset records a per-field following marker to mask legacy read-through without rewriting legacy columns. | Listing + pin row CAS, review stamp and language/coordinate audit. No implicit shared promotion. |
| `master-content.service` | Records language-specific following intent on the new table; updates listing version/status. | Only matching marketplace languages and following fields cascade; operator pins stay isolated. Inherited child translations cascade only when the resolver identifies the edited parent as owner. Outbound payload carries language; tests inspect payloads without providers. |
| Translation PUT, global language PATCH | Use the same bulk router and sheet contract. Global requests refuse mixed languages/scopes rather than silently omitting values. | Named validator errors, explicit address and Product CAS; translations route through `translation-write.ts`. |
| Translation review/delete/AI result; bulk AI generation; mapping application | Translation review/delete and mapping-generated drafts call `translation-write.ts`; bulk AI uses addressed bulk edits. Mapping refuses a pin address as a shared-cascade source. | AI paths refuse missing addresses before generation when they intend a write; machine text remains unreviewed and its content publication is not queued. No provider was invoked in verification. |
| Formula authoring/materialisation/restore | Formula HTTP bridge uses bulk; initial authoring requires an address. An acknowledged shared formula is stored with a master scope, so reevaluation retains its destination. New snapshots preserve their content address. | Existing transaction/reentrancy protocol remains. Restore is atomic; legacy JSON/listing-column snapshot replay is refused. Persisted formulas materialise from their recorded scope/language/coordinate. |
| Product history restore | Uses the addressed bulk writer instead of direct Product mutation. | Same field validation/CAS/cascade; its history records language. |
| Shopify adapter | Carries an address for fact fields. Shared/localizable core text uses the bulk router; legacy native title/body writes through the narrow adapter are refused. | Mixed adapter/bulk batches retain every cell; primary and listing counters remain separate. |

Pin-table readers are hydrated in the studio, sheet rows, formula/mapping contexts, store workspace and syndication reads. Existing sheet/API list shapes remain `[]`. Legacy Product JSON and listing text remain read-through data at these authoring boundaries. Protected flat-file runtime files and the batch-feed service remain untouched; media-owned caption storage remains the accepted separate media concern.

## Verification boundaries

- Local positive controls use actual Product Edit Studio components, actual Fastify product/studio/formula routes and actual write services, against `127.0.0.1:55439/nexus_development` only.
- The isolated host substitutes existing local CategorySchema reads for seller-provider schema reads, disables worker/cache scheduling in the test bundle, blocks external transports, and holds every newly created fixture queue row until 2099 **inside the same transaction before commit**. No eligible rehearsal queue is exposed to existing local workers.
- Only XAVIA GALE variant `cmokmy0lr0009pm0p9yxk7ho0` is writable. The host refuses all other write endpoints/products. All values, versions, timestamps and temporary follow flags are restored by value; rehearsal-created translations and queue rows are removed. Audit history is retained.
- The complete standalone `check-editor-open.mjs --strict` passed on :3000 at 2026-09-12T21:07:34.383Z, exit 0, with a dedicated local test login. Credentials stayed in the gate process environment; no storage-state export or auth bypass. All attempted content writes were held/aborted before the database. See `editor-open-authenticated.json` and its log.

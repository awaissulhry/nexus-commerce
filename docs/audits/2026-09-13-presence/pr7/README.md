# PR.7 measured progress

**W0-COPY and W2-WIRE are posted. W3-VOCAB and PR.7 AT-WAVE-4 remain incomplete. Nothing committed.**

The copy pass replaces the five lying surfaces, pins the corrected sentences, blocks permanent deletion after a failed or incomplete check, and requires one actual SKU with exact case and whitespace. The existing destructive radio default is unchanged. The channel refusal reaches the operator with `level: none`.

The wire adds strict presence/one/history/Amazon-posture/verify loaders; imports the DS unions; preserves failed discovery, revoked and expired accounts, participation, and binned products; exposes sync source coverage; maps channelSnapshot as a distinct provenance layer. A family read requires explicit complete membership and cannot silently accept a product-only response. A one-read rejects a response naming any different coordinate level. ReadinessPanel uses the grid/datagrid shim.

| Measurement | Result |
|---|---|
| Hermetic baseline | 121 files passed; 1692 passed, 13 skipped; exit 0 |
| W0 after | 125 files passed; 1755 passed, 13 skipped; exit 0 |
| W2 hermetic studio + hard-delete, 20:49 | 130 files passed; 1803 passed, 0 failed, 13 skipped; exit 0 |
| API producer mirror | All six additive fields match optionality/nullability; passes |
| Null-default guard, 20:49 | 28 → 13, baseline 17 unchanged; exit 0 |
| Web typecheck, 20:48–49 | Exit 2; one PR.6 catalog fixture type mismatch, requested to its owner |
| W3 scratch meta/store | 34 passed; exit 0; not applied |
| Vocabulary gate, earlier 19:58 run | Exit 1; canonical meta and four consumers pending; 83 ratcheted prose findings, 0 new |

[W2 commands, load readings and results](w2-results.json). [Hermetic suite](studio-w2.log). [Null-default output](wire-null-w2.log). [Copy string table](../w0/copy/strings.md).

The brief’s six old studio failures are gone. Some had already been repaired before this lane’s baseline; current single-route master-write assertions were retained, channelSnapshot now has its own truthful layer, and the local-only test uses ctx.skip with a reason. The Owner approved retaining visible read-only WooCommerce Presence access on 2026-09-13. The original channel/market/tab membership assertions remain, with their meaning documented explicitly: navigation availability grants neither publishing permission nor a selling fact. Prepared W3 tests also pin server-provided Woo/Etsy refusal text.

PR.1’s existing offerClosedAt/By/Reason/syncPaused type+SELECT+serialization pair and source coverage are consumed. PR.8 applied the count-unit producer/consumer pair under the Owner’s D23 ruling and retains the released status hunks while preparing that pair. The master and channel cells now mount DS CellSaveMark; the memoized channel renderer subscribes to its actual tracker entry so a state-only change is visible. Renderer/scope tests57/57, including saving/waiting/unknown and refused/saved controls.

W3 preparation lives under `/private/tmp/nexus-pr7-presence/w3/`: one pure meta module, a shared abortable family-read store/provider, descriptor preparation and tests. It preserves null intent with provisional provenance, six fact states, stale/refused/expired/suspended evidence, server reversal/refusal, scheduling as a note, and aggregate weakest confidence/oldest as-of. No automatic verify or per-child HTTP fan-out is introduced. The source/consumer pair remains unapplied until all required Wave 2 gates and W3-API.

Current external prerequisites: W1.5-PUSH is now DONE with47 source-derived functions. Presence SQL is applied and verified locally; the full both-database W2-SCHEMA-APPLIED gate is absent because PR.5 has a separately measured production workspace/backfill prerequisite. Full W2-READ/W2-DS and W3-API remain pending. No PR.7 Owner question remains open; other lanes retain their claims and questions.

Remaining null-default hits were reviewed: literal absence markers, accessible video names and unnamed product/variant wording, two refusal fallbacks, one layout width, and two post-edit master cell metadata defaults. None asserts an unknown channel state. No baseline was raised.

The earlier static series at19:58 remains historical: i18n-catalog0, link-targets0, button-vocabulary0, help-cursor0, silent-disabled1, grid-kit1. [Historical static results](results.json). The grid-kit increase is LX’s `products/listing-readiness/page.tsx`; GridLabClient is dirty but its DataGrid import pre-existed HEAD. The silent-disabled findings were reported to PR.6 and are being repaired there. These are not a current clean final run. D22 uses a finite, tested list of intentional English fallbacks; Italian deletions remain with W3.

Interim signed-in Chrome captures used localhost:3000, with localhost:8091 read as the API origin before interaction. There were no data edits, delete confirmations, presence verifications or channel writes. At1440×900 the refusal follow-up measured a46px DS Banner and623px grid; both themes and drawer Listings were inspected, error log0. **PR.4 later disclosed an API source save during that hold, so these captures are potentially disturbed interim evidence, not final acceptance.** [Interim screen notes](../w3/copy/interim-notes.md). Final marks, all requested scope/theme combinations, hard-delete modal and skeleton after-height still require an isolated final browser run.

Historical measurement correction: the19:41 sandbox suite was discarded because it lacked loopback permission and was queued before inspecting an above-8 load reading. Accepted remeasurements used independently inspected load readings and maxWorkers=2; both API origins were unreachable port1. No production/channel write, server restart, nav takeover, commit or deployment has been performed by PR.7.

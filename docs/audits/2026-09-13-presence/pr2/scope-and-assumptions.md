# PR.2 scope and assumptions

No commits, production writes, channel writes, migrations, server restarts, or liveness widening. APIs resolve local Docker `127.0.0.1:55439/nexus_development`; all API Vitest runs start in `apps/api` and print that target.

- **ASSUMED:** The current brief and D10 override older C5: `dispatchSync` refuses both lifecycle types before reaching a channel update adapter. The BullMQ lifecycle branch is the channel dispatcher. Ordinary worker routing/guards are preserved; Owner-approved W1.9 corrects its completion record and success counter for SKIPPED.
- **ASSUMED:** Queue enums have no UNKNOWN. The durable outcome is `payload.delistOutcome`, the fact is `payload.channelFact`; retryable unknown parks PENDING with holdUntil, terminal unknown is SKIPPED. Neither means a confirmed channel failure or success. PR.7 received this contract and operator-copy module path.
- **ASSUMED:** There are no account/alias/original-product columns on OutboundSyncQueue. Existing channel/region/external-ID columns are populated, both cascade FKs are null, and all five original coordinate values are captured in payload. No schema invented. Deduplicated eBay ItemID jobs also retain their full coordinate list in the durable ProductEvent.
- **ASSUMED:** A cascade covers all Offer scopes. One distinct active Offer SKU wins; no Offer falls back to Product.sku; multiple distinct seller SKUs refuse instead of guessing. Named FBM/FBA scope resolution is available to the direct caller.
- **ASSUMED:** `channelCascadeDispatched` is the number of post-commit PENDING survivors submitted to the instant-lane helper, never a remote acknowledgement. `channelCascadeRetained` counts all surviving IDs, including CANCELLED. Failed evidence reads return null counts, partial=true, an operator explanation, and committed queue IDs for cancellation.
- **ASSUMED:** The disposable Product is DRAFT under GALE's verified XAVIA family; its fake ChannelListings are ACTIVE solely to satisfy the unchanged liveness predicate. They are unpublished, sync-paused, and use fake external IDs. Background workers are disabled. The exact scope was announced before writing.
- **ASSUMED:** Synthetic transport fixtures are distinct from the forbidden outbound fetch trap. Transport/access arms invoke one synthetic fixture each; every arm asserts zero outbound trap calls and zero Amazon delete calls. This is not a live-channel test.
- **ASSUMED:** Cleanup removes only sentinel fixture values, including their synthetic ProductEvents. It never changes AuditLog or restores whole existing rows. A rollback proof precedes committed fixtures; fixture, outcome and cleanup reads wait at least eight seconds.

## Requests and holds

- **REQUEST TO PR.1 — fulfilled:** register the guarded cancel route, add the exact products.delete manifest rule, and own the after-commit catalog response spread. Applied at 17:10:17Z. Read-failure and CANCELLED-count review requests were implemented in the PR.2 helper without another shared-route save.
- **REQUEST TO PR.3 — fulfilled:** exact-coordinate helper, direct callers capture seller SKU/account, and canonical FBA predicate. The outbound service now imports/re-exports isFbaCoordinate as isFbaListing; existing tests retained.
- **REQUEST TO PR.4 — fulfilled:** shared assertPushAllowed, sellingRisk, publish gate. All five push methods in outbound-sync consume the current lock. Selling-risk filtering retains the original SQL status predicate. Wider unowned push-path inventory is PR.4's disclosed follow-up.
- **REQUEST TO PR.7 — handed off:** DELIST_OPERATOR_COPY at `apps/api/src/services/delist-error-codes.ts`; render the server errorMessage/errorCode and payload delistOutcome/channelFact. W1.1 intentionally makes unavailable delists visibly RED on day one.
- **REQUEST TO PR.1 — pending READY:** post-lifecycle verification API `services/presence/verify.service.ts` / `enqueuePresenceVerify({coordinates,reason,actor,workspaceId})`. The owner explicitly asked not to import until READY. Current acknowledgement results keep channelFact UNKNOWN and persist a durable outcome event; no verification job/type is invented. Workspace must come from verified internal context.
- **W1.9 DONE (resumed Owner approval):** M4 DONE: local and production AMAZON STATUS_UPDATE all-time 0, 30-day SUCCESS 0; historical empty-patch incidence could not be measured because no durable marker exists. Positive QUANTITY_UPDATE SUCCESS controls were reported by PR.5. The Owner approved the recommendation, relayed verbatim as PR-OWNER W1.9 GO. The empty-patch arm and all three completion writers are fixed and measured in w19-rehearsal.json.
- No new Owner design question was needed. W1.9 authorization is an explicit prerequisite from the user's brief, not an inferred permission requirement.

## What was not done and why

No channel-read verification or channel mutation was attempted. Amazon/eBay reversible unpublish and Shopify delist refuse until their later adapters exist. Etsy and Woo coordinates are named as skipped. The D6/Wave-4 liveness predicate and eBay ENDED external-ID clearing were not changed. PR.1's shared catalog hunks, index registration and manifest were not edited by PR.2. W1.9 is now DONE; final AT-WAVE-4 still awaits the automatic verification integration required by §B.

- **ASSUMED (cancel refinement):** The five-minute undo applies only before the first attempt, not to later retry holds. retryCount0 and createdAt within five minutes join the original status/hold CAS; the actual DB transport arm proves cancellation cannot relabel attempted UNKNOWN as NOT_SENT.
- **ASSUMED (identity refinement):** Optional targeting fields fall back to payload only if undefined. Explicit null remains unresolved and refuses; it cannot borrow stale payload data. Four refusal controls and existing attributed positive controls pass.
- **REQUEST PR.4 — fulfilled:** global W1.5-PUSH DONE18:42:47Z; PR.2 reran the installed47-function/3-contract scanner with0 missing. Earlier147/141 candidate inventory is superseded, retained only as historical evidence.

- **ASSUMED (ordinary drain after UNKNOWN):** Refusing an unsupported dispatch cannot change an earlier channel attempt into FAILURE or consume another channel retry. The queue becomes terminal SKIPPED, retaining UNKNOWN and its original code, with the dispatcher refusal recorded separately. Four failing regressions were fixed; the actual local cascade rehearsal now also drains its transport fixture and reads the result after eight seconds.

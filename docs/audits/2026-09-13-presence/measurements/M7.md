# M7 — ChannelListing.platformAttributes._versionHistory

Key present: **local 0 of 1012 / production 0 of 977**. Non-empty platformAttributes: **1008 local / 976 production**. Same-session key enumeration returns `attributes` on **725** rows on each DB, witnessing that JSON keys are visible.

The 2026-09-01 ledger ruling **#115** recorded 0 of 977 in the then-production catalogue (Amazon/eBay); nearby #116 explicitly identifies the production queue. That historical result is relayed, not re-run in time, and its exact connection hostname was not recorded in that line. Today's directly measured 977-row result is the Neon production host named below.

What this decides: The measured _versionHistory-clobber risk is latent on these snapshots; the key is not currently populated.


## local: `127.0.0.1:55439/nexus_development`

Probe started `2026-09-13T16:51:14.134Z`. READ ONLY + REPEATABLE READ; ROLLBACK witnessed. Runtime session: 
```json
[
  {
    "workspaceId": "nexus_legacy_workspace",
    "status": "active",
    "role": "nexus_workspace_runtime"
  }
]
```

Positive control, in the same connection and workspace as this measurement:

At `2026-09-13T16:51:14.199Z`.

```sql
SELECT current_database() database,current_user role,current_setting('nexus.workspace_id',true) workspace_setting,(SELECT count(*)::int FROM "Product") products,(SELECT count(*)::int FROM "ChannelListing") listings,(SELECT count(*)::int FROM "OutboundSyncQueue") queue_rows,(SELECT count(*)::int FROM "Offer") offers
```

```json
[
  {
    "database": "nexus_development",
    "role": "nexus_workspace_runtime",
    "workspace_setting": "nexus_legacy_workspace",
    "products": 358,
    "listings": 1012,
    "queue_rows": 34903,
    "offers": 0
  }
]
```

### M7-history

At `2026-09-13T16:51:14.452Z`.

```sql
SELECT count(*)::int total,count(*) FILTER(WHERE "platformAttributes" ? '_versionHistory')::int history_rows,count(*) FILTER(WHERE "platformAttributes" IS NOT NULL)::int platform_nonnull,count(*) FILTER(WHERE "platformAttributes" IS NOT NULL AND "platformAttributes"<>'{}'::jsonb AND "platformAttributes"<>'null'::jsonb)::int platform_nonempty FROM "ChannelListing"
```

```json
[
  {
    "total": 1012,
    "history_rows": 0,
    "platform_nonnull": 1008,
    "platform_nonempty": 1008
  }
]
```

### M7-key-control

At `2026-09-13T16:51:14.467Z`.

```sql
SELECT k.key,count(*)::int rows FROM "ChannelListing" l CROSS JOIN LATERAL jsonb_object_keys(CASE WHEN jsonb_typeof("platformAttributes")='object' THEN "platformAttributes" ELSE '{}'::jsonb END) k(key) GROUP BY k.key ORDER BY count(*) DESC,k.key LIMIT 5
```

```json
[
  {
    "key": "attributes",
    "rows": 725
  },
  {
    "key": "conditionId",
    "rows": 268
  },
  {
    "key": "descriptionThemeId",
    "rows": 251
  },
  {
    "key": "browseNodeId",
    "rows": 248
  },
  {
    "key": "itemSpecifics",
    "rows": 248
  }
]
```

## prod: `ep-purple-river-altf6t3y-pooler.c-3.eu-central-1.aws.neon.tech/neondb`

Probe started `2026-09-13T16:51:14.488Z`. READ ONLY + REPEATABLE READ; ROLLBACK witnessed. Runtime session: 
```json
[
  {
    "workspaceId": null,
    "status": "no Workspace/RLS schema",
    "role": "neondb_owner"
  }
]
```

Positive control, in the same connection and workspace as this measurement:

At `2026-09-13T16:51:16.526Z`.

```sql
SELECT current_database() database,current_user role,current_setting('nexus.workspace_id',true) workspace_setting,(SELECT count(*)::int FROM "Product") products,(SELECT count(*)::int FROM "ChannelListing") listings,(SELECT count(*)::int FROM "OutboundSyncQueue") queue_rows,(SELECT count(*)::int FROM "Offer") offers
```

```json
[
  {
    "database": "neondb",
    "role": "neondb_owner",
    "workspace_setting": null,
    "products": 338,
    "listings": 977,
    "queue_rows": 34552,
    "offers": 0
  }
]
```

### M7-history

At `2026-09-13T16:51:22.623Z`.

```sql
SELECT count(*)::int total,count(*) FILTER(WHERE "platformAttributes" ? '_versionHistory')::int history_rows,count(*) FILTER(WHERE "platformAttributes" IS NOT NULL)::int platform_nonnull,count(*) FILTER(WHERE "platformAttributes" IS NOT NULL AND "platformAttributes"<>'{}'::jsonb AND "platformAttributes"<>'null'::jsonb)::int platform_nonempty FROM "ChannelListing"
```

```json
[
  {
    "total": 977,
    "history_rows": 0,
    "platform_nonnull": 976,
    "platform_nonempty": 976
  }
]
```

### M7-key-control

At `2026-09-13T16:51:22.690Z`.

```sql
SELECT k.key,count(*)::int rows FROM "ChannelListing" l CROSS JOIN LATERAL jsonb_object_keys(CASE WHEN jsonb_typeof("platformAttributes")='object' THEN "platformAttributes" ELSE '{}'::jsonb END) k(key) GROUP BY k.key ORDER BY count(*) DESC,k.key LIMIT 5
```

```json
[
  {
    "key": "attributes",
    "rows": 725
  },
  {
    "key": "descriptionThemeId",
    "rows": 251
  },
  {
    "key": "browseNodeId",
    "rows": 248
  },
  {
    "key": "bestOffer",
    "rows": 247
  },
  {
    "key": "bestOfferCeiling",
    "rows": 247
  }
]
```

Probe: `/private/tmp/nexus-pr5-presence/measure.mjs`; supplemental probe: `/private/tmp/nexus-pr5-presence/supplement.mjs`. Both commands exited 0; complete sanitized SQL/results are in [evidence.json](evidence.json). Initialization and script digests are in [README.md](README.md).

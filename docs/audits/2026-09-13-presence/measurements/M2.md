# M2 — Offer coverage on Amazon coordinates

Amazon Offer rows: **local 0 / production 0**. The entire Offer table is also empty on both. These are measured zeros with runtime workspace set locally, RLS established first, and non-empty Product + ChannelListing controls.

Every observed Amazon marketplace (DE, ES, FR, IT) has **0 FBA and 0 FBM** Offer rows on each database; `FulfillmentMethod` values were read from pg_enum. Grouped Offer JOIN output is empty, while the per-market listing denominators below are non-empty.

What this decides: D8: offerScope is inert today; do not expose offerScope:'FBM' on the basis of Offer coverage. FBA remains fail-closed with the named Seller Central hand-off.


## local: `127.0.0.1:55439/nexus_development`

Probe started `2026-09-13T16:51:14.134Z`. READ ONLY + REPEATABLE READ; ROLLBACK witnessed. Runtime session: 
```json
[
  {
    "workspaceId": "nexus_legacy_workspace",
    "status": "active",
    "role": "nexus_workspace_runtime"
  }
]
```

Positive control, in the same connection and workspace as this measurement:

At `2026-09-13T16:51:14.199Z`.

```sql
SELECT current_database() database,current_user role,current_setting('nexus.workspace_id',true) workspace_setting,(SELECT count(*)::int FROM "Product") products,(SELECT count(*)::int FROM "ChannelListing") listings,(SELECT count(*)::int FROM "OutboundSyncQueue") queue_rows,(SELECT count(*)::int FROM "Offer") offers
```

```json
[
  {
    "database": "nexus_development",
    "role": "nexus_workspace_runtime",
    "workspace_setting": "nexus_legacy_workspace",
    "products": 358,
    "listings": 1012,
    "queue_rows": 34903,
    "offers": 0
  }
]
```

### M2-offers

At `2026-09-13T16:51:14.208Z`.

```sql
SELECT l.marketplace,o."fulfillmentMethod",count(*)::int offer_rows,count(DISTINCT l.id)::int listing_rows FROM "Offer" o JOIN "ChannelListing" l ON l.id=o."channelListingId" WHERE l.channel='AMAZON' GROUP BY l.marketplace,o."fulfillmentMethod" ORDER BY l.marketplace,o."fulfillmentMethod"
```

```json
[]
```

### M2-denominators

At `2026-09-13T16:51:14.209Z`.

```sql
SELECT channel,marketplace,count(*)::int listing_rows FROM "ChannelListing" GROUP BY channel,marketplace ORDER BY channel,marketplace
```

```json
[
  {
    "channel": "AMAZON",
    "marketplace": "DE",
    "listing_rows": 217
  },
  {
    "channel": "AMAZON",
    "marketplace": "ES",
    "listing_rows": 123
  },
  {
    "channel": "AMAZON",
    "marketplace": "FR",
    "listing_rows": 115
  },
  {
    "channel": "AMAZON",
    "marketplace": "IT",
    "listing_rows": 276
  },
  {
    "channel": "EBAY",
    "marketplace": "DE",
    "listing_rows": 21
  },
  {
    "channel": "EBAY",
    "marketplace": "IT",
    "listing_rows": 256
  },
  {
    "channel": "ETSY",
    "marketplace": "GLOBAL",
    "listing_rows": 2
  },
  {
    "channel": "SHOPIFY",
    "marketplace": "GLOBAL",
    "listing_rows": 2
  }
]
```

### Supplement: M2-fulfillment-enum

At `2026-09-13T16:53:37.632Z`.

```sql
SELECT enumlabel FROM pg_enum WHERE enumtypid='"FulfillmentMethod"'::regtype ORDER BY enumsortorder
```

```json
[
  {
    "enumlabel": "FBA"
  },
  {
    "enumlabel": "FBM"
  }
]
```

## prod: `ep-purple-river-altf6t3y-pooler.c-3.eu-central-1.aws.neon.tech/neondb`

Probe started `2026-09-13T16:51:14.488Z`. READ ONLY + REPEATABLE READ; ROLLBACK witnessed. Runtime session: 
```json
[
  {
    "workspaceId": null,
    "status": "no Workspace/RLS schema",
    "role": "neondb_owner"
  }
]
```

Positive control, in the same connection and workspace as this measurement:

At `2026-09-13T16:51:16.526Z`.

```sql
SELECT current_database() database,current_user role,current_setting('nexus.workspace_id',true) workspace_setting,(SELECT count(*)::int FROM "Product") products,(SELECT count(*)::int FROM "ChannelListing") listings,(SELECT count(*)::int FROM "OutboundSyncQueue") queue_rows,(SELECT count(*)::int FROM "Offer") offers
```

```json
[
  {
    "database": "neondb",
    "role": "neondb_owner",
    "workspace_setting": null,
    "products": 338,
    "listings": 977,
    "queue_rows": 34552,
    "offers": 0
  }
]
```

### M2-offers

At `2026-09-13T16:51:16.805Z`.

```sql
SELECT l.marketplace,o."fulfillmentMethod",count(*)::int offer_rows,count(DISTINCT l.id)::int listing_rows FROM "Offer" o JOIN "ChannelListing" l ON l.id=o."channelListingId" WHERE l.channel='AMAZON' GROUP BY l.marketplace,o."fulfillmentMethod" ORDER BY l.marketplace,o."fulfillmentMethod"
```

```json
[]
```

### M2-denominators

At `2026-09-13T16:51:16.879Z`.

```sql
SELECT channel,marketplace,count(*)::int listing_rows FROM "ChannelListing" GROUP BY channel,marketplace ORDER BY channel,marketplace
```

```json
[
  {
    "channel": "AMAZON",
    "marketplace": "DE",
    "listing_rows": 214
  },
  {
    "channel": "AMAZON",
    "marketplace": "ES",
    "listing_rows": 123
  },
  {
    "channel": "AMAZON",
    "marketplace": "FR",
    "listing_rows": 115
  },
  {
    "channel": "AMAZON",
    "marketplace": "IT",
    "listing_rows": 273
  },
  {
    "channel": "EBAY",
    "marketplace": "IT",
    "listing_rows": 252
  }
]
```

### Supplement: M2-fulfillment-enum

At `2026-09-13T16:53:38.245Z`.

```sql
SELECT enumlabel FROM pg_enum WHERE enumtypid='"FulfillmentMethod"'::regtype ORDER BY enumsortorder
```

```json
[
  {
    "enumlabel": "FBA"
  },
  {
    "enumlabel": "FBM"
  }
]
```

Probe: `/private/tmp/nexus-pr5-presence/measure.mjs`; supplemental probe: `/private/tmp/nexus-pr5-presence/supplement.mjs`. Both commands exited 0; complete sanitized SQL/results are in [evidence.json](evidence.json). Initialization and script digests are in [README.md](README.md).

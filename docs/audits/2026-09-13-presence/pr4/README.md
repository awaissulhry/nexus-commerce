**W1.5-PUSH is DONE. PR.4 remains incomplete pending Shopify fact persistence after W2-SCHEMA-APPLIED; SHOP-P DONE and PR.4 AT-WAVE-4 are not posted.**

The repository-wide sweep is applied and its installed source gate is green: 47 derived listing-push functions plus three explicit contracts. Owner approved PR.4 ownership of the remaining sweep; PR.1 applied and tested the prepared route guards under its own claim, and PR.3 retained its completed label guard. The additional eBay feed-upload and promotion paths are now guarded too. Shared helpers, the Amazon master-gate correction and the Shopify Wave 1 status/capability producer are applied.

No production or channel writes, commits, schema changes, deployment-flag changes, or Shopify sheet intent writes were made. Local database measurements name `127.0.0.1:55439/nexus_development`, workspace `nexus_legacy_workspace`, role `nexus_workspace_runtime`; disposable DRAFT fixtures under GALE XAVIA are rolled back. Pure shared tests and the Node probe use no database. API unit tests mock persistence/transports; the explicitly enabled local fixture is the exception.

| Current measurement | Exit | Evidence |
|---|---:|---|
| Shared build | 0 | [build](shared-build.log) |
| Shared tests: 20 files, 219 passed | 0 | [tests](shared-tests.log) |
| Four built subpath imports from apps/api; no TS loader | 0 | [probe](dist-probe.log), [reproducible script](dist-probe.mjs) |
| API tsc, private build-info | 0 | [typecheck](api-tsc.log) |
| API focused suite: 28 files, 563 passed, maxWorkers 2 | 0 | [tests](api-tests.log), [exact file set](api-tests-list.json) |
| Shopify wire projection: all three remote statuses | 0 | [3 passed, 13 not selected](shopify-projection.log) |
| Real local DB: ACTIVE → ARCHIVED → DRAFT; delayed row reads and rollback | 0 | [1 passed in 24.62s](shop-p2-local-db.log) |
| Installed push gate: 47 derived functions, 3 explicit contracts, 0 missing guards | **0** | [complete derived list](push-lock-current.log) |
| Five seeds on installed source: pricing, new writer, Amazon feed bypass, eBay upload, marketing | 1 each, exactly one defect | [pricing](push-lock-seeded-missing.log), [new path](push-lock-seeded-new-path.log), [Amazon feed](push-lock-seeded-feed-bypass.log), [eBay feed](push-lock-seeded-ebay-feed.log), [marketing](push-lock-seeded-marketing.log) |
| eBay feed/promotion boundaries, including allowed payloads and refused persistence: 54 passed | 0 | [tests](ebay-promotion-feed-tests.log) |

[Machine-readable gates](available-gates.json) and [current source hashes](source-sha256.json) accompany these logs. Hashes identify the measured shared files, which may contain other lanes' work; they are not claims of sole authorship. The API command runs from `apps/api`; its configuration prints the local host before discovery. Shared commands run from `packages/shared`. The probe runs from `apps/api`: `node ../../docs/audits/2026-09-13-presence/pr4/dist-probe.mjs`.

The original gate receipt covered only the original §D slice and was explicitly reopened when the wider inventory exposed more writers. The installed command now defaults to all derived listing callers. `--slice` is an explicitly limited diagnostic. Discovery reads executable TypeScript: the clients' actual write methods, method-first Shopify requests, direct REST URLs including local constants, GraphQL mutation constants and typed adapters, listings-feed submissions, and eBay dynamic operation descriptors. Named nested functions and registered route handlers have separate boundaries. The guard recognises direct calls, the label guard's `map(assertPushAllowed)`, and a called local verifier containing the guard. The Amazon feed consumer additionally must build from `prepareRowsForPush(...).rows`; a source comment cannot satisfy it. This is a static regression check backed by transport-barrier tests, not a general control-flow proof.

HTTP DELETE, explicit eBay withdraw, a literal variation `<Delete>true</Delete>` and its literal zero-quantity fallback are classified as lifecycle removals. Amazon order/cancellation feeds, eBay location configuration/notification preferences and Shopify Collection-owned metadata are not listing pushes. These classifications use verbs, payloads and paths; they are not a list of permitted function names. `--transport-inventory` also exposes the broader transport candidates. The five scratch seeds each fail on exactly the introduced defect; the new-writer seed creates a 48th path in `listing-push-controls.ts`, outside the original five-file slice. Feed upload discovery follows the actual `/sell/feed/v1/task/.../upload_file` endpoint to its wrapper and caller. Generic listing POST wrappers are derived by following their URL parameter to the literal endpoint in their callers. This caught the markdown and volume-discount paths that the earlier 42-function census missed. The earlier red run is retained in [historical gates](before-sweep-available-gates.json).

**Applied behavior and receipts.**

- `@nexus/shared/push-lock` exports the one nonthrowing `assertPushAllowed` runtime predicate. It returns typed `{code,sentence}` refusals for syncPaused, offerClosedAt and HELD/WITHDRAWN/ENDED/DISCONTINUED/RELEASED, with optional future intent. The 15-arm helper suite covers precedence and allowed controls. No schema dependency was added.
- Pricing reads full listing controls and refuses an absent/ambiguous coordinate before Amazon price replacement. A refused push records FAILED/SKIPPED with the canonical code; it does not stamp IN_SYNC. The claimed pricing route hunk forwards account/alias. Shopify preview, synchronization, native offer writes and the second linked-information write surface guard their stored rows before writes. Deliberate ENDED refusal names its date/actor when present and returns 409.
- Additional guarded services cover FBA restore, variation processing, Amazon wizard/batch/cockpit, Woo inventory containment, legacy marketplace/eBay create/price/stock, eBay descriptions, labels (PR.3), shared images, shared creation, axes/membership/relabel/adoption/order publication, and Shopify/eBay bulk submission. The extra tests include blocked transports and unlocked mocked submissions; metadata tests explicitly distinguish reaching a subsequent read/review boundary from completing a channel protocol.
- eBay feed uploads parse and establish every payload SKU before sending the multipart file. Seventeen tests fail before the guard and pass afterward. Markdown and volume promotions inspect full stored controls before dispatch and again at the dispatcher, record FAILED with the canonical refusal, and preserve dry-run PENDING behavior. Thirty-seven promotion tests include all lock arms, unknown targets, a hold introduced after preflight, unchanged unlocked payloads and durable failure records. An inventory-wide promotion conservatively checks every stored eBay row; this can refuse due to a held row in another account or market because the legacy request lacks account scope. No transport target is inferred from this lookup. The existing feed task type is `LMS_ACTIVE_INVENTORY_REPORT` despite its inventory-task comments; this unrelated protocol discrepancy is recorded without changing the task type.
- `closedMarketSet` now has eight owned call sites: pricing `pushAmazonPrice`; flat-file `prepareRowsForPush` and `syncRowsToPlatform`; FBA `restoreFbaListings`; variation `processVariationSync`; Amazon wizard `publish`; batch `submitAmazonListingsBatch`; cockpit POST `/products/:id/publish-amazon`. It remains a conservative cross-account refusal belt, never a transport target selector. PR.3 corrected its formerly unsupported universal-caller comment.
- `deleteListingsItem` uses `getAmazonPublishMode()`. Master flag off plus mode live makes no request; the enabled-live positive control makes one mocked DELETE. [Before](delete-gate-before.log) and [after](delete-gate-after.log) evidence are retained. Deployment flag values were not changed.
- Flat-file `PR-PRESENCE-SANCTIONED-PAIR` documents the one sanctioned offerActive:false → skip_offer pairing. Row preparation refuses held/lifecycle-delete submissions before feed construction; PR.1's actual submit route consumes prepared rows. Row loading respects amazonFileExcluded. Local end stamping uses PR.3's five-level `whereCoordinate` for pre-read and write, with explicit account/alias metadata; the editors were not touched.
- `@nexus/shared/listing-risk` contains unchanged identityHeld and optional-field sellingRisk. The original studio test was copied byte-identically (SHA256 `322784a1ed664dc02cc5d9fd3a595477d163e698f5ce9f953884595e3b5aa31d`) and its [six cases passed](original-listing-risk-test.log) after PR.7's re-export. Shared risk tests pass 18 cases. PR.1's hard-delete preflight and PR.2's cascade consumer were repointed through exact ledger requests. Both retain the existing `listingStatus in [ACTIVE,INACTIVE] AND externalListingId not null` attempt set. D6 widening is not part of this change.
- `@nexus/shared/publish-gate` holds the existing gateNote body for PR.1/PR.7; its historical row-count phrase is preserved source text, not a new PR.4 measurement. `@nexus/shared/listing-capabilities` reports offerActiveHonoured true only for Amazon. PR.1 applied the actual studio row constructors and posted 26/26 producer tests, including false for Shopify/eBay/Woo with stored offerActive:true and no snapshot.

**Shopify producer/consumer pair and pending schema work.**

PR.7 applied the two prepared API producer hunks together with the optional carrier types and drawer consumer. Verified Shopify ACTIVE persists listingStatus ACTIVE; DRAFT and ARCHIVED persist INACTIVE, never the literal ARCHIVED. The wire carries raw status in channelFactDetail.shopifyStatus. The real local fixture runs `synchronizeContent`, waits eight seconds for each state, reads actual parent/child rows, checks increasing versions, proves rollback beforehand and checks zero residue afterward. Channel transport is mocked; no Shopify mutation was performed. The current-tree repeat passed at 20:19 local.

Four old `channel-sheet.vitest.test.ts` nativeEdits-save assertions still fail. A copied pre-PR.4 projection and byte-copied old test fail on the identical four names; the added three projection controls pass. [Baseline comparison](shopify-sheet-baseline-comparison.log), [before run](shopify-sheet-baseline.log), and [current full-file failure evidence](shopify-sheet-current-baseline-failures.log) preserve attribution. No assertion or numeric baseline was weakened. The separate focused API suite above is green; it does not claim this entire older file is green.

W2-SCHEMA-APPLIED is absent. `channelFact`, `channelFactAt`, `channelFactVia` and persisted `channelFactDetail` writes remain a [prepared, unapplied patch](pending-shopify-fact-patch.py). It reads publication evidence rather than inferring selling from ACTIVE; empty/future-only publications differ from failed reads. The corresponding future-schema assertions in the local fixture are not enabled. This patch must be reviewed against PR.1's finalized fact contract and rerun on the applied schema. D13(a) writes into stored Shopify status/salesChannels cells, lifecycle channel verbs and restore/recreate flows remain Wave 4/5 work.

**Outstanding requests and assumptions.**

1. FULFILLED: PR.1 applied the [original requested route hunks](requested-route-hunks.diff), extended the ordinary-row preflight across the feed/API split, and tested 39 real Fastify injection controls. These tests are included in the 563-test current run. End/deactivate/DELETE behavior remains intact. PR.4 respected the live route claim and did not duplicate its save.
2. REQUEST TO PR.5: publish the exact W2-SCHEMA-APPLIED gate before PR.4 writes/queries new columns. PR.5 owns production prerequisite/approval handling; PR.4 has not requested or performed a production write.
3. HANDOFF PENDING: PR.6 released the whole pure Presence module for a shared move **after its AT-WAVE-4 gate**, preserving its current final measurements. The [exact paired patch](pending-presence-runtime-pair.json) includes one canonical shared module, both DS re-exports and the exports-map entry. A virtual shared build passes and the staged built runtime matches the current DS across [4,000 verdict/line cases](pending-presence-runtime-probe.log), with no runtime web import. No source move or runtime READY receipt is claimed yet.
4. Fulfilled cross-lane requests: PR.2 outbound guards (file read, never edited by PR.4); PR.3 narrowing, FBA helper and label guard; PR.1 prepared-feed consumption and capability producer; PR.7 risk/gateNote re-exports and Shopify status carrier/drawer pair; PR.1/PR.2 sellingRisk repoints without D6 widening.
5. ASSUMED: legacy writers lacking an account/alias inspect all matching stored controls conservatively. `readPushControls` never chooses the transport account or grants authorization. Missing/unreadable controls refuse existing-listing writers; only explicit new-listing creation permits absent rows. Seller SKU comes from real product/variant relationships and the snapshot, not an invented ChannelListing.channelSku column. Intent remains optional until migration/client generation; legacy listingStatus is not substituted for the new intent.
6. ASSUMED: existing dry-run flags and explicit caller rehearsal flags retain precedence. Lifecycle takedowns must not be blocked by their own ordinary-push hold. Positive controls run with transport factories mocked; no environment deployment flag was changed.
7. OWNER QUESTION RESOLVED: PR.4 owns completion of the repository-wide sweep. PR.1's concurrent route handoff and PR.3's completed label guard were respected. No PR.4 ownership question remains. PR.5 owns the separate production prerequisite review; its local-only apply is not the required both-database W2-SCHEMA-APPLIED receipt.

The first metadata scratch fixture imported a heavy queue dependency and hit blocked Redis DNS; it was corrected before the clean rerun. The older Amazon rehearsal fixture likewise crossed the credential-resolving factory before its old SDK mock and hit blocked local DB access; the corrected factory mock retains the original sentinel/assertions. Neither attempt established a successful connection. A metadata source save overlapped PR.7's newly posted browser hold because it was not yet in the preceding ledger read; the overlap was disclosed promptly to PR.7. No browser acceptance is claimed by PR.4.

# Xavia family-consolidation session rolled back

**Current status: rollback complete.** The merchant canceled consolidation, confirmed the future Nexus family / separate Shopify colour-product approach, and explicitly requested undoing this session's work. No Nexus architecture implementation was authorized or performed during that discussion.

- The session-created MOSS draft `11136277741895` is deleted. All original 43 product IDs remain, with zero differences in Admin's displayed statuses, stock totals and variant counts against the original authenticated list.
- Rehearsal theme `200516206919` is absent after a fresh theme-library reload. It was already absent during this continuation, so no theme deletion was submitted here. Published theme `200489992519` remains **Nexus Impact 7.2.0 - performance**, last saved **Yesterday at 11:40 pm**; it was never edited in this session.
- The three session-created metafield definitions were deleted after each showed zero usage: product `nexus.resolved`, variant `nexus.resolved`, and variant `custom.media_with_text_product_page`. All 24 original product definitions and 14 original variant definitions match the original IDs and order. The unstructured variant metafield list is empty. The original product-level content and variation fields remain.
- All four AIREON relationship lists had already been restored and saved. Fresh readback verifies the original four product titles and uniquely matching thumbnails in the original order on every list. Their pre-existing cross-garment relationship remains intentionally restored.
- The session's local `linked_products.py` helper, its builder hooks, the managed-template repeater change, and the session README bullet were removed. The restored builder passes Python syntax parsing. This was a surgical reversal of known session changes; there is no complete pre-session source snapshot for a byte-for-byte comparison. Earlier integration work was retained.
- No original SKU, price, inventory, review, translation or order write requires reversal. The translation import was never submitted, the temporary test cart had already been restored empty, and no paid subscription was started. Current stock was read rather than overwritten with a snapshot.

Backups and historical canceled rehearsal artifacts are retained as evidence. **Do not upload or execute the canceled rehearsal packages.** This rollback does not resolve pre-existing AIREON grouping or REGAL colour-identity inconsistencies, and it does not certify a new exhaustive field-by-field store audit.

See [machine-readable rollback results](../../../output/shopify-family-migration-2026-09-09/rollback-result.json). **No session rollback actions remain.**

## Second rollback audit

The merchant requested an additional assurance pass. Fresh Admin readbacks again match all 43 original product IDs and displayed statuses/stock totals/variant counts. All four AIREON reference values were copied from native bulk-view cells and match the exact original IDs and order; no Save was performed. All 24 product and 14 variant definitions match the baseline IDs and order, and unstructured variant values remain empty. The fully expanded theme library excludes the rehearsal theme and retains the original published theme/save timestamp. All 13 rehearsal gallery media IDs are proven to have existed on the original MOSS pages before the session. Application source searches find no remaining session helper or hooks. No further store or application reversal was necessary. Only backup, audit and explicitly canceled rehearsal artifacts remain.

See [second verification results](../../../output/shopify-family-migration-2026-09-09/rollback-second-audit.json). These checks cover the recorded session changes, not a new exhaustive export of every pre-existing field, review or translation.

## Historical work before cancellation — superseded

The following entries preserve the execution history. Statements about a current draft, pending migration or future cutover below are historical and must not be treated as active instructions.

**Current scope: MOSS native draft only, starting from Shopify Products.** The merchant explicitly stopped expansion to other products, then asked to work from Products rather than the store editor. The current target is draft product `11136277741895` (`moss-jacket-native-rehearsal-2026-09-09`), with 12 Black/Grey size variants. It remains unpublished, with zero stock and zero channels. Nexus integration is deferred.

**Saved and verified:** all six Grey variants now reference the same five original Grey media-with-text metaobjects in the original order. The five previously missing assignments were saved through Products → Variants → Bulk edit. A full reload and native cell-copy readback verified the exact reference IDs for all six sizes. All 12 native display contracts were read back: seven Black gallery media IDs or six Grey IDs, in order, with the correct featured image and zero available/on-hand quantities. Black inherits the original five Black product-level content entries; their handles and order were rechecked. Shared descriptions, product content and recommendation fields remain present. These are Admin data checks, not a completed storefront rehearsal.

**Preview remains unresolved:** the private MOSS product preview loads published theme `200489992519`; adding draft-theme and `view=nexus` parameters did not change the preview bar's theme. The draft theme has the separate `product.nexus` template, but the product is assigned Default product. Native colour switching and all custom sections have not passed end-to-end verification on the intended draft theme. Do not report the original linked-product tests as evidence that the native draft works.

**Browser connection recovered in this session.** Claiming an existing Chrome tab through the supported runtime restored control despite tab-list timeouts. No new chat, login, reinstall or subscription is currently required. Retain the working connection and tab; old connection-failure instructions below are historical.

**Earlier scope expansion was reversed:** four AIREON `custom.variation_products` lists were briefly reduced to garment pairs. After the merchant objected, all four were restored and saved with the original four references in their original order: `10419669172551`, `10419681198407`, `10419699679559`, `10419704955207`. No AIREON SKU, inventory, price, image or review was edited. Do not resume other families without a new scope instruction.

**Draft theme history:** two prepared files (`snippets/product-info.liquid`, `assets/theme.js`) were saved to draft theme `200516206919` and reloaded with exact content verification. The corrected `templates/product.nexus.json` was saved earlier. The two-file refinement keeps selected sizes across original linked colour products. Original MOSS Black/M → Grey/M navigation was verified with seven/six gallery items, corresponding content and original Grey/M cart variant `52599321526599`; the initially empty test cart was restored empty. These checks concern the original products, not the native MOSS draft. No theme was published.

**Translation work:** the source product translations agree for title, description, type and SEO in German, Spanish, French and Italian; localized handles differ and remain reserved for their original products. A MOSS-only 20-row native translation CSV is prepared. LangShop's free product editor locks Spanish, French and Italian, so no paid feature was activated. Native translation import/readback remains in progress.

Shopify Basic cannot provide the actual Plus Combined Listings app resource. The target is the requested colour-specific storefront behaviour on the current subscription. Original source products remain recoverable. No source stock cutover has occurred. Full migration, preservation, mobile/keyboard/locale and native selector verification remain incomplete.

The [prepared two-file package](../../../output/shopify-family-migration-2026-09-09/combined-listings-theme/) preserves the other 240 exported files byte-for-byte. Theme Check reports 18 findings, including the existing product-info complexity guard (167/120). [Acceptance mappings](../../../output/shopify-family-migration-2026-09-09/combined-colours-acceptance.json) describe 466 possible original-size transitions; they are not 466 executed browser tests.

## Authenticated continuation through Chrome

The merchant identified Google Chrome and `https://admin.shopify.com/store/xaviaracing`. The installed Browser runtime selected Chrome successfully and verified the signed-in Xavia Racing Admin. Reuse that Chrome connection; neither a new login nor a Nexus connection is required. The earlier in-app Browser diagnosis below is historical.

- **Product backup downloaded:** the native authenticated CSV contains 43 products, 327 variants, 40 active products and 3 drafts. The additional drafts are IND Suit, MOSS Jacket and Knee Sliders. There are 280 empty SKU values across all 327 variants. Matching each of the 221 linked source variants by handle and ordered option values yields one export row each and zero SKU differences, including 187 confirmed blanks. The native CSV does not include Shopify IDs. A later seven-page authenticated Inventory capture verified product, variant and inventory-item IDs for all 221 linked variants with zero SKU/product-ID differences. It covers all 321 tracked variants on 42 products; six untracked gift-card variants explain the difference from 327. Per-location completeness and cutover reconciliation remain pending.
- **Review backups downloaded:** Judge.me exports contain 18 published reviews, zero hidden reviews and zero archived reviews. Published associations are GALE black 9, GALE yellow 1, EVO brown 2, VENTRA men 2 + 1, MISANO black 2, and the unlinked RISER Gloves 1. The exports retain text, dates, ratings, original product IDs/handles, replies and picture URLs, plus 18 unique review metaobject handles. They do not include a dedicated review-ID column or establish full verification-state/media-byte backup completeness. Private UI evidence was also saved. Nothing was reimported or reassigned.
- **Paid review grouping declined by merchant:** the authenticated app shows Forever Free, and both static and dynamic group creation are disabled. Awesome is offered at USD 15/month with a 15-day trial. The merchant explicitly declined purchasing Judge.me Awesome. Preserve reviews using a supported free approach if verified; the preservation requirement has not been waived. No trial or subscription was started. [Judge.me's supported Product Groups guidance](https://judge.me/help/en/articles/8375162-sharing-reviews-across-product-groups) confirms support for merged products without review reimport.
- **Identity evidence:** authenticated export titles and image filenames distinguish the two AIREON jackets from the two trousers. Both trousers have the legacy Type `Jacket`; retain that discrepancy in the source backup. Splitting the cross-link would produce 12 native families. REGAL women's `regal-jacket-1` (`10413384040775`) has a stored `Grey` label but black front/back images in Admin; `regal-jacket-2` (`10413388595527`) has `Gray` and a light-grey gallery with black panels. Both explicitly belong to WOMEN. Screenshots and page evidence are saved; physical stock identity is not verified, and no colour label has been changed.
- **Native inventory readback:** Chrome blocked the inventory download on the Shopify storage domain. No alternate request to that blocked download was attempted. The accessible Admin Inventory view was captured directly instead: seven pages, 321 unique tracked variants, no committed/unavailable/incoming stock in that view, and valid inventory totals. This is a timed snapshot, not proof of cutover stock or complete location activation. MOSS also has a linked Green draft with six variants, and Black/Grey use different custom-content references. Preserve draft publication and colour content.
- **Supplied native archives received and verified:** the merchant supplied the active-theme ZIP, all-state inventory CSV and full translation ZIP from Downloads. Private copies and SHA-256 checksums are recorded. The theme ZIP passes integrity checks (242 members). The inventory CSV has 321 tracked variants at the single exported location, Muhammad Awais (Xavia Racing), and zero SKU/available-quantity differences from the product CSV; committed, unavailable and incoming quantities are all zero at that snapshot. The translations ZIP passes integrity checks and contains five CSV parts, 59,912 rows covering German, Spanish, French and Italian. English is the default language. These supplied files resolve the earlier email/download retrieval gaps.
- **Draft theme rehearsal preparation:** published theme `200489992519` was duplicated to draft `200516206919`, named `Xavia native families - rehearsal 2026-09-09`. The live export is missing `templates/product.nexus.json`, although its supporting integration files exist. Shopify rejected the old local template because it retained bindings to a removed accordion repeater. The builder now removes those invalid bindings and retains the managed-content flag. The corrected template was saved only to the draft, reloaded and compared exactly. Existing live locale and country-flag revisions were preserved. Theme Check reports existing advisory findings and no new template issue; successful server save provides the compatibility check that the static checker missed.
- **MOSS native CSV imported to draft:** product `11136277741895`, handle `moss-jacket-native-rehearsal-2026-09-09`, has 12 Black/Grey size variants and 13 ordered existing image references. Authenticated readback verified exact SKUs (six populated, six blank), prices, compare-at prices, zero inventory and full description HTML. All destination product/variant/inventory-item IDs are mapped to their originals. The draft is published to zero channels. All 12 variants now have explicit seven-image Black or six-image Grey gallery assignments; colour content and storefront verification are in progress. Original Black/Grey stock and the separate Green draft remain untouched.
- **Additional authenticated coverage:** all 29 linked product metafield pages, all 29 unstructured metafield pages, all 88 referenced metaobject forms and all 30 collection pages are saved. All collection displayed item counts match captured links. Before rehearsal definitions were added, all 14 existing variant definitions showed zero usage and the unstructured variant list was empty. All 18 original numeric Judge.me review IDs are now uniquely matched to the native export; review text, original attribution and verified indicators are recorded. Media binaries, Markets/publication details, dependent apps and cutover order reconciliation remain incomplete.
- **Native tooling selected:** the merchant completed Matrixify startup, and New Export became accessible. Matrixify then requested additional scopes including store credit, shipping and translations; these were not approved by the agent. No export or import started. The merchant asked to use Shopify native product export/import instead, and execution is proceeding on that basis. Product CSV excludes variant metafields and changing option values can recreate variant IDs; use separate inventory/translation/content workflows and explicit source/destination mapping. The developer dashboard displays no existing apps; no app or credential was created. Installed dependency apps also include Veeqo, Marketplace Connect, LangShop, Flow, Search & Discovery, StarApps Variant Image, Bundles, PageFly, Section Store, Klaviyo, loyalty, shipping and returns apps. Their actual product references remain to be verified.

See [authenticated preflight evidence and checksum manifest](../../../output/shopify-family-migration-2026-09-09/authenticated-preflight.json). Original ZIP/CSV exports, review data and screenshots are saved under `output/shopify-family-migration-2026-09-09/private/`, excluded by that directory's `.gitignore`, with private directory/file permissions. The earlier public audit is unchanged.

**Zero families have passed the full rehearsal or been migrated live.** Complete media, Markets/publication and dependent-app backups, live-order/inventory reconciliation and all preservation readbacks remain outstanding. Do not treat the native CSV alone as a complete recoverable store backup or proceed to cutover based on this preflight.

## Evidence collected

The public Xavia Racing catalogue was downloaded on 9 September 2026, including all 40 product pages and an empty second catalogue page to check pagination completion. The snapshot includes 306 variants. Public data exposes 39 nonempty SKU values and 267 null/empty SKU values; those empty values require verification against Admin and must not be filled with invented SKUs.

Actual server-rendered colour selectors connect 29 products and 221 variants into 11 components. Of these linked variants, 34 expose SKUs and 187 expose empty SKU fields. These are **candidate groups**, not an approved migration manifest. The AIREON component contains two different garment types and needs splitting after authoritative identity verification.

- [Machine-readable audit and provisional source-variant mapping](../../../output/shopify-family-migration-2026-09-09/public-audit.json)
- [Downloaded public source snapshots](../../../output/shopify-family-migration-2026-09-09/raw/)
- [Read-only audit script](../../../integrations/shopify/migration/audit-public-catalogue.py)
- [Evidence parsing tests](../../../integrations/shopify/migration/test_public_audit.py)

The mapping records source product IDs, variant IDs, literal SKU values, colour labels, size options, prices, compare-at prices, availability, weight, shipping/tax flags and null destination IDs. Original catalogue JSON also retains descriptions, tags, image ordering and public product metadata. HTML and catalogue files have SHA-256 checksums in the audit. This is a public baseline, **not a complete backup**, and does not cover draft/archived products or other Markets.

## Candidate groups and exceptions

| Observed group | Products | Variants | Public empty SKUs | Key finding |
| --- | ---: | ---: | ---: | --- |
| MOSS Jacket | 2 | 12 | 6 | Black / Grey |
| AIREON jackets and trousers | 4 | 36 | 36 | Incorrectly cross-linked; split garment types after Admin verification |
| AIRMESH Jacket | 2 | 18 | 18 | Black / Yellow |
| EVO Gloves | 2 | 10 | 10 | Brown / Black |
| Xavia X leather suit | 7 | 49 | 49 | Seven distinct colour labels |
| VENTRA women | 2 | 16 | 16 | Preserve separate fit group |
| VENTRA men | 2 | 16 | 8 | Preserve separate fit group |
| REGAL men | 2 | 14 | 7 | Grey / Black |
| MISANO Jacket | 2 | 18 | 12 | Black / Brown |
| GALE jacket | 2 | 18 | 11 | Black / Yellow |
| REGAL women | 2 | 14 | 14 | Labels Grey / Gray; first image appears black |

Men/women labels above come from existing product tags and separate link sets. Matching titles alone are not a basis for merging. CLASSIC's men/women jackets have the same title but no colour links, so the scanner leaves them outside these candidate groups. Eleven total public products have no connected colour group.

Each component has matching ordered size values. Descriptions match within each component except the AIREON component, which mixes jackets and trousers. This does not establish parity for private metafields, metaobjects, gallery contents or translations.

**AIREON:** `aireon-jacket` and `aireon-jacket-1` are titled AIREON Jacket. `aireon-jacket-copy` and `aireon-pant` are titled AIREON Pant. Every selector links all four as colours, repeating Nero Neo and Crema e Vino. They cannot form an unambiguous Colour × Size family as currently linked. Preserve any intended outfit recommendations separately from colour variants.

**REGAL women:** `regal-jacket-1` is labelled Grey but its main image (`REGAL-JACKET-WOMEN-B.webp`) visually shows black. `regal-jacket-2` is labelled Gray and its main image (`REGAL-JACKET-WOMEN-E.webp`) visually shows light grey. This is a visual inference; confirm source item identity before assigning final colour values. Do not collapse these records into one colour merely by normalizing Grey/Gray spelling.

## Review preservation

Judge.me is installed. Its currently published guidance says bulk review reassignment is unavailable for merged products and recommends Product Groups; exporting and reimporting reviews risks duplicates. Product Groups share the original review records across the family while retaining their original product association. The feature requires the Awesome plan; the store's entitlement and existing groups have not been checked. [Judge.me transfer guidance](https://judge.me/help/en/articles/12050430-moving-reviews-from-one-product-to-another), [Product Groups documentation](https://judge.me/help/en/articles/8375162-sharing-reviews-across-product-groups).

Observed server-rendered summaries include GALE black 9 / yellow 1, EVO brown 2, VENTRA men 2 / 1, and MISANO black 2. These are widget summaries, not an authoritative inventory of reviews. REGAL men's original page and the original leather suit have no server-rendered per-product summary; absence must not be interpreted as zero reviews. Existing group sharing could also make sums misleading.

Before cutover, export/record original review IDs, original product associations, any recorded variant/SKU, text, ratings, dates, media, merchant replies, publication/verification state and existing group membership. Keep that private backup outside version control. Verify the same records remain accessible after grouping; compare unique review IDs, not just displayed totals. Do not create or alter reviews. Preserve legacy Shopify products as unpublished/archived records instead of deleting them. Check how retired products remain in Judge.me groups before changing publication status.

The result must distinguish preserved original review attribution from new family-level display. Product grouping does not establish a new SKU attribution for reviews that never recorded one. Verify selected-colour review presentation and the native template's Judge.me widget against the merchant's preservation requirement.

## Required authenticated work and acceptance criteria

1. **Authoritative backup and identity.** Read all active/draft/archived products and explicit colour-reference metafields with full pagination. Capture product/variant/inventory IDs, SKUs including blanks, barcodes, options, prices/costs, tax and shipping attributes, media and file references, descriptions, SEO, all metafields/metaobjects, translations, collection membership/order, publication destinations, Markets prices/catalogues, shipping profiles, selling plans and app references. Check actual Nexus canonical families and offer IDs; do not match blank SKUs by guesswork. Snapshot the active theme and app configuration. Establish completeness and checksum each backup.
2. **Concrete family manifest.** Resolve the AIREON cross-link and REGAL label defect. Keep fit/model/garment distinctions. Select a retained source product/handle where practical to minimize identity changes, recording the reason per family. Preserve the literal SKU of every existing variant, including any confirmed blanks; any separate SKU repair requires a resolved source-of-truth mapping. Establish old → new product, variant and inventory IDs before switching dependent systems. No two source sellable combinations may resolve to the same destination combination.
3. **Preview and comparison.** Rehearse one fully mapped family in a non-selling state. Preserve each colour's ordered gallery, descriptive alt text, custom content, feature cards, size guide and locale-specific data. Test native size/colour changes, cart variant identity, unavailable combinations, quick buy, desktop/mobile keyboard interaction, and configured Markets. The prior local fixture tests and theme installation do not prove this live behaviour.
4. **Reviews and dependent systems.** Verify supported Judge.me group membership and original review integrity. Update or prepare Nexus, inventory/fulfilment, feeds, product-specific discounts, recommendations, subscriptions and other actual app references using the ID mapping. Preserve original order associations; handle outstanding orders and returns explicitly. Do not create new paid app subscriptions without the merchant's decision.
5. **Controlled stock and publication cutover.** Re-read changed source data and stock immediately before writing. Account for inventory at each location and its available/committed/reserved states; do not duplicate sellable stock or zero committed stock blindly. Ensure only one product identity sells each SKU at the transition. Reconcile outstanding old-ID orders, fulfilment and asynchronous webhooks. Persist every successful operation and read back ambiguous outcomes before retrying. Shopify's separate writes are not an atomic transaction.
6. **URLs and merchandising.** Preserve original colour-card presentation and placement through the managed collection model. Record all old product/variant URLs and map them to the appropriate family/colour/size. Shopify redirects apply to broken source routes, so publication changes and redirect timing must be coordinated. Verify query strings, localized paths, collection-context links, canonical/SEO data, and bookmarks; a product-level redirect alone does not prove every old variant link works. [Shopify redirect guidance](https://help.shopify.com/en/manual/online-store/menus-and-links/url-redirect).
7. **Readback and rollback.** Compare every mapped variant and review record against the authoritative baseline. Require zero unexplained differences, full family coverage and no duplicate active sellable offers before marking a family verified. Keep originals and a per-operation journal. Rollback must first stop replacement sales, reconcile orders/stock received since cutover, restore source publication/app mappings and previous redirect/group settings, then verify again. Restoring stale snapshot inventory would be unsafe. Record unresolved differences openly; do not claim AAA certification or exact parity without evidence.

Shopify creates new variants under a destination product; their returned IDs must be mapped explicitly. SKU text is distinct from Shopify's internal variant/inventory identity. [Shopify variant creation](https://shopify.dev/docs/api/admin-graphql/latest/mutations/productVariantsBulkCreate), [variant updates](https://shopify.dev/docs/api/admin-graphql/latest/mutations/productVariantsBulkUpdate).

## Access and implementation state

- **Latest retry, 9 September 2026 at 10:15 UTC:** setup using the currently installed `browser/26.901.51231/scripts/browser-client.mjs` succeeds. Selecting the requested in-app Browser returns `Browser is not available: iab`; documented read-only browser discovery returns `[]`. The previous missing-service error did not recur. No browser or authenticated Shopify tab is exposed to this session, so the merchant's reported login remains unverified. The merchant was asked to open Shopify Admin in this Codex window's Browser and report when ready. No alternate browser was substituted.
- The existing read-only access check was rerun from `apps/api` and completed with `{"status":"ACCOUNTS","accounts":[]}`. Its first executable attempt was blocked by the sandbox's local-pipe restriction; the approved retry completed with exit code 0. No Shopify connector tools were available in the session. This check was only an alternate access investigation; the merchant subsequently clarified that Nexus integration is deferred. The current blocker is direct authenticated Shopify Browser access.
- The saved audit was reused without refreshing or overwriting it. All 42 recorded snapshot checks match: two catalogue JSON files by original-byte SHA-256 and 40 HTML files using the audit's original `read_text().encode()` hash convention, which normalizes line endings. The source mapping has 29 unique product IDs and 221 unique variant IDs; destination IDs remain unset. This confirms saved evidence integrity only, not current store state or authenticated backup completeness. See [latest access evidence](../../../output/shopify-family-migration-2026-09-09/access-retry.json).
- This retry made **zero store writes**. Authenticated backups, Admin SKU/identity resolution, Judge.me review/group verification, rehearsal, cutover and preservation readback remain unperformed. Existing migration authorization remains valid; restored authenticated access is the prerequisite.

Earlier session findings (the browser startup diagnosis below is superseded by the latest retry):

- The existing read-only Nexus access preflight, run from `apps/api`, reaches its local database and returns zero Shopify accounts.
- The root-configured database preflight fails because role `nexus_workspace_runtime` does not exist. No database roles or policies were changed.
- Browser control fails before selecting a browser: the session references missing `browser/26.901.31953/scripts/browser-service.mjs`, while the installed Browser package is `26.901.51231`. A retry produced the same error. Computer Use fallback also fails with a native-pipe startup error. These are tool startup failures, not evidence that the merchant is signed out.
- The merchant subsequently confirmed they had signed in to Shopify Admin. A fresh browser setup attempt, a control-session reset and another setup attempt still failed on the same missing `26.901.31953` service. The desktop fallback again failed to start its native pipe. No authenticated tab or store data could be inspected, so the reported login has not been independently verified. Restored browser control is needed to use that login; Nexus's server-side publisher separately needs a usable Shopify destination. Next recovery step: restart the Codex app and reopen this conversation with Shopify Admin still signed in, then retry the documented browser setup before repeating any catalogue work.
- No credentials were requested in chat, extracted from browser state, or printed. No publish gates were enabled. No store products were created, archived or deleted.
- The existing content importer reads selected source content, not a complete migration backup. It skips some field types/namespaces and does not establish all offers, inventory, reviews, SEO, collection and app migration dependencies. It must not be treated as a lossless catalogue migration by itself.

## Local verification

Six focused parsing/guard tests pass: matching titles and recommendation links do not trigger merges; explicit links preserve source SKU nulls without authorizing writes; cross-garment/duplicate-colour groups are flagged; asymmetric links are flagged; review summaries are scoped to the right product and missing is not zero; incomplete catalogue pagination fails. The scanner was then run against all downloaded pages. No platform UI, design-system files or application runtime code changed, so platform type/token/UI checks are not implicated.

```sh
python3 -m unittest discover -s integrations/shopify/migration -p 'test_*.py'
python3 integrations/shopify/migration/audit-public-catalogue.py \
  output/shopify-family-migration-2026-09-09/raw \
  output/shopify-family-migration-2026-09-09/public-audit.json
```

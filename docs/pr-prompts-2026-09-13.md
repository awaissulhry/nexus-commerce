# PR — Presence (the listing lifecycle programme) — session prompts, 2026-09-13

**What this is.** The Owner approved the Presence proposal as a repair programme with a UI at the end of it. Eight
sessions build Waves 0–3 and the parallel lane P concurrently; a ninth verifies everything at the end; Waves 4 and 5
(the first channel-reaching verbs, then the irreversible ones) are their own approval and are pasted later (§F).

**Sources (read from disk; the artifacts are the originals):**
- The proposal — `docs/2026-09-13-presence-proposal.md` (plain-text copy of
  https://claude.ai/code/artifact/cca49a82-b46c-4ae0-9ca2-e2d64779bcad). Sections: *Before anything else* (the ten
  do-first items), *What is actually true right now* (three ground reports), *The spine* (the state model),
  *The sequence* (waves and lanes), *Decisions only you can make* (24 — settled in §B below), *The specifications*
  (17, each with a summary, DS gaps, API, schema, open questions, risks and the full spec), *Deliberately not in this
  programme*.
- The audit — `docs/audits/2026-09-13-presence/audit-full.md` (plain-text copy of
  https://claude.ai/code/artifact/b348b831-98c7-47f8-ab16-83158283076a; 180 findings, 163 verified, refuted ones marked)
  and `audit-index.md` (titles only, grouped).
- `docs/2026-09-01-product-edit-studio-layout.md` §1b, §2.10, §5 — the studio's approved layout and coordination
  protocol. `docs/vt-prompts.md` § "The AAA bar" — the Owner's quality bar, binding here verbatim.
- `docs/pes-claims.md` — the ledger. Rulings newest-first at the TOP, lane sections at the BOTTOM; read both ends.

**Management.** The Owner manages every session directly (ledger #794 mode; there is no hub and no orchestrating
session). Sessions coordinate ONLY through the gate lines in §C, written into `docs/pes-claims.md`, and report to the
Owner in their own terminal and their own ledger section. Nothing is committed or pushed by anyone.

**Launch.** Each session in its own terminal from the repo root. Lanes PR.1–PR.8 on Opus 5; the closing session PR.F on
Fable 5.1:

```
cd ~/nexus-commerce && claude --model claude-opus-5
cd ~/nexus-commerce && claude --model claude-fable-5-1      # PR.F only, after every AT-WAVE-4 line is in the ledger
```

Paste the lane's block (§E) as the FIRST message, whole. Every block begins with one line naming the lane; a session
that arrives without its brief reads, claims nothing, and asks the Owner. Start PR.5 and PR.1 first (measurements and
guards gate everything), then PR.4 and PR.6 (they produce what others consume), then PR.2, PR.3, PR.7, PR.8 — all eight
have day-one work; stagger the starts by a few minutes so the first claims land before the next session reads the ledger.

---

## §A — Shared rules (binding on every session; the AAA bar in `docs/vt-prompts.md` applies verbatim on top)

1. **Read the ledger from both ends, then claim.** Rulings at the TOP (newest first — #794 hand-over, #753, #169,
   D9 #214, hub #12 migration staging, the 2026-09-12 WooCommerce ruling); lane sections at the BOTTOM. Append
   `### PR.n — <your session name> — <mandate> — 2026-09-13` at the bottom with the EXACT paths you will create or
   edit. Before claiming a path, `/usr/bin/grep -n "<path>" docs/pes-claims.md | tail -5`: a path inside a claim dated
   2026-09-13 by a session that has not posted a closing line is LIVE (MX.G / MX.P / MX.1 / MX.F may still be on
   `_studio/matrix/**` and `design-system/grid/{editors,renderers}/*matrix*`; `_studio/sheet/master/columns.tsx`,
   `master/channelColumns.tsx` and `design-system/grid/editors/{sheetColumn,shapeColumn,sheetWriter}.ts` were VT's).
   A live path is a ledger REQUEST to that session with the exact hunk, never an edit.
2. **Edit only what §D gives you.** A fix half in your file and half in another lane's: post the request with the hunk
   and wait; if it cannot wait, one lane does both and DISCLOSES to the owner first, then the ledger. Producer and
   consumer land in ONE write (a declaration in one tool call and its use in the next crashes the dev server for
   every session). Assert every scripted `replace` anchor — a missed anchor is a silent no-op.
3. **A claim must match its measurement.** Every "done", "works", "writes", "renders" carries the number, the
   `file:line` (cite CONTENT — line numbers move under nine writers), the test name or the screen reading. A write's
   RESPONSE is not what it wrote — read it back after 8 s. A negative needs a positive control in the same run.
   "Could not measure" and "measured empty" are different sentences; an unwitnessed zero neither passes nor convicts.
4. **Name the database before any number you cite and before any write.** `node scripts/_vtf-env-which-db.mjs`
   prints the host `apps/api` resolves from each CWD. Today: API `:8091` → `apps/api/.env` →
   `127.0.0.1:55439/nexus_development` (local Docker; GALE-JACKET `Product.version` 59 local vs 51 Neon); Neon is
   production. **Prod writes from a lane: NONE** (the one exception is PR.5's migration, on the Owner's own word in
   the terminal). Local web `:3000` hits whatever `NEXT_PUBLIC_API_URL` says — read the API origin in the network
   panel before any interaction that writes; assume production until proven otherwise. Fixtures: the XAVIA family
   only (GALE-JACKET `cmokmy3a40078pm0p1fvnu523`, MISANO, AIREON, XRI01) and disposable DRAFT rows under it; GALE's
   eBay·IT item 257584954808 and ASIN B0F7J163XJ are LIVE. **Channel-reaching writes: NONE in this programme** —
   every Wave 0–3 verb is local-reach and `verify` is a READ. Announce a fixture write in the ledger BEFORE it; restore
   by value, not by row; prove a rehearsal's write is reversible before it (AuditLog is append-only).
5. **Never `git checkout --`, `git stash`, `git reset`, `git restore`, `git add -A`, `--amend`** on the shared tree —
   every file carries other lanes' uncommitted layers. Nothing is committed or pushed. A test baseline is built by
   COPYING files aside. Ratchet attribution: dirty ∧ listed = suspect (from `git status --porcelain`); clean ∧ listed
   = already inside the baseline. Never raise a baseline to unblock.
6. **Typecheck etiquette.** `uptime` first; no tsc / vitest / gate / regen while the 1-minute load is above 8. Scope
   tsc to your package with a PRIVATE build-info file (`npx tsc --noEmit -p apps/web/tsconfig.json
   --tsBuildInfoFile <scratchpad>/web.tsbuildinfo`; API likewise) or `scripts/typecheck-scoped.mjs <files…>`; one run
   into a file, then grep; wait on your own pid, never on a pattern; release in slots of two. Exit codes are captured
   by REDIRECT (`cmd > log 2>&1; echo $?`), never through a pipe. `apps/api` vitest runs FROM `apps/api` (from the
   repo root it resolves DATABASE_URL to Neon — print the host first). `apps/web` vitest is node-only.
7. **Browser gates run ALONE.** Before `check-editor-open`, `check-layout-v2`, `check-control-census` or
   `check-grid-chrome`: post `PR-GATE-RUN <script> STARTING — <session> — <hh:mm>` in the ledger, confirm no other
   STARTING lacks its FINISHED, check `pgrep -f "node scripts/check-"` and the Chromium count, run, post FINISHED.
   While ANY session's run is open, no `apps/*` saves from anyone — a save recompiles the page under the measuring
   browser (feedback_a_hold_binds_the_one_who_asked). Check hydration before measuring (a server-HTML-only page has
   every control inert and reads as "nothing happened"). Open the web at `http://localhost:3000`, never `127.0.0.1`.
8. **`grep` in this shell is a function over `ugrep` that skips gitignored and NUL-carrying files** — use
   `/usr/bin/grep` with a positive control for every set claim. `tsx watch` restarts on the import graph only; a
   `.vitest.test.ts` save restarts nothing. `timeout` does not exist on macOS — a `timeout 600 …` wrapper reports
   EXIT=0 from "command not found".
9. **Build on the DS, from scratch.** Old trees (`edit/tabs/**`, `recover/**`, the cockpits) are specification, never
   source. A DS gap is fixed IN the DS by PR.6 (request it), never page-locally. One definition, zero copies: shared
   components are EXACTLY the same on every surface; `apps/factory` mirrors follow the fork-drift ratchet; a new DS
   piece ships with its `.d.ts`, its test and its catalog entry.
10. **Copy is verbatim from the proposal's specifications; sentences are derived, not authored.** The reversal
    sentence is rendered from `reversal{verb,fidelity}`, the refusal from the server's `refusal` field, the target
    restated from the row. No "Live" (it is "Listed"). An absent value renders as absence ("not checked", "never"),
    never as a default that looks like data (`check-wire-null-defaults` is the brief for that).
11. **You do not rule.** §B rules. A design question mid-build is a `QUESTION FOR THE OWNER:` line in your ledger
    section with the measurement and a recommendation, said again in your terminal — then you continue with the parts
    that do not depend on it. Every default you took is an `ASSUMED:` line. `REQUEST TO: PR.n` for cross-lane hunks.
12. **Servers.** Web `:3000` (pid 96911 at 17:55) and API `:8091` (pid 3185) are up and owned by other sessions —
    never kill or restart them. If one is down, say so; do not start a second on the same port; a verify server goes
    on `:3199` after `lsof -ti :3199`.
13. **Done means:** every item of your brief measured green; your gates on ONE clean run pasted with exit codes; your
    ledger section holds the numbers, every ASSUMED/QUESTION/REQUEST line, and "what I did NOT do and why"; then you
    post your `PR-GATE PR.n AT-WAVE-4` line and STOP. The Owner launches PR.F.

---

## §B — Decisions in force (the proposal's 24, taken at its recommendation; the Owner may edit this block before launch — the block as pasted binds)

| # | Decision | In force |
|---|---|---|
| D1 | CH.1 and menu tone | CH.1 stands (no new toolbar buttons); lifecycle lives only on the Listings surface. `MenuItemDef` gains `tone?: Tone` and a separator group; both adapters map `GridAction.danger`; a `.nds-menu` danger rule lands. SheetToolbar's silent force-disable during a pending write gains a reason. |
| D2 | D9 — new object | A NEW OBJECT with tab id `listings`, label **Listings**: first item of each per-channel nav group above "Listing information", plus one product-wide roll-up under THIS PRODUCT. Plural verbs (plan/apply) become buildable in Wave 4. |
| D3 | Legacy Listing Hub | Stays mounted on the legacy route until Wave 4 lands; retired in the same write that lands the plural plan. Nothing removed now. |
| D4 | RBAC | In-file preHandlers that throw 403 regardless of `NEXUS_RBAC_MODE`, in Wave 0; the real actor resolved in the same wave; `release` (Wave 5) is gated on the enforce flip, which is a separate ops decision. |
| D5 | Flat-file rule | Covers the EDITORS. Server-side routes and services are in scope: `ebay-flat-file.routes.ts` end/deactivate branches, the two removal services, the row read-filter, the `amazonFileExcluded` stamp. The editors, the FBA quantity field and the existing import path stay untouchable. |
| D6 | PD-2 widening | ONE write across both sites (preflight + cascade enqueue), showing both numbers — in Wave 4, after W1.1–W1.6, never before. Wave 1 moves the predicates into `packages/shared` without widening. |
| D7 | Empty-patch arm | Measure M4 first (PR.5); the fix ships as an announced ops change on the Owner's `PR-OWNER W1.9 GO` line, not before. |
| D8 | FBA | Fail-closed stands; presence emits a named Seller Central hand-off; `offerScope:'FBM'` only if M2 shows Offer populated. The two `isFbaCoordinate` predicates become ONE. |
| D9 | Etsy | READ-ONLY through Waves 1–4, stated in those words, never swept into the Woo rule. Guard the routes and delete the two broken write methods NOW (Wave 0). Ingest + enum + both dispatch branches in ONE write in Wave 5. |
| D10 | WooCommerce | Containment only: `dispatchSync` refuses lifecycle syncTypes; `syncToWoocommerce` calls `assertPushAllowed`; Woo presence renders read-only with the existing gate sentence. Labelled containment, never channel work. |
| D11 | eBay hold | M5 is measured (the Owner reads Seller Hub); until then eBay `unpublish` REFUSES rather than ending. If the preference is off, the shipped "deactivate — the safe hide" column's description is corrected retroactively. The qty-0 route becomes a named Wave 4 verb, not a quiet re-route. |
| D12 | eBay relist / ads | An eBay end is permanent for that ItemID; "publish again as a new listing" with lineage recorded; presence writes `ListingIdentity` only, never `EbayListingIndex`. GB/UK bidirectional normaliser only; reconciling stored values is its own data change. |
| D13 | Shopify lock | Wave 4 writes the intent into the stored `status`/`salesChannels` cells (a); the Wave 1 guard is (c): `assertPushAllowed` on `synchronizeContent`/`previewContentSync`/`syncNativeShopifyOffer`. One sentence states this connection is one webstore; NO Marketplace rows per Shopify market. The /products bin cascade downgrades to `end` for Shopify and says so (Wave 4). |
| D14 | Amazon `end` | Wave 4 states plainly: a reversible offer close and a permanent delete, nothing between. `patchListingsItem` is Wave 5 if wanted. Removing the preview-only refusal in `channelActions.ts` is its own approval line after Wave 3 has run in production. PR.5 records the running gate flags so every confirmation's copy is honest. |
| D15 | Scheduling | Stored and displayed, executed by nothing; the surface says "a note, not an alarm". Executor (LIFECYCLE kind on the existing scheduled-changes cron) is Wave 5 if wanted. |
| D16 | Stop-sale | Proposed, not automatic; product-scoped; two-step lift; `ListingIdentity.doNotRecreate` authoritative at the coordinate, `Product.lifecycleState` the product-level intent, read by ONE helper. Wave 5. |
| D17 | Identity moves | RELAUNCH-SKU, MOVE-ACCOUNT, ATTRIBUTE in Wave 5; MERGE and account consolidation out of scope. eBay MOVE-ACCOUNT cosmetic until MAP.4. `sku` leaves `writerAcceptsField`. |
| D18 | Restore / recover | Relist review is a MODE in History; edit-then-replan (AskToConfirm stays boolean); local write FIRST through the ordinary mutator; `/recover` guarded in Wave 0, folded into History in Wave 5. |
| D19 | Tokens | Stateless HMAC over the plan digest with a short TTL; plural typed-confirm REFUSED (per-coordinate verb offered); the literal-DELETE gate fixed now (exact, case-sensitive, and the phrase is the SKU / external id). |
| D20 | Red tree | Wave 1 lands with the reds stated and attributed; PR.1 captures the census as this programme's baseline; the swatch gate is repaired in Wave 0; `channelSnapshot` and `masterWrite` tests are fixed by PR.7. |
| D21 | DS semantics | `description != null` is the held discriminator; reason in BOTH `title` and `description`; `--nds-success-text` raised or the exception written into the token file with its reason; `ProvenanceMark` as `role="img"` + `aria-label`, browser-checked after. |
| D22 | Copy edges | Delete the three Italian keys (visible English fallback); the vocabulary gate scoped to `_studio/**` + listing/lifecycle key prefixes, ratcheted, excluding the flat-file enum; the "Unpublish (recommended)" string fixed now, the default flips to `none` with PD-2; a refusal may NAME a legacy control in prose, no hyperlink. |
| D23 | Toolbar | Honest subset only (units contract, typed status slot, portalled views prompt, padding specificity). The collapse ladder, the Transfer merge and the gutter are HELD for a separate ruling. The "3 columns" test changes only on the Owner's nod. |
| D24 | Fulfilment | Master cell relabelled "Fulfilment (product default)", not removed; amazon-mapper's `|| 'FBA'` on a null fulfilment fixed first; FCF.1 unchanged; the `matrix.routes` Offer writers RETIRED (this block is the nod; prove zero web callers first); the inbound-sync Offer writer repaired. |

Sequencing rules that bind like decisions: every additive migration is applied to BOTH databases and verified before it
is declared in `schema.prisma` (PR.5); no lifecycle DB write on a `dryRun` result, in any adapter; an adapter may never
perform a more destructive act than it was asked for; a transport failure is UNKNOWN, never SUCCESS and never FAILURE;
every presence write names all five levels or fails closed; a `verify` is enqueued after every lifecycle write.

---

## §C — Gate lines (how sessions wait on each other without a hub)

A gate is ONE line in your ledger section, exactly `PR-GATE <name> DONE — PR.n <session> — <ISO time> — <one-line
measurement>` (or the variant named below). Consumers poll it in the background and do independent work meanwhile:

```
( until /usr/bin/grep -q 'PR-GATE W1.1 DONE' docs/pes-claims.md; do sleep 600; done; echo GATE-OPEN ) > <scratchpad>/gate-W1.1.log 2>&1 &
```

Never wait on a process pattern; never infer a gate from a file's mtime — the line is the gate.

| Gate | Written by | Unblocks |
|---|---|---|
| `W0-SEC` | PR.1 | PR.2 W1.3 and W1.7 (the cascade block), PR.3 W1.4 — anything that changes what a destructive endpoint attempts |
| `W0-GATES` | PR.1 | PR.F's baseline comparison; PR.6's swatch-gate run |
| `W0-COPY` | PR.7 | PR.F |
| `M1 — host=<…>` `M2` `M3` `M4` `M6` `M7` | PR.5 | `M1`: every count anyone quotes; `M3`: PR.3 W1.4; `M4`: PR.2 W1.9 (with the Owner's GO); `M2`: PR.6/PR.8 whether `offerScope` appears anywhere |
| `M5 PENDING-OWNER` → `M5 DONE` | PR.5 (after the Owner answers) | Wave 4's eBay verb copy |
| `W1-SHARED-STUB` | PR.4 | PR.2 W1.5-QUEUE; PR.4's own W1.5-PUSH |
| `W1-COORD` | PR.3 | PR.2 W1.7 (payload coordinate), PR.4's flat-file narrowing hunk |
| `W1.1` `W1.2` `W1.3` `W1.5-QUEUE` `W1.7` `W1.9` | PR.2 | Wave 2 needs W1.1–W1.6 |
| `W1.4` `W1.6-OFFER` | PR.3 | Wave 2 |
| `W1.5-PUSH` `W1.6-GATE` `W1.8` `SHOP-P` | PR.4 | Wave 2; PR.7's `identityHeld` import (W1.8 posts the path) |
| `W2-SCHEMA-STAGED` → `W2-SCHEMA-APPLIED — local=<…> prod=<…>` | PR.5 | PR.1 reads of the new columns; PR.4's channelFact writes (SHOP-P2 second half) |
| `W2-DS-TYPES` (export names listed) → `W2-DS` | PR.6 | PR.1 W2-READ-SHAPE (imports the unions), PR.7 W2-WIRE, PR.8 P-TOOLBAR's typed slot |
| `W2-READ-SHAPE — docs/pr-presence-wire.md` → `W2-READ` | PR.1 | PR.7 W2-WIRE, PR.8 W3-SURFACE |
| `W2-WIRE` | PR.7 | PR.8 W3-SURFACE, PR.1 W3-API (the verbs' client contract) |
| `W3-API` | PR.1 | PR.8 verbs, PR.7 W3-VOCAB (the errorCode/refusal copy) |
| `W3-VOCAB` `W3-SURFACE` `P-TOOLBAR` `P-A11Y` | PR.7 / PR.8 / PR.6 | PR.F |
| `PR.n AT-WAVE-4` | each lane | PR.F starts when all eight are present |
| `PR-GATE-RUN <script> STARTING/FINISHED` | any | serialises browser gates (§A.7) |
| `PR-OWNER W1.9 GO` · `PR-OWNER MIGRATION APPLY` · `PR-OWNER WAVE-4 APPROVED` | the Owner (or a lane quoting the Owner's terminal words verbatim, with the time) | W1.9; the prod migration; §F |

Wave blocking, restated: **Wave 1** items that widen what a destructive path attempts wait for `W0-SEC`; W1.4 waits for
`M3`; **Wave 2** waits for W1.1–W1.6 and `M1` (the DS contract, PR.6, has no data dependency and starts day one);
**Wave 3** waits for all of Wave 2 (D2 and D22 are settled in §B). **Wave 4 and 5** wait for `PR-OWNER WAVE-4 APPROVED`.

---

## §D — Ownership (one owner per path; a path not listed is claimed by whoever needs it FIRST in the ledger, and stays theirs)

| Lane | Owns (create/edit) |
|---|---|
| PR.1 routes · auth · read API · local verbs | `apps/api/src/routes/{amazon-flat-file.routes.ts (guard hunks only), etsy.ts, matrix.routes.ts, listing-recovery.routes.ts, product-studio.routes.ts (additive)}`; `apps/api/src/routes/products-catalog.routes.ts` EXCEPT the `bulk-hard-delete` cascade transaction block (PR.2's after `W0-SEC`); `apps/api/src/lib/auth/**`; NEW `apps/api/src/services/presence/**`; NEW `apps/api/src/services/products/operational-impact.service.ts`; `apps/api/src/services/pim/listing-alias.service.ts`; `apps/api/src/services/marketplaces/etsy.service.ts` (deleting the two write methods only); `apps/api/src/services/products/bulk-edit.service.ts` (the contentAddress hunk); `apps/api/src/services/pim/studio-sheet.service.ts` (LISTING_SELECT additive); `scripts/check-grid-swatch-contrast.mjs`; NEW `scripts/check-recreate-guard.mjs`; NEW `docs/pr-presence-wire.md`; `docs/audits/2026-09-13-presence/baseline/**`; tests beside each |
| PR.2 the delist path | `apps/api/src/services/channel-delist.service.ts`; `apps/api/src/services/outbound-sync.service.ts`; `apps/api/src/workers/bullmq-sync.worker.ts`; `apps/api/src/services/outbound-enqueue.ts`; the `bulk-hard-delete` cascade block of `products-catalog.routes.ts` (after `W0-SEC`; announce each save to PR.1); `apps/api/src/services/ebay-trading-api.service.ts` (`siteIdForMarket` + site-id resolution hunks only); NEW `apps/api/src/routes/delist-cascade.routes.ts`; NEW `apps/api/src/services/delist-error-codes.ts`; tests |
| PR.3 narrowing · offer services | `apps/api/src/services/ebay-flat-file-delete.service.ts`; `apps/api/src/services/amazon/amazon-flat-file-remove.service.ts`; `apps/api/src/services/amazon-market-offer.service.ts`; `apps/api/src/routes/sync-control.routes.ts`; `apps/api/src/routes/marketplaces.routes.ts`; `apps/api/src/services/listings/recovery.service.ts`; `apps/api/src/services/ebay-label-guard.service.ts`; `apps/api/src/services/listing-reconciliation.service.ts`; `apps/api/src/services/amazon-mapper.service.ts` (the `|| 'FBA'` hunk, D24); NEW `apps/api/src/lib/listing-coordinate.ts`; tests |
| PR.4 push lock · gate · shared · Shopify | `packages/shared/**`; `apps/api/src/clients/amazon-sp-api.client.ts`; `apps/api/src/services/amazon-publish-gate.service.ts`; `apps/api/src/services/pricing-outbound.service.ts`; `apps/api/src/services/amazon/flat-file.service.ts` (skip_offer path + the narrowing hunk on PR.3's helper); `apps/api/src/services/amazon-eu-quantity-guard.ts`; `apps/api/src/services/shopify/{content-sync.service.ts, offer-sync.service.ts, channel-sheet-projection.ts, listing-information-plan.ts, content-publisher.ts}`; NEW `scripts/check-push-lock.mjs`; tests |
| PR.5 measurements · schema | Wave 0: NO repo source files (results under `docs/audits/2026-09-13-presence/measurements/**`, scripts in the scratchpad); Wave 2: `packages/database/prisma/schema.prisma` (additive), `packages/database/prisma/migrations-pending/<pr folder>` → `prisma/migrations` at apply time, `packages/database/scripts/**` (grants/RLS), migration tests |
| PR.6 design system | `apps/web/src/design-system/**` + `apps/factory/src/design-system/**` mirrors; ALL `.d.ts` regeneration; `.claude/DS-GAPS.md` (append only); NEW `scripts/check-focus-visible.mjs`, `scripts/check-token-role.mjs`; `scripts/check-silent-disabled.mjs`; the DS catalog page; tests |
| PR.7 studio copy · wire · vocabulary | `apps/web/src/app/products/[id]/edit/_studio/**` EXCEPT `_studio/listings/**`, `_studio/sheet/SheetToolbar.tsx` (+ its tests), `_studio/navigation.tsx` / `StudioSubheader.tsx` / `StudioTabHost.tsx` mount hunks (PR.8) and `_studio/matrix/**` (MX); `apps/web/src/app/products/_components/BulkActionBar.tsx`; `apps/web/src/lib/i18n/messages/{en,it}.json` (studio + listing/lifecycle keys); `apps/api/src/services/pim/family-projection.service.ts` (the `excludedReason()` hunk only — announce); NEW `_studio/presence/**` (the meta module and loaders); NEW `scripts/check-presence-vocabulary.mjs`; tests |
| PR.8 the Listings surface · toolbar | NEW `_studio/listings/**`; `_studio/navigation.tsx`, `StudioSubheader.tsx`, `StudioTabHost.tsx` (tab id + mount hunks only); `_studio/sheet/SheetToolbar.tsx` + tests; new rows in `scripts/check-control-census.mjs` and `scripts/check-layout-v2.mjs` (the listings surfaces); `docs/audits/2026-09-13-presence/w3/**`; tests |
| PR.F closing | Everything, read-mostly: `docs/audits/2026-09-13-presence/final/**`; fixes only for defects it finds, each disclosed to the owning lane's section |

Files nobody above owns that a wave item touches (`apps/api/src/services/pim/readiness.service.ts`,
`sheet-rows.service.ts`, `channel-ops/*` in the API, `apps/web/src/app/products/next/**`) are claimed first-come in
the ledger with the hunk named; the closing lane checks every such claim.

---

## §E — The prompts (paste one block per session, whole, as the first message)

### PR.1 — routes, authorisation, the read API, the local verbs

```
You are PR.1 (routes · authorisation · the read API · the local verbs) on the Presence programme.

READ FIRST, in this order: (1) docs/pr-prompts-2026-09-13.md — §A rules, §B decisions in force (D4, D5, D9, D18, D19, D24 are yours), §C gate lines, §D ownership; (2) docs/2026-09-13-presence-proposal.md — "Before anything else" items 1, 9, 10; "The sequence" → Wave 0 (W0-SEC, W0-GATES), Wave 2 (READ API), Wave 3 (W3-API); "The spine" → API and PRECONDITION P9; the specifications "One operational-impact service…", "Repair the delist path…" (the two live in-process callers of dispatchChannelDelist), "The Etsy adapter…" (the unguarded routes), and the API blocks of the four adapter specs; (3) docs/audits/2026-09-13-presence/audit-index.md → groups "The methodology critic" (the preHandlers finding), "What a takedown touches" (default-user, products.delete unused, API-key family, no audit row), "The question you asked" (alias archive has no un-archive writer; exclude→include is one-way); (4) docs/2026-09-01-product-edit-studio-layout.md §1b, §2.10, §5; (5) docs/pes-claims.md from BOTH ends (rulings #794, #573 "a projection is one channel on one market", hub #12; lane sections at the bottom — MX.1 was the last API lane, check its claimed paths). Memory traps for this area: reference_session_rbac_cannot_gate_a_service_caller, reference_family_verbs_split_permissions, reference_api_route_probe_by_inject, reference_attr_write_needs_marketplace_context, reference_product_version_not_row_version, reference_check_what_else_read_from_what_you_narrowed, reference_a_list_of_members_is_a_set_claim.
RULES: §A verbatim. Claim your §D paths at the bottom of the ledger BEFORE editing. Prod writes: none. Channel writes: none. Nothing committed.

YOU OWN: the §D row for PR.1. products-catalog.routes.ts is shared with PR.2 by HUNK: you own everything except the bulk-hard-delete cascade transaction block; announce every save of that file in the ledger with the hunk's first line, and read PR.2's announcements before each of yours.

YOUR ITEMS, in this order.

WAVE 0 — start now.
1. GUARDS (do-first #1, D4). Explicit in-file preHandlers that throw 403 regardless of NEXUS_RBAC_MODE (rbac-hook.ts defaults to shadow) on: POST /api/amazon/flat-file/remove (amazon-flat-file.routes.ts — measured 29 route registrations, 0 auth references); POST /api/products/bulk-hard-delete (products-catalog.routes.ts; the file's only preHandler is on a different route); all six app.post registrations in routes/etsy.ts (POST /etsy/sync/inventory/to-etsy reaches a live PATCH); PUT /api/products/:id/matrix/channel-listing/:listingId (matrix.routes.ts — also scope the where to productId: today `where:{id:listingId}` has no product scoping); the three listing-recovery routes (listing-recovery.routes.ts says at its head that it has no guard while executeRecovery permanently deletes and renames Product.sku globally). Each guard names its permission from the manifest's EXISTING vocabulary (`products.delete` for the destructive ones — it is grantable and required by no route today; `products.publish` where a channel is reached; list your choices in the ledger). Manifest entries ordered BEFORE the /api/products prefix rule (first-match-wins already produced one unreachable rule here); extend permissions-manifest-order.vitest.test.ts with every new entry. Prove each with app.inject(): 403 without the permission, past the guard with it, with NEXUS_RBAC_MODE unset. Run `npx tsx apps/api/src/scripts/check-rbac-coverage.ts` and paste the result.
2. ACTOR (do-first #1, D4). `userIdFor()` returns the literal 'default-user' (products-catalog.routes.ts ~:82-84) and the literal appears at ten non-test sites (derive the list with /usr/bin/grep, positive control on a string you know exists). Resolve the request's real user on every guarded route and every audit row it writes; fix the sites in your files; `REQUEST TO:` the owner for each site in another lane's file. Test: the audit row of a guarded route carries the session user, never the literal.
3. ETSY (D9). Delete the two legacy Etsy write methods (they PATCH price and quantity to an endpoint whose vendored update schema contains neither) and the route registrations that reach them; the read client stays; state in a header comment on routes/etsy.ts that Etsy presence is READ-ONLY through Wave 4 by decision D9. Prove zero remaining callers with /usr/bin/grep.
4. MATRIX OFFER WRITERS (D24). Prove zero web callers of the matrix.routes Offer writers (/usr/bin/grep over apps/web/src with a positive control), then retire them (remove or 410 with one sentence). The route registration count before/after in the ledger.
5. RECOVER PAGE (D18). The /recover routes are guarded by item 1; add a header comment on listing-recovery.routes.ts naming the Wave 5 fold into History. Nothing else on that page now.
6. W0-GATES (do-first #10, D20). Repair scripts/check-grid-swatch-contrast.mjs: it requires a literal `.dark {` block and tokens.css declares `.dark, .dark body:has(.nds-theme-responsive), .dark body .nds-theme-responsive {` — parse the block by its selector LIST. Seed it RED on purpose once (a temporary token in a scratch copy, never the shared tokens.css) to prove it convicts, then run it bare and paste the exit code. Then capture this programme's BASELINE into docs/audits/2026-09-13-presence/baseline/ — one file per instrument with the exact command, the exit code by redirect, the time and the load: check-grid-kit-ratchet (34 vs 32 with the importer list; attribute by dirty∧listed, never a stash), check-wire-null-defaults (28 vs 17, all 28 rows), check-ds-dts-fresh (the stale list), check-route-prisma-ratchet (brand-story 32→33 attributed), the studio vitest run made hermetic (NEXT_PUBLIC_API_URL=http://127.0.0.1:1 PES3_API=http://127.0.0.1:1 npx vitest run "src/app/products/[id]/edit/_studio" from apps/web — expect 6 failures in 2 files; if the count differs, say so, it is a reading), web tsc, api tsc. This is what PR.F compares against.
7. bulk-edit contentAddress (ground:tests, outside the waves, live). apps/api/src/services/products/bulk-edit.service.ts calls contentAddress(change.contentAddress) for EVERY change before the isLocalizableContent branch and refuses the WHOLE batch if any change lacks one — every /api/products/bulk caller without a per-change contentAddress is refused, including POST /products/:id/restore. Check the ledger for a live claim on the file (it was LX's); claim the hunk; write the failing test first (a non-localizable change without contentAddress is accepted; a localizable one without it is refused by NAME); move the requirement inside the branch; prove the restore route with a test.
8. Post `PR-GATE W0-SEC DONE` (after items 1–5) and `PR-GATE W0-GATES DONE` (after 6). Say in the ledger which permission each guard requires — PR.7's copy and PR.F's checks read it.

WAVE 2 — the read API. Start the wire doc as soon as `PR-GATE W2-DS-TYPES DONE` is in the ledger (you import PR.6's unions); implement reads of the NEW columns only after `PR-GATE W2-SCHEMA-APPLIED`; everything else after W1.1–W1.6 and M1.
9. FIRST, docs/pr-presence-wire.md — the response shapes of GET /api/products/:id/studio/presence (every real coordinate plus every Opening: intent WITH provenance when derived, fact + asOf + via, verdict derived on read, gate mode, per-verb availability {available, reach, reversal, refusal (the server's sentence), handoffs}, fan-out as NAMED coordinates never a count), GET …/presence/one (400 naming the missing dimension; requireCoordinate's shape widened to account and alias), GET …/presence/history (ProductEvent for the coordinate incl. retired ListingIdentity rows), GET …/presence/amazon-posture, POST …/presence/verify, POST /api/products/operational-impact. The intent/fact/verdict unions are IMPORTED from PR.6's design-system/grid/renderers/presence.ts and named by path in the doc; RowReadinessState is restated in four places today — do not add a fifth. Post `PR-GATE W2-READ-SHAPE DONE — docs/pr-presence-wire.md`.
10. Implement the reads under services/presence/. NO BACKFILL and no derivation that turns an ended eBay row ({listingStatus:'ENDED', offerActive:false, externalListingId:null}) into "never published" — a null fact is UNKNOWN; a null intent is 'never stated' with a provisional intent derived from the legacy columns and carried WITH its provenance. A fact older than the last intent change with nothing in flight degrades the verdict to `unknown`, never to agreement. Etsy and WooCommerce coordinates carry the honest gate sentence (channel-ops/syncQueue.ts's `gateNote` vocabulary, read from the API side or re-exported — one definition).
11. POST /presence/verify — a channel READ that writes only channelFact/At/Via/Detail; extends the existing operator-triggered read path (ChannelTruthPanel / live-channel-images refresh), reuses getListingsItem / getItemListingStatus / the Shopify product read; four-valued outcome with 'could-not-ask' load-bearing; per-call coordinate cap and an hourly ceiling (Amazon reports run a serial queue); refuses when the gate mode cannot answer. Enqueue hook for Wave 3's writers. Rehearse against GALE's LIVE eBay·IT coordinate (a read) on the LOCAL DB and read the columns back after 8 s.
12. Projection, no migration: offerClosedAt/offerClosedBy/offerCloseReason and syncPaused into LISTING_SELECT and onto the studio wire (zero occurrences of offerClosedAt in apps/web/src today); Product.deletedAt; Marketplace.isParticipating/participationStatus; connection health as a tri-state so a FAILED read never renders as "not connected"; a `sources` block on the sync-queue read naming the three tables it does and does not query. studio-sheet.service.ts is claimed additively — announce.
13. operational-impact.service.ts: extract the five queries inside GET /products/hard-delete-preflight; add POST /api/products/operational-impact (coordinate-shaped body, read-only, one query per check partitioned in memory — only ChannelListing has the composite index). The two NEW checks (stock holds, ads) are ADVISORY: status 'unavailable' in a `checks` key, never inheriting the GET's all-or-nothing 500 and never refusing the verb. The GET keeps its shape (BulkActionBar reads it). Tests for every check incl. the unavailable arm.
14. Post `PR-GATE W2-READ DONE` with app.inject timings on GALE (name the DB).

WAVE 3 — the local-verb writers. After `PR-GATE W2-WIRE DONE` and all Wave 2 gates.
15. Writers under services/presence/, each five-level or 400, expectedVersion → 409 with `current`, real actor on the audit row, a ProductEvent with aggregateType 'ChannelListing' and the full coordinate in `data`, a `verify` enqueued after the write, and a test that stubs fetch BEFORE the import and asserts ZERO outbound channel calls: hold/resume (syncPaused — honoured on every push path after PR.4's W1.5); exclude-child/include-child (the raw-SQL variationExcluded helpers in family-projection.service.ts, BOTH directions — exclude→include is a one-way ratchet today; a re-included child must be pushable again); discontinue/undiscontinue (presenceIntent + At/By/Reason; intent only; blocks create); presenceEffectiveFrom/until stored and returned, executed by nothing (D15). PATCH /api/products/:id/aliases/:aliasId { status: 'ACTIVE'|'ARCHIVED' } with a real restoreAlias beside archiveAlias in listing-alias.service.ts (there is no restore writer anywhere today — verify); refused while the alias holds identity with a non-terminal intent; inert until PES.5-ii — the response says so in one sentence.
16. INTENT IS A LOCK — the recreate guard. One helper `assertRecreateAllowed(coordinate)` reading ListingIdentity: RELEASED or doNotRecreate refuses recreation without an explicit override flag. Derive the list of every non-test channelListing.create/upsert site with /usr/bin/grep (the spine counts 46 — re-measure), post it in the ledger, wire the sites in files you own, `REQUEST TO:` the owner for each other site with the exact hunk, and write scripts/check-recreate-guard.mjs counting sites vs guarded (seeded red first). The enqueue-side refusal of terminal intents is PR.2's (dispatch) and PR.4's (push) through assertPushAllowed — verify by reading their code, not their comments.
17. A gate that asserts no writer sets two of {isPublished, offerActive, syncPaused, presenceIntent} in one statement, with the Amazon skip_offer pairing declared as the one sanctioned exception (add it to check-recreate-guard.mjs or a sibling; positive control).
18. Post `PR-GATE W3-API DONE`, then `PR-GATE PR.1 AT-WAVE-4` with: every route added (method, path, permission), every gate's exit code on one clean run, the studio/api vitest counts, "what I did NOT do and why", and every ASSUMED/QUESTION/REQUEST line.

DONE = items 1–18 measured green, api tsc 0, api vitest green from apps/api with the DB host printed, check-rbac-coverage green, both new gates green with their positive controls, ledger numbers. STOP and ask the Owner only for: any prod write, any non-additive change, a permission name outside the manifest's vocabulary, a deletion not named above, a conflict with another lane's live claim.
```

### PR.2 — the delist path: queue, dispatch, worker, cascade

```
You are PR.2 (the delist path — queue · dispatch · worker · cascade) on the Presence programme.

READ FIRST, in this order: (1) docs/pr-prompts-2026-09-13.md — §A rules, §B decisions (D7, D9, D10, D11, D12, D13 rider), §C gates, §D ownership; (2) docs/2026-09-13-presence-proposal.md — "Before anything else" items 2, 3, 7; "The sequence" → Wave 1 (you are W1-QUEUE; the internal order is binding: no-escalation and honest outcomes FIRST); "The spine" → PRECONDITIONS P1, P2, P3, P4, P6, P7 and section 7 "Making the irreversible step hard"; the specifications "Repair the delist path before any lifecycle UI is wired — corrected" (all of it — two dispatchChannelDelist callers are LIVE today, and D5 must land last), "Presence on Shopify" (SHOP-P1, SHOP-P3, SHOP-P4), "Presence on eBay" (the connection that OWNS the ItemID; GB hits every Trading call; ENDED stops nulling externalListingId is WAVE 4, not yours), "The Amazon adapter" (the seller SKU resolver); (3) docs/audits/2026-09-13-presence/audit-index.md → "The question you asked" (cascade rows destroyed by FK cascade), "Channel by channel" (ASIN in the SKU slot; eBay UK throws; "already ended" swallows "cannot be accessed"; Shopify double-wrapped body; Woo rows refused then cascade-deleted), "What a takedown touches" (parked PENDING with no retry then green via the update path; empty patch records SUCCESS; primary-account eBay); (4) docs/pes-claims.md from both ends. Memory traps: reference_transport_failure_write_is_unknown_outcome, reference_outbox_publish_before_stamp, reference_event_durable_vs_ephemeral_lane, reference_cron_is_per_process, reference_the_quoted_exit_path_skipped_the_notice, reference_write_predicate_must_match_its_readers, reference_prisma_upsert_on_conflict, reference_node_probe_pure_modules (stub fetch BEFORE the import).
RULES: §A verbatim. Claim your §D paths first. Prod writes: none. Channel writes: none — every rehearsal runs with `fetch` stubbed and the Amazon gate in dry-run on the LOCAL Docker DB, and asserts zero outbound calls. Nothing committed.

YOU OWN: the §D row for PR.2. channel-delist.service.ts carries all four adapters and has ONE writer: you. products-catalog.routes.ts is shared with PR.1 by HUNK — you own ONLY the bulk-hard-delete cascade transaction block, and only after `PR-GATE W0-SEC DONE`; announce every save with the hunk's first line. outbound-sync.service.ts is yours; PR.4 publishes assertPushAllowed in packages/shared and you wire it into every push path inside your file.

YOUR ITEMS, in this order (the Wave 1 step numbers are the proposal's).

Start now (these refuse and narrow; they widen nothing, so W0-SEC is not needed for them):
1. W1.1 NO ESCALATION. The `action === 'unpublish'` branch of the Amazon adapter logs a warning and then calls deleteListingsItem unconditionally — delete the fall-through and REFUSE with a named errorCode ("Amazon has no reversible unpublish in this client yet") until patchListingsItem exists (it does not; verified). Delete the duck-typed Shopify `typeof svcAny.updateProduct === 'function'` → deleteProduct path; delistShopify returns { success:false, retryable:false, error } for BOTH actions instead of the double-wrapped 2xx (SHOP-P1; note both current failure arms — a missing env credential throws into a retryable loop, present credentials produce the green lie). Tests: an Amazon unpublish NEVER reaches deleteListingsItem; a Shopify delist NEVER reaches deleteProduct; both assert the outbound stub was not called. Post `PR-GATE W1.1 DONE` and say in the ledger that the delist dashboard goes visibly RED on day one by design.
2. W1.2 HONEST OUTCOMES. Split ALREADY_ENDED_PATTERNS into ENDED_CONFIRMED and COULD_NOT_ASK (`item cannot be accessed`, `invalid item`, `item not found` and the not-found arm are ownership/reachability refusals → outcome UNKNOWN, fact REFUSED, never success). A transport failure is UNKNOWN — never SUCCESS, never FAILURE. applyDelistResultToQueue sets holdUntil on every retryable park. dispatchSync REFUSES lifecycle syncTypes outright (it has no lifecycle branch; the BullMQ worker does) instead of routing them into syncToAmazon/syncToShopify/syncToWoocommerce — this is the whole WooCommerce containment (D10) and SHOP-P4 (today an UNPUBLISH_LISTING row on a Shopify listing without nexusFamilyId falls through to `newQty = payload?.quantity ?? 0`, sets inventory to 0 and reports SUCCESS). New errorCodes on OutboundSyncQueue.errorCode in services/delist-error-codes.ts, each with OPERATOR copy (a code nobody renders is a different silence) — post the module path for PR.7. Tests per pattern and per syncType. Post `PR-GATE W1.2 DONE`.

After `PR-GATE W0-SEC DONE`:
3. W1.3 TARGETING. Amazon delist resolves the SELLER SKU (Offer.sku for the scope, else Product.sku) — the producer joins product:{select:{sku:true}} at enqueue and carries sellerSku on the payload, because by drain time the Product row is gone — and never passes externalListingId (flat-file-pull populates it with the ASIN). eBay resolves the connection that OWNS the ItemID (never tryResolveConnection({channel:'EBAY', primary:true})). The worker reads targetChannel from the queue row it just loaded, not from job.data. The eBay site-id resolution moves OUT of the auth try/catch and returns EBAY_UNKNOWN_MARKET; siteIdForMarket gains a bidirectional GB/UK fold — it is called from reviseInventoryStatus, getItemListingStatus, getItemQuantities, reviseInventoryStatusBatch and tryRemoveVariationFromListing (re-derive the caller list; a test per caller). Post `PR-GATE W1.3 DONE`.
4. W1.5-QUEUE (after `PR-GATE W1-SHARED-STUB DONE`). Repoint the dispatch refusal (the syncPaused + offerClosedAt test in outbound-sync.service.ts) to PR.4's assertPushAllowed and call it on EVERY push path inside your file — syncToShopify and syncToWoocommerce have ZERO syncPaused checks today (measured), the STATUS_UPDATE path too. PR.4's scripts/check-push-lock.mjs reads your file; run it. Post `PR-GATE W1.5-QUEUE DONE`.
5. W1.7 CASCADE SHAPE (after `PR-GATE W1-COORD DONE`). Enqueue delist rows with productId:null and channelListingId:null (both already nullable — no migration), the full five-level coordinate on the row's own columns plus payload incl. channelConnectionId, aliasKey and sellerSku (SHOP-P3 — the adapter REFUSES a job with no connection id rather than falling back to env credentials), holdUntil +5 minutes, POST /api/products/delist-cascade/cancel (the worker already honours CANCELLED; without the route the five minutes is a delay with nothing to undo to — guard it like PR.1's routes), channelCascadeDispatched read back AFTER commit (not the pre-commit create count), channelSkipped naming every coordinate that stays live because no adapter exists (Etsy; WooCommerce). The liveness predicate at the enqueue stays as it is — PD-2 widening is Wave 4 (D6); PR.4 posts the sellingRisk import path in W1.8 and the widening waits. Post `PR-GATE W1.7 DONE`.
6. W1.9 — ONLY after `PR-GATE M4 DONE` AND the Owner's `PR-OWNER W1.9 GO` line (D7). The empty-patch arm: a STATUS_UPDATE that sent Amazon nothing records NOT_SENT (or SKIPPED) with a reason, never SUCCESS; the status writer stops keying the green line on dryRun alone. Until the GO line: leave it, and write the M4 count beside the item.
7. REHEARSAL, no channel. On the LOCAL Docker DB (name it), a disposable DRAFT row under the XAVIA family with a fake externalListingId: enqueue → dispatch → result for (a) a transport failure (stubbed fetch throws) → row UNKNOWN with holdUntil set and errorCode named; (b) an eBay "item cannot be accessed" body → UNKNOWN/REFUSED, not success; (c) an Amazon unpublish → refused, deleteListingsItem never called; (d) cancel within the window → CANCELLED and nothing dispatched. Read every row back after commit; assert the outbound stub's call count is 0 for every arm. Clean up by value.
8. Post `PR-GATE PR.2 AT-WAVE-4` with the gates on one clean run (api tsc, api vitest from apps/api with the host printed, check-push-lock, check-cron-clustered, check-event-contract), the rehearsal table, and every ASSUMED/QUESTION/REQUEST line.

DONE = items 1–8 measured green and posted. STOP and ask the Owner only for: a channel write of any kind, a prod write, a change to the liveness predicate (that is D6/Wave 4), a change to what the worker does on a non-lifecycle syncType, a conflict with a live claim.
```

### PR.3 — five-level narrowing and the offer services

```
You are PR.3 (five-level narrowing · the removal and offer services) on the Presence programme.

READ FIRST, in this order: (1) docs/pr-prompts-2026-09-13.md — §A, §B (D5, D6, D8, D12, D24), §C, §D; (2) docs/2026-09-13-presence-proposal.md — "Before anything else" items 4 and 6; "The sequence" → Wave 1 (you are W1-NARROW; step 4 and step 6's offer half); "The spine" → PRECONDITION P8 and section 6 "Enumerate, never widen"; the specifications "The Amazon adapter for Presence — corrected" (the inverted gate, the three resurrection paths, the four resolvers — read them now, you build them in Wave 4), "Presence on eBay — the channel adapter, corrected" (the label-guard backfill; Guard 2 is only correct AFTER the target rows are deleted), "Fulfilment lanes, corrected" (amazon-mapper defaults a null fulfilment to FBA on a live outbound path; the two isFbaCoordinate predicates); (3) docs/audits/2026-09-13-presence/audit-index.md → "The question you asked" (per-market removal wipes every account and alias; no way to end ONE listing of a multi-listing coordinate), "Channel by channel" ('Remove from Amazon IT' deletes the local row first; row-level removal bypasses the FBA warning), "What a takedown touches" (removal commits the local delete before the SP-API call; a deliberately removed Amazon listing comes back in the flat file); (4) the ledger from both ends. Memory traps: reference_prisma_not_excludes_null, reference_null_matches_every_lte, reference_and_guard_does_not_exclude_null, reference_prisma_upsert_on_conflict, reference_check_what_else_read_from_what_you_narrowed, reference_write_predicate_must_match_its_readers, reference_contract_field_varies_by_market, reference_idempotency_guard_matched_the_quote, reference_clean_up_by_value_not_by_row, reference_workbook_escaped_header_collision (compare record counts).
RULES: §A verbatim. Claim your §D paths first. Prod writes: none. Channel writes: none. The flat-file EDITORS are untouchable; the two removal SERVICES are yours by D5(a). Nothing committed.

YOU OWN: the §D row for PR.3. flat-file.service.ts is PR.4's — its narrowing hunk (the row loader around "3198-3204" in the proposal's numbering; cite content) is a `REQUEST TO: PR.4` with your helper's call.

YOUR ITEMS, in this order.
1. NOW — the ONE narrowing helper: apps/api/src/lib/listing-coordinate.ts exporting a five-level coordinate type {productId, channel, marketplace, channelConnectionId, aliasKey} and `whereCoordinate(c)` producing the Prisma `where` used by BOTH the pre-read and the write; a missing level THROWS a named error (never widens to a three-level match); a documented rule for NULL levels (both ChannelListing unique indexes are NULLS NOT DISTINCT because every row today carries a NULL alias — Prisma `null` matches NULL only when written explicitly; `undefined` drops the clause and matches everything). Tests: every missing-level arm; the emitted where names all five; the NULL arm matches NULL rows and not non-NULL ones (run against the LOCAL DB with two seeded DRAFT rows under XAVIA, cleaned by value). Post `PR-GATE W1-COORD DONE — <path>`.
2. WAIT for `PR-GATE M3 DONE` (PR.5's NULL distribution) and read it: if channelConnectionId or aliasKey are NULL on the rows these sites touch, narrowing without the NULL rule is a silent no-op — write the measured shapes into the helper's tests. Also wait for `PR-GATE W1.1 DONE` and `W1.2 DONE` (the adapters must refuse before the removal services feed them) and `PR-GATE W0-SEC DONE`.
3. W1.4 — at EVERY site, the pre-read AND the write through the helper in the SAME edit (narrowing the delete alone is strictly worse than today): ebay-flat-file-delete.service.ts (the loader and the deleteMany — today `(productId, channel, marketplace)`, destroying every account and alias in the market); amazon-flat-file-remove.service.ts (loop + deleteMany + the parentId child fan-out — the fan-out is NAMED in the result, never silent); amazon-market-offer.service.ts loadRow (a three-level findFirst today — the reference path withdraw-offer is modelled on); sync-control.routes.ts's ZERO_PIN/FOLLOW loader; marketplaces.routes.ts at BOTH sites that hardcode aliasKey:''; listings/recovery.service.ts (both loaders — recovery nulls the siblings' ASINs today); ebay-label-guard.service.ts (the six-hourly identity backfill must not re-attach an ItemID to a deliberately ended coordinate on the wrong account — narrow it AND skip rows whose presenceIntent is terminal once the column exists; until then skip listingStatus ENDED with endedAt-style evidence, and say what you could not test); listing-reconciliation.service.ts confirmReconRow (aliasKey '' hardcoded, listingStatus 'ACTIVE' unconditional — it must not flip an ENDED coordinate back to ACTIVE; consult PR.1's recreate guard when it lands, refuse the flip until then). One test per site: the narrowed call touches ONE row when two rows share (productId, channel, marketplace) — seed both on the LOCAL DB under XAVIA, read back, count. Cite content, not line numbers. Post `PR-GATE W1.4 DONE` with the site table (file · before-shape · after-shape · test).
4. W1.6-OFFER (amazon-market-offer.service.ts). NO lifecycle DB write on a dryRun result: closeMarketOffers persists offerClosedAt/By/Reason ONLY on a real acknowledgement (patchPurchasableOffer returns {success:true, dryRun:true} under the default gated mode — today that renders WITHDRAWN over a live selling offer). closeMarketOffers also writes offerActive:false so the next flat-file submit does not clear skip_offer and re-open the buy box (PR.4 owns the skip_offer pairing — agree the field semantics in the ledger, one sentence). Reopen keeps forcing followMasterQuantity and its immediate QUANTITY_UPDATE behind the EU shared-quantity guard. FBA fail-closed stands (D8): unify the two isFbaCoordinate predicates into ONE exported function — the push path's is the dangerous one. Tests: dry-run → no DB write; real ack → both columns; FBA → refused on both paths through the one predicate. Post `PR-GATE W1.6-OFFER DONE`.
5. D24 — amazon-mapper.service.ts reads `product.fulfillmentChannel || 'FBA'` into a live Amazon variation payload while bulk-edit lets that cell clear to null: an unset fulfilment must NOT assert FBA — refuse the payload with a named reason (or omit the attribute — read what the SP-API schema requires and say which). Test both arms. The master cell relabel is PR.7's (request it: "Fulfilment (product default)").
6. REHEARSAL, no channel: on the LOCAL DB, two DRAFT XAVIA rows on the same (product, AMAZON, IT) with different aliasKeys and fake external ids; the narrowed remove takes ONE and leaves the sibling (read back); show what the old three-level shape would have taken (count only — do not run it). Clean up by value.
7. Post `PR-GATE PR.3 AT-WAVE-4` with gates on one clean run (api tsc, api vitest from apps/api with the host printed, check-route-prisma-ratchet attributed), the site table, the rehearsal, and every ASSUMED/QUESTION/REQUEST line.

DONE = items 1–7 measured green. STOP and ask the Owner only for: any prod write, any channel write, widening any predicate (D6 is Wave 4), touching a flat-file EDITOR, a conflict with a live claim.
```

### PR.4 — the push lock, the Amazon gate, packages/shared, the Shopify preconditions

```
You are PR.4 (the push lock · the Amazon gate · packages/shared · the Shopify preconditions) on the Presence programme.

READ FIRST, in this order: (1) docs/pr-prompts-2026-09-13.md — §A, §B (D6, D8, D10, D13, D14), §C, §D; (2) docs/2026-09-13-presence-proposal.md — "Before anything else" items 5, 6, 8; "The sequence" → Wave 1 (you are W1-SHARED and W1-GATE: steps 5, 6, 8); "The spine" → PRECONDITIONS P5, P9 and section 5 "Intent is a LOCK, not a label" (the layer table); the specifications "Presence on Shopify — the adapter, corrected" (ALL of it: C1/SHOP-P6 is the one that sinks the slice; SHOP-P2; the two write surfaces; restore-offer 409s), "The Amazon adapter for Presence — corrected" (§2 "The gate is inverted for exactly the two verbs that matter", the three resurrection paths); (3) docs/audits/2026-09-13-presence/audit-index.md → "What a takedown touches" (a price push RE-CREATES a closed Amazon offer; eBay's end path leaves isPublished true; promotions written to ended listings), "Channel by channel" → SHOPIFY (every studio-created listing keeps DRAFT; ARCHIVED never used); (4) the ledger both ends. Memory traps: reference_a_banked_rule_can_go_false, reference_comment_asserts_property_code_lacks (closedMarketSet's comment claims callers it does not have), reference_docs_describe_deleted_code, reference_api_accepts_a_flag_it_ignores, reference_explicit_flag_beats_inference, reference_channellisting_mapped_fields_are_columns, reference_producer_and_consumer_land_together, reference_ag_module_silent_omission (a missing exports-map entry fails at RUNTIME, not typecheck).
RULES: §A verbatim. Claim your §D paths first. Prod writes: none. Channel writes: none. Nothing committed.

YOU OWN: the §D row for PR.4. outbound-sync.service.ts is PR.2's: you publish the helper, PR.2 wires it there; your gate script reads that file. products-catalog.routes.ts is PR.1/PR.2's: W1.8's repoints there are `REQUEST TO:` lines with the exact hunks.

YOUR ITEMS, in this order.
1. FIRST, today — `assertPushAllowed(listing)` in packages/shared (find the existing module pattern beside shopify-information.ts; one file, one export): the ONE predicate extracted from outbound-sync.service.ts's dispatch refusal (syncPaused + offerClosedAt), extended to presenceIntent ∈ {HELD, WITHDRAWN, ENDED, DISCONTINUED, RELEASED} read OPTIONALLY (typed `string | null | undefined` — the column lands in Wave 2; no schema dependency now). Returns a typed refusal {code, sentence} rather than throwing, so callers can record the code. Tests per arm. The exports-map entry AND `npm run build` in packages/shared — apps/api resolves dist, so a missing entry or a stale build "passes" tsc and fails at runtime (prove with a node probe that imports the built path). Post `PR-GATE W1-SHARED-STUB DONE — <import path>`.
2. W1.5-PUSH. pricing-outbound.service.ts selects offerClosedAt, syncPaused (and presenceIntent once it exists) — today it selects only {id, platformAttributes} on an unscoped findFirst, then patchListingPrice issues op:'replace' on /attributes/purchasable_offer, the very instance close removed with op:'delete', and stamps syncStatus:'IN_SYNC' — and calls assertPushAllowed BEFORE the client call; a refused push writes the refusal, never IN_SYNC. syncNativeShopifyOffer (offer-sync.service.ts gates on syncPaused/syncLocked/isPublished only) and previewContentSync/synchronizeContent (content-sync.service.ts) call it — the D13(c) guard half of SHOP-P6; the confirmActive prompt names the intent ("this listing was deliberately ended on <date> by <who>"), not "a live or archived product". closedMarketSet has ZERO callers while its comment claims "used by every Amazon write stack" — wire its callers on the Amazon write paths you own and list them. scripts/check-push-lock.mjs: derive every push path from SOURCE (functions that call the SP-API / eBay Trading / Shopify admin / Woo clients' write methods — print the derived list) and assert each calls assertPushAllowed; seed it red; PR.2's file is read, not edited. Post `PR-GATE W1.5-PUSH DONE`.
3. W1.6-GATE. deleteListingsItem reads `process.env.AMAZON_PUBLISH_MODE` directly while every other write calls getAmazonPublishMode() — with the master flag NEXUS_ENABLE_AMAZON_PUBLISH off and AMAZON_PUBLISH_MODE=live the permanent delete still goes out. Route it through getAmazonPublishMode(); test: master flag off + mode live → no request. In flat-file.service.ts declare the skip_offer pairing (offerActive:false ⇒ skip_offer injected) as the ONE sanctioned two-flag write, with a comment PR.1's gate reads; apply PR.3's narrowing helper to the row loader (their `REQUEST TO:` names the hunk). Post `PR-GATE W1.6-GATE DONE`.
4. W1.8. Move isLiveOnChannel UNCHANGED into packages/shared as `identityHeld` (listingRisk.vitest.test.ts must keep passing byte-identical — PR.7 repoints the studio import; you post the path) and add `sellingRisk` beside it (identityHeld AND intent ∉ terminal OR fact = SELLING; intent/fact optional until Wave 2). `REQUEST TO: PR.1` and `PR.2` the repoints of the hard-delete preflight and the cascade enqueue to sellingRisk — WITHOUT widening what they attempt (the current AND on listingStatus stays until D6/Wave 4; the request says so). Exports map + build again. Post `PR-GATE W1.8 DONE — <paths>`.
5. SHOPIFY PRECONDITIONS. SHOP-P2: content-sync.service.ts persists listingStatus (ACTIVE when Shopify says ACTIVE; INACTIVE for DRAFT or ARCHIVED — never the literal ARCHIVED) and, after `PR-GATE W2-SCHEMA-APPLIED`, the channelFact* columns it already computes — and channel-sheet-projection.ts stops stamping the literal 'ARCHIVED' onto listingStatus on the wire and carries it in channelFactDetail instead: BOTH halves in ONE write (drawer/types.ts reads it — tell PR.7 before saving). Assert the DB row, not the mapping object; the positive control is a fixture whose remote status you can flip on the LOCAL DB read model (no Shopify write). SHOP-P5's server half: on a SHOPIFY coordinate the offer-toggle's "Buyers stop seeing this offer" is false (offerActive is read by nothing there) — your services expose `offerActiveHonoured: false` for Shopify/eBay/Woo on the wire so PR.7's copy is derived, not authored. Post `PR-GATE SHOP-P DONE`.
6. Post `PR-GATE PR.4 AT-WAVE-4` with gates on one clean run (packages/shared build + tests, api tsc, api vitest from apps/api with the host printed, check-push-lock green with its derived path list), and every ASSUMED/QUESTION/REQUEST line.

DONE = items 1–6 measured green. STOP and ask the Owner only for: a prod write, a channel write, writing the intent INTO the Shopify sheet cells (that is D13(a), Wave 4), a change to the publish-gate flags, a conflict with a live claim.
```

### PR.5 — the measurements, then the schema

```
You are PR.5 (the seven measurements · the additive schema) on the Presence programme.

READ FIRST, in this order: (1) docs/pr-prompts-2026-09-13.md — §A, §B (D7, D8, D11, D14, the sequencing rule on migrations), §C, §D; (2) docs/2026-09-13-presence-proposal.md — "Before anything else" item 10 (M1–M7) and item 6; "The sequence" → Wave 0 (W0-MEAS) and Wave 2 (W2-SCHEMA); "The spine" → Schema (every ADDITIVE / NOT PROPOSED / NOT DECLARED line and SEQUENCING); the specifications' Schema blocks ("Presence on Shopify" → ListingIdentity.retiredSnapshot; "Stop-Sale" → what Wave 5 will add, so the model you ship does not block it); (3) the ledger both ends — hub #12 (migration staging), #10 (a sibling's migrate deploy DRAGS parked folders under prisma/migrations), R-LX-28 and R-LX-30 (lx4/lx5 shipped WITHOUT RLS on prod — do not repeat), the VT.F2 prod-DB test guard. Memory traps: reference_which_database_is_this_api_on, reference_vitest_from_repo_root_targets_prod_db, reference_migrate_deploy_drags_parked_migrations, reference_prisma_migration_p3009_blocks_deploys, reference_neon_migrations (strip -pooler), reference_pgbouncer_advisory_locks, reference_could_not_measure_vs_measured_empty, reference_a_fixture_pins_a_dimension, reference_financial_field_security_exists (RLS state).
RULES: §A verbatim. Wave 0 is READ-ONLY: no repo source file is edited; your scripts live in the scratchpad; results go under docs/audits/2026-09-13-presence/measurements/. Every number carries the HOST it was taken on and the time. The only prod write in the whole programme is your migration, on the Owner's own words in your terminal. Nothing committed.

YOU OWN: the §D row for PR.5.

YOUR ITEMS.

WAVE 0 — start now. One file per measurement, M<n>.md: the question, the exact SQL or script, the host (from the connection string, password stripped), the number, the positive control, the time, and one line "what this decides".
M1 WHICH DATABASE. `node scripts/_vtf-env-which-db.mjs` (host per CWD); the DB behind API :8091 via a request (GALE-JACKET Product.version through the API vs `SELECT version FROM "Product" WHERE id='cmokmy3a40078pm0p1fvnu523'` on each DB); whether RLS is on and what `current_setting('nexus.workspace_id', true)` returns on a bare connection to each. Post `PR-GATE M1 DONE — local=<host> prod=<host> api8091=<which>`.
M2 Offer coverage on Amazon coordinates on BOTH DBs: rows in "Offer" joined to AMAZON ChannelListings, per marketplace, per fulfillmentMethod — taken with the workspace set if RLS applies (a zero without it is vacuous); positive control: a table you know is non-empty under the same session. Decides whether offerScope is inert (D8). Post `PR-GATE M2 DONE — local=<n> prod=<n>`.
M3 NULL distribution: per channel, counts of ChannelListing rows with channelConnectionId NULL/non-NULL and aliasKey ''/NULL/other on both DBs; AND the multiplicity — (productId, channel, marketplace) groups with more than one row, with the distinct (channelConnectionId, aliasKey) shapes inside them. That multiplicity is the set of rows a three-level delete takes together. Post `PR-GATE M3 DONE` with the table.
M4 The empty-patch arm's blast radius: 30-day count of OutboundSyncQueue rows, targetChannel AMAZON, syncType STATUS_UPDATE, status SUCCESS, that carried no attributes to send — read outbound-sync.service.ts around the empty-patch return and the status writer to find the data signature (result payload / errorMessage / a marker); if the rows are not distinguishable in data, write "could not measure" and exactly what field would make it measurable. Both DBs. Post `PR-GATE M4 DONE`.
M5 The eBay account-level out-of-stock preference — only the Owner can read it: post `QUESTION FOR THE OWNER: M5 — Seller Hub → Account → Selling preferences → "Use the out-of-stock control" (on/off) for the connected account(s); the shipped 'deactivate — the safe hide (ItemID kept)' column has been ENDING listings if it is off`, and `PR-GATE M5 PENDING-OWNER`. When the Owner answers in your terminal, write the answer verbatim with the time and post `PR-GATE M5 DONE`.
M6 Whether any Etsy listing_id is persisted anywhere: ChannelListing rows with channel ETSY and externalListingId; any Product column or platformAttributes key that looks like an Etsy id (search the schema first, then the data); both DBs. Decides whether the hard-delete Etsy trap is armed today or armed by our own ingest. Post `PR-GATE M6 DONE`.
M7 `_versionHistory` present in ChannelListing.platformAttributes: count of rows carrying the key, both DBs (0 of 977 on 09-01 on one DB — say which). Post `PR-GATE M7 DONE`.
M8 (added, cheap, needed by D14's rider): the running values of NEXUS_RBAC_MODE, NEXUS_ENABLE_AMAZON_PUBLISH, AMAZON_PUBLISH_MODE and the Shopify publish-mode flag on production (Railway variables, READ only) and on the local API (.env). Post `PR-GATE M8 DONE`.
Also: re-verify that migration `20260911a_vp2_variation_excluded` does NOT exist under prisma/migrations and record the folders that do — variationExcluded stays raw-SQL and is NOT declared (spine: NOT DECLARED).

WAVE 2 — the schema. After W1.1–W1.6 are DONE in the ledger and M1 is posted.
1. STAGE under packages/database/prisma/migrations-pending/<timestamp>_pr_presence/ (a `.sql` and a `.rollback.sql`, like the pes5-ii precedent; README updated): ChannelListing.presenceIntent String?, presenceIntentAt DateTime?, presenceIntentBy String?, presenceIntentReason String?, presenceEffectiveFrom DateTime?, presenceUntil DateTime?, channelFact String?, channelFactAt DateTime?, channelFactVia String?, channelFactDetail Json?, endedAt DateTime?, endedBy String?, endedReason String?, saleStopId String?; the ListingIdentity model exactly as the spine lists it (workspaceId with the dbgenerated NULLIF(current_setting('nexus.workspace_id', true), '') default that 412 of 432 models carry; productId a plain String with NO Prisma relation; sku, channel, marketplace, channelConnectionId String?, aliasKey String @default(""), externalListingId, externalParentId, fulfillmentMethod String?, offerSku String?, firstSeenAt, lastSeenAt, retiredAt DateTime?, retiredReason, retiredBy, doNotRecreate Boolean @default(false), lastSnapshotId String?, retiredSnapshot Json?, supersededByExternalListingId String?; @@unique([workspaceId, channel, marketplace, channelConnectionId, aliasKey, externalListingId]); indexes for the read paths PR.1 names in docs/pr-presence-wire.md); the runtime role's GRANTs and the `nexus_workspace_isolation` RLS policy for ListingIdentity in the SAME migration (R-LX-28: a grant-less table passes prisma validate, generate, tsc and every mocked test and answers 42501 in the running app). Every column nullable; NO backfill of any kind (the eBay end path already wrote {ENDED, offerActive:false, externalListingId:null} and a naive derivation reads that as never published — the reads derive, the schema does not). NOT proposed: a presence event table, an Etsy table, nullable ChannelListingSnapshot.channelListingId, OutboundSyncQueue FK changes. Post `PR-GATE W2-SCHEMA-STAGED DONE — <sql path>` and `QUESTION FOR THE OWNER: apply the presence migration to Neon prod? (additive, nullable, no backfill; SQL at <path>)`.
2. APPLY LOCAL first (Docker; name it), verify: every column present with the right type and nullability, the policy present (`SELECT polname FROM pg_policy WHERE polrelid='"ListingIdentity"'::regclass`), the grants (`node scripts/check-table-grants.mjs` — LOCAL only by design), the unique index. Then PROD only after the Owner's words in your terminal, quoted verbatim into the ledger as `PR-OWNER MIGRATION APPLY — "<words>" — <time>`: strip `-pooler` from the URL, take the advisory-lock precaution, apply, verify the same four things on prod. Only THEN move the folder into prisma/migrations, declare the columns and the model in schema.prisma, `prisma generate`, api tsc. Post `PR-GATE W2-SCHEMA-APPLIED DONE — local=<host> prod=<host> — <columns n> <policy yes/no> <grants yes/no>`.
3. If the Owner does not answer, everything else in the programme proceeds on the LOCAL apply and PR.F records prod as NOT APPLIED — say so in the ledger rather than waiting silently.
4. Post `PR-GATE PR.5 AT-WAVE-4` with the measurement table (M1–M8 with hosts), the migration record, and every ASSUMED/QUESTION line. Stay reachable for Wave 5's schema (SaleStop, the ETSY enum) — not now.

DONE = M1–M8 posted with hosts and positive controls; the migration applied and verified on both DBs (or prod recorded as not applied on the Owner's silence); schema declared; api tsc 0. STOP and ask the Owner only for: the prod apply (always), any non-additive change, any backfill, any data change.
```

### PR.6 — the design system

```
You are PR.6 (the design system — the type contract, the menu, the renderers, accessibility) on the Presence programme.

READ FIRST, in this order: (1) docs/pr-prompts-2026-09-13.md — §A, §B (D1, D21, D23 rider), §C, §D; (2) docs/2026-09-13-presence-proposal.md — "What is actually true right now" → ground:measure (MenuItemDef's measured shape; the swatch gate crash; the contrast numbers; `.nds-cell-is-waiting`/`-unknown` byte-identical; `--nds-success` the worst of the family; provenance marks aria-hidden by a documented but unsupported reason; DS-GAPS half stale); "The sequence" → Wave 2 (DS CONTRACT, DS MENU, DS RENDERERS, DS A11Y) and Parallel lane P (P-A11Y); the specifications "The Consequence Contract — a destructive-action pattern derived from typed reach and reversibility (corrected)" (ALL: `frictionFor`, `validateAction`, the tone selector that matches nothing, the misfiled component, the change that would have deleted the family verbs), "Six accessibility defects, fixed in the design system at the AAA bar — corrected" (ALL: A11Y-1…6 with the corrections — do NOT drop `disabled` from Menu, do NOT sort danger last, AG's icon is Element|string, success-text derived against the declared grounds), "Say it plain" → the DS pieces (PresenceMark on Pill/Tag, AsOf), and the "Design-system gaps this needs" block of every other spec (Disclosure tone; SummaryTable per-row tone — refused, ride a Pill); (3) docs/audits/2026-09-13-presence/audit-index.md → "The experience" (every a11y and design-inconsistency row); (4) `.claude/DS-GAPS.md` (append-only; its Menu entry is half stale), docs/2026-09-01-product-edit-studio-layout.md §2.9; (5) the ledger both ends — rulings #110 (four surfaces render one declaration identically), #133/#231 (an instruction may never live only on a disabled control), CT.1 (the control tier), CH.1's LANDED lines, MX.G's claimed DS paths (may be live). Memory traps: reference_token_scale_defined_twice (radius restated in css-vars.ts), reference_alias_freezes_on_a_descendant, reference_ds_option_list_two_copies, reference_ds_toast_two_providers, reference_ag_theming_api_accepts_var_calc, reference_clip_rect_is_the_padding_box, reference_tag_identity_is_glyph_not_colour, feedback_shared_components_no_copy_props, feedback_check_ds_before_handrolling, the 09-05 archive's CSS/contrast/guard lines.
RULES: §A verbatim. Claim your §D paths first — check MX.G's claims on grid/editors and grid/renderers before touching either directory. Every ratio is COMPUTED against the declared grounds (grid cells in grid/theme/grid.css, the panel, the primary-soft [data-selected] ground), light AND dark, with a script whose output you paste — never against white. Nothing committed.

YOU OWN: the §D row for PR.6, including ALL `.d.ts` regeneration (51–52 of 243 are stale now; name the set before writing; check each source's mtime for an in-flight edit and skip a file someone saved in the last two minutes, re-run later).

YOUR ITEMS, in this order — no data dependency; start now.
1. TYPES FIRST (three lanes wait on this). In design-system/grid/actions/registry.ts: `reach: 'local' | 'local-destructive' | 'channel'` on GridAction (gates availability and grouping before any preflight); on ActionImpact: `reversal: { verb: string; fidelity: 'exact' | 'lossy' | 'none' }`, `subject`, `acknowledge`, `asOf`, `refusal`, `handoffs`, optional read-only `review`; `findings[].severity` gains 'unknown' plus a `blocking` flag and an `asOf`; a sibling `validateAction(action)` (validateImpact receives only the impact and structurally cannot see an action-level field) refusing a reach:'channel' verb whose preflight returns no reversal; a pure `frictionFor(impact)` deriving the minimum friction rung, with validateImpact refusing anything SOFTER (a lane may escalate, never soften) — and the one-line reorder in runAction the surface spec names, so a lane's honest refusal (level:'none' + unavailable) reaches the operator instead of the DS diagnostic string. MenuItemDef gains `tone?: Tone` (from primitives/tone.ts) and a group/separator affordance (declaration order is pinned by registry.vitest.test.ts — do not reorder). NEW design-system/grid/renderers/presence.ts as a pure sibling of readiness.ts: the intent table (NONE|DRAFT|LIVE|HELD|WITHDRAWN|ENDED|DISCONTINUED|RELEASED), the fact table (UNKNOWN|SELLING|NOT_SELLING|SUPPRESSED|ABSENT|REFUSED), the verdict table (agrees|pending|diverged|unknown|unreachable), each → {label, tone, sentence}; NO converter to either readiness vocabulary (ruling #3's local-mapping ban). Label this honestly in the ledger: the audit REFUTED "ActionImpact cannot express reversibility" as a defect — this is a contract enhancement whose case is that a guard can then enforce what is prose today. Regenerate the DECLARED .d.ts set (name it: registry, NexusGrid, DataGrid, Modal, Tabs, Drawer, Listbox, Menu, and every file you touch). Post `PR-GATE W2-DS-TYPES DONE — <export names, paths>`.
2. MENU (D1, D21). One danger rule with the CORRECT selector — `.nds-menu button[data-tone="danger"], .nds-menu a[role="menuitem"][data-tone="danger"]` (`.nds-menu-item[data-tone]` matches nothing: Menu emits only .nds-menu-item-text/-name/-desc); text in `--nds-danger-text` (compute it on the panel and on the [data-selected] primary-soft ground). Both adapters map GridAction.danger → tone (measured 0 occurrences of `danger` in menuAdapters.tsx against a control of 5 for `label`); AG's contextMenu `icon` takes Element|string — an SVG string or CSS glyph there, the React node in the DS adapter. A disabled verb's reason lands in `description` (keyboard-reachable; Menu's navigation skips disabled items) AND stays in `title`; `description != null` is the held discriminator. Do NOT drop the `disabled` attribute before every `:disabled` rule has an `[aria-disabled="true"]` twin (four in components.css, two in patterns.css) — A11Y-2 lands FIRST. No reordering of danger items (ruling #110). SheetToolbar's force-disable of every overflow item during a pending write is PR.8's file — send them the `description` sentence to use. Append the correction to `.claude/DS-GAPS.md` (its Menu entry recites six fields; `title`/`description` have since landed) — append, never edit.
3. RENDERERS. PresenceMark built on Pill and Tag — NOT Badge (the SP/SB/SD advertising mark, no tone) and NOT SourceIndicator (its kind union is the cell-value provenance axis); an AsOf component rendering "not checked" / "never" rather than an em dash; ActionConfirm moved to components/ (two studio surfaces already call useActionConfirm with hand-built literals; replace its four uses of the grid-only .nds-cell-strong); Disclosure gains `tone?: Tone` + `.nds-disclosure.warning/.danger` (open already works — DisclosureProps spreads onto <details>); catalog entries and tests for each; factory mirrors.
4. A11Y (P-A11Y — preconditions for any confirmation dialog). A11Y-1: `.nds-btn:focus-visible` paints nothing on .primary and .danger (the later .danger rule wins border-color; the focus token is 12% alpha at 1.17:1) — a real outline, measured ≥3:1 against both grounds. A11Y-2: the [aria-disabled] pairing. A11Y-3: ProvenanceMark `role="img"` + `aria-label` from the label MARKS already carries for all eight members (the aria-hidden reason in the file — "100 tab stops per row" — does not hold: aria-hidden suppresses announcement, it creates no tab stop); `.nds-cell-prov-refused` → `--nds-warning-text`; `--nds-prov-formula-fg` raised (spec numbers). A11Y-4: `.nds-grid-confirm-error` / `.nds-cell-stock-out` → `--nds-danger-text`, `.nds-grid-confirm-warn` → `--nds-warning-text`; raise `--nds-danger-text` to #8b2a25 light / #f2aea7 dark (monotonic on every declared ground — verify); `--nds-success-text`: raise it to clear 7:1 on the four declared grounds or write the exception INTO the token file with its reason (D21) — `.nds-cell-stock-ok` at 3.30:1 is the worst member; add `.nds-inline-error` and send PR.7/PR.8 the seven message sites to repoint (they own those files). A11Y-5: `CellSaveMark({state})` beside CellSaveReason — glyph plus ring for saving/waiting/unknown (waiting and unknown have byte-identical backgrounds today; a ring on waiting must keep unknown distinguishable), glyph in `--nds-text`, role="img"; the mount points in the sheets are PR.7's — request. A11Y-6: the tone-danger menu text (item 2). Every ratio to two decimals, both themes, in the ledger.
5. GATES. NEW scripts/check-focus-visible.mjs and scripts/check-token-role.mjs (a fill token used as text is the defect class), each with a positive control in the same run and each SEEDED RED before the fix. Widen check-silent-disabled, re-measure the baseline, THEN tighten — two writes, never one (a single write makes the count uninterpretable). check-ds-dts-fresh --check green after your regeneration; check-ds-fork-drift green (mirrors); the swatch gate green once PR.1 posts W0-GATES (until then, its RED convicts nothing — say so); check-dark-alias-scope, check-dark-pin-parity, check-token-resolution, check-css-parse, the hex/radius/shadow ratchets green.
6. Post `PR-GATE W2-DS DONE`, `PR-GATE P-A11Y DONE`, then `PR-GATE PR.6 AT-WAVE-4` with web tsc 0, DS vitest green, every gate above on one clean run, the contrast table, and every ASSUMED/QUESTION/REQUEST line.

DONE = items 1–6 measured green. STOP and ask the Owner only for: a token change outside the ones named (a new token needs the nod), a Menu behaviour change beyond D1/D21, a ScopeBar change (forbidden — nine consumers), a conflict with a live claim.
```

### PR.7 — the studio: copy, wire, vocabulary

```
You are PR.7 (the studio — honest copy · the presence wire · the vocabulary) on the Presence programme.

READ FIRST, in this order: (1) docs/pr-prompts-2026-09-13.md — §A, §B (D2, D3, D11, D20, D22, D24 rider), §C, §D; (2) docs/2026-09-13-presence-proposal.md — "What is actually true right now" → ground:measure (check-wire-null-defaults is the brief: 28 offenders, all in _studio/, "a plausible default looks like data"; the dead studio CSS; the skeleton arithmetic) and ground:tests (the 6 failures: layers.vitest.test.ts:55 is LIVE — channelSnapshot unmapped; masterWrite's 5 are STALE; the bare-return test; scopes.vitest pins Woo live); "The sequence" → Wave 0 (W0-COPY), Wave 2 (STUDIO WIRE), Wave 3 (W3-VOCAB — producer and consumer in ONE write); the specifications "Say what it does — the honest-copy pass, corrected" (ALL — the corrected strings verbatim; three of the draft's replacements were new lies; two groups struck; the load-bearing `level:'none'` change), "Say it plain — one presence vocabulary, two axes, one as-of" (ALL — the meta module, the scoped parity gate, the marks NOT on the alias band, the six-valued fact axis, the renders for revoked/expired/failed/suspended/binned), "Presence on Shopify" → SHOP-P5, "Presence on screen — corrected" (the two different BulkActionBars; the typed-DELETE gate is case-insensitive; the ratchet fix does NOT clear the ratchet); (3) docs/audits/2026-09-13-presence/audit-index.md → "What a takedown touches" (the "Live" verdict ignores listingStatus; "Not listed" is an affirmative claim; no as-of; a revoked account makes the channel vanish; expired grant reads healthy; a binned product opens as Active; marketplacesFailed thrown away), "The experience" (three definitions of Live; four names for one verb; the raw status string; money rendered four ways; window.confirm as the leave guard), "The question you asked" (the offer-toggle promise; "Excluded" while on sale; delete-child copy); (4) docs/2026-09-01-product-edit-studio-layout.md §1b, §2.10, §5; docs/2026-09-03-cell-editing-contract.md; (5) the ledger both ends (#742 the band-width failure; the 2026-09-05 AliasBandCell decision; #133/#231; CH.1; VT/LX/MX claimed paths in _studio — a live claim is a request). Memory traps: reference_wire_parse_boundary_rules, reference_contract_emits_same_write_cell_twice, reference_a_list_of_members_is_a_set_claim (RowReadinessState is restated in four places), reference_read_the_screen_not_the_diff, reference_stale_measurement_looks_like_a_missing_one, reference_strictmode_latching_cleanup_flag, reference_autosave_still_needs_a_nav_guard, reference_next_query_cursor (history.pushState({}, …) never window.history.state), reference_local_web_hydrates_only_on_localhost_origin, reference_products_next_lost_live_refresh.
RULES: §A verbatim. Claim your §D paths first. Local web :3000 may hit PROD — read the API origin before any interaction that writes; a cell blur is a write. XAVIA only. Copy verbatim from the specs. Nothing committed.

YOU OWN: the §D row for PR.7. PR.8 owns _studio/listings/**, SheetToolbar.tsx and the nav/tab-host mount hunks; PR.6 owns the DS. family-projection.service.ts's `excludedReason()` hunk is yours by announcement (VT.1 has closed; check for a live claim first).

YOUR ITEMS, in this order.

WAVE 0 — start now.
1. THE FIVE LYING SURFACES, with the "Say what it does" spec's corrected strings verbatim: (a) the /products hard-delete modal's three radio bodies in apps/web/src/app/products/_components/BulkActionBar.tsx — write them for the adapter PR.2 is shipping (an Amazon unpublish REFUSES; Shopify returns a visible failure; eBay unpublish REFUSES per D11), not for today's; the "Unpublish (recommended)" string is fixed now, the pre-selected default stays until PD-2 (D22); (b) readiness.ts's "Live · Already published on this channel" and its unlisted sibling — "Listed" and an honest absence; (c) ListingsPane.tsx's raw listingStatus; (d) AliasBandCell.tsx's `?? 'NOT LISTED'` default — two lines, because bandTitle's `status.toLowerCase()` throws on null; (e) family-projection's two divergent excluded-variant reasons through ONE exported `excludedReason()` called from BOTH sites. Every replacement string has a test that pins it.
2. LOAD-BEARING (the spec's one code change): set `level:'none'` wherever `unavailable` is set in sheet/channel/channelActions.ts, so the honest refusal reaches the operator — today registry.ts + runAction.ts replace it with the DS's internal "preflight failed but still asked for a confirmation" (PR.6 lands the runAction reorder; yours is the caller side). SHOP-P5: on a SHOPIFY scope the offer-toggle verb is replaced by hold/resume on syncPaused, or states its true local-only consequence — derived from PR.4's `offerActiveHonoured` wire flag once it lands; until then, the honest local sentence. A test pins the sentence per channel.
3. BULKACTIONBAR (D19): never arms while preflightError is set (a 500 on the preflight must block the permanent delete, not render an amber note); confirmPhrase comparison exact and case-sensitive; the phrase is the SKU / external id per registry.ts's own rule ("A SKU, never 'DELETE'"), and it is NOT pre-shown in the placeholder. Tests for all three.
4. STALE AND LIVE TESTS (D20): masterWrite.vitest.test.ts (5 failures asserting the deleted two-route write; the replacement contract is already tested in sheet/content-write.vitest.test.ts) → rewrite to the current single-route contract, never weaken; live-channel-scope.vitest.test.ts's bare `if (CHANNEL !== 'AMAZON') return` → `ctx.skip()` with the reason; scopes.vitest.test.ts pins WooCommerce as live in three assertions (fixture from 2026-09-01, predating the 2026-09-12 ruling) → `QUESTION FOR THE OWNER` with both readings, do not change it alone. Run the studio suite hermetic (NEXT_PUBLIC_API_URL=http://127.0.0.1:1 PES3_API=http://127.0.0.1:1) and paste the counts before and after.
5. Post `PR-GATE W0-COPY DONE` with the string table (surface · before · after · test).

WAVE 2 — the wire. After `PR-GATE W2-DS-TYPES DONE` and `PR-GATE W2-READ-SHAPE DONE`.
6. Map ValueSource 'channelSnapshot' in drawer/types.ts's LAYER_OF/toLayer (attribute-resolver.ts defines it as "a following legacy listing value; drift, never an operator pin"; the drawer renders it "Unrecognised" today; layers.vitest.test.ts:55 is RED) with a layer that reads as "what the channel holds now", never folded into master; correct the stale eight-member comment.
7. channel-ops/ReadinessPanel.tsx off components/DataGrid onto the grid/datagrid shim (grid-kit ratchet 34 → 33; the remaining red is another lane's — attribute it by dirty∧listed in the ledger, do not absorb it).
8. THE PRESENCE WIRE under _studio/presence/: loaders for GET /studio/presence, /presence/one, /presence/history, /presence/amazon-posture and the verify POST (shapes from docs/pr-presence-wire.md); types IMPORT the unions from PR.6's renderers/presence.ts — no restating; offerClosedAt/By/Reason, syncPaused, Product.deletedAt, Marketplace participation, connection-health tri-state and the sync-queue `sources` block onto the studio contracts (contracts.tsx, sheet/channel/types.ts, drawer/types.ts) with the mirror parity test the LX design demanded; a FAILED connections read renders as "could not load" never "sells nowhere" (marketplacesFailed is threaded through three files and thrown away — consume it); a revoked or expired grant renders the channel with its state, never vanishes; a binned product opens as binned. identityHeld / sellingRisk imported from PR.4's packages/shared path (listingRisk.ts becomes a re-export; its test keeps passing). Then check-wire-null-defaults: every one of the 28 offenders in _studio/ replaced by an honest absence (`?? 'NOT LISTED'`, `|| 'Not published'` — note the `||` also swallows an empty-string id, ComparePane's invented "No listing on this coordinate.", ChannelProjection's invented alias sentence) → ≤ 17 with the gate's exit code pasted. Post `PR-GATE W2-WIRE DONE`.

WAVE 3 — the vocabulary, landing WITH the wire fields. After all Wave 2 gates and `PR-GATE W3-API DONE` (the verbs' refusal copy comes from the server).
9. `_studio/presence/meta.ts` — the ONE pure meta module (intent / fact / verdict → label, tone, sentence; the roll-up as a distribution "Amazon: 3 selling, 1 ended, 1 not checked" whose confidence is the weakest member's and as-of the oldest's — never "mostly live"), consumed by the toolbar descriptor, the drawer's ListingsPane (gains the two marks + AsOf; stays singular and read-only), the footer and every confirmation — PR.8's surface imports it. 'Live' → 'Listed' across _studio/** and the listing/lifecycle key prefixes in en.json; delete the three Italian keys the "Say it plain" spec names (visible English fallback — D22); NEW scripts/check-presence-vocabulary.mjs: scoped by key prefix and `_studio/**`, ratcheted not zeroed, EXCLUDING the flat-file enum where 'deactivate' is a data value an operator types — seeded red, then green, exit code by redirect. Marks in the descriptor and the drawer, NOT on the alias band (AliasBandCell.tsx records the 2026-09-05 decision; bandWidth's derivation takes the variant row, so a third pill compresses the alias label — the #742 failure). The refusal sentence on every held verb comes from the server's `refusal`; the reversal sentence from `reversal`; the target restated from the row. Scheduling dates render with "a note, not an alarm" (D15). The master Fulfilment cell relabelled "Fulfilment (product default)" (D24 — PR.3's request). Post `PR-GATE W3-VOCAB DONE`.
10. Screens: at 1440×900, signed in, both themes, on GALE-JACKET master, Amazon·IT, eBay·IT and Shopify: the toolbar descriptor, the drawer ListingsPane, the hard-delete modal on /products (do NOT confirm it), the channel offer verb's held state — zero console errors; screenshots into docs/audits/2026-09-13-presence/w3/copy/. Browser gates per §A.7.
11. Post `PR-GATE PR.7 AT-WAVE-4` with web tsc 0, the studio suite hermetic (expected: the 6 baseline failures gone; state the count), check-wire-null-defaults, check-presence-vocabulary, check-i18n-catalog, check-link-targets, check-silent-disabled, check-button-vocabulary, check-help-cursor on one clean run, and every ASSUMED/QUESTION/REQUEST line.

DONE = items 1–11 measured green. STOP and ask the Owner only for: any write outside XAVIA, changing the hard-delete default radio (D22 ties it to PD-2), the WooCommerce test assertions, a nav/tab change (PR.8's), a conflict with a live claim.
```

### PR.8 — the Listings surface and the sheet toolbar

```
You are PR.8 (the Listings surface · the sheet toolbar's honest subset) on the Presence programme.

READ FIRST, in this order: (1) docs/pr-prompts-2026-09-13.md — §A, §B (D1, D2, D3, D19, D23), §C, §D; (2) docs/2026-09-13-presence-proposal.md — "The spine" → sections 1 (Where it lives), 2, 6 (the selection lives on the presence grid, not the sheet), 7; "The sequence" → Wave 3 (W3-SURFACE) and Parallel lane P (P-TOOLBAR — the honest subset ONLY: units, typed status slot, portalled views prompt, padding specificity; the ladder, the Transfer merge and the gutter are HELD by D23); the specifications "Presence — a listing surface, a pinned coordinate, and a second state axis (corrected)" (ALL — the D9-safe fallback that would mount a coordinate-keyed panel on a coordinate-less surface is the trap), "Presence on screen — corrected" (ALL — where the verbs live; SheetToolbar is a lane file; two different BulkActionBars), "One bar, six zones, one collapse ladder — the sheet toolbar redesign (corrected)" (read all; build only D23's subset), "The Consequence Contract" (what the confirmation renders from); (3) docs/audits/2026-09-13-presence/audit-index.md → "The design critic" (all eight), "The experience" (three chips print N columns for three quantities; `trailing` carries a control; the save-view Input in the no-wrap bar; the padding pair; channel scope counts a selection it gives no verbs to), "The methodology critic" (no viewport below 1440; useContainerTier has zero consumers — leave it, D23 holds the ladder); (4) docs/2026-09-11-variants-page-spec.md §2 (the band budget: 49 · 40 · 40 · 40 · 30 · 28+1 · 36×n · 36; identity 380; controls 28) and its canvas — the Variants page is the frame precedent for a second grid tab; docs/2026-09-13-matrix-page-design.md §3 (the Matrix tab is the latest new-tab precedent — mount the same way); docs/2026-09-01-product-edit-studio-layout.md §1b, §2.10, §5; docs/2026-09-03-cell-editing-contract.md; (5) the ledger both ends — CH.1's two LANDED lines, #742, MX.P's claimed paths (StudioTabHost / navigation hunks may be live: request, never edit over). Memory traps: feedback_grid_chrome_lives_in_the_engine, feedback_grid_height_follows_rows_per_page, reference_gridsheet_measures_the_document, reference_ag_fill_handle_swallows_dblclick, reference_ag_popup_editor_owns_keys, reference_ag36_react_editor_onvaluechange, reference_ag_react_inline_options_rerun_column_model, reference_grid_column_state_frozen_at_mount, reference_ssrm_node_group_unset_at_render, reference_next_query_cursor, reference_scroll_probe_must_verify_it_moved, reference_hmr_remount_doubles_the_header_set, feedback_visibility_over_minimalism, feedback_no_dead_space_layouts, feedback_ship_live_not_dark.
RULES: §A verbatim. Claim your §D paths first. Build on GridSheet + NexusGrid + the DS (PR.6 adds any missing piece — request it; nothing page-local). XAVIA only; every verb you wire is local-reach and lands through PR.1's writers. Nothing committed.

YOU OWN: the §D row for PR.8.

YOUR ITEMS, in this order.

NOW — P-TOOLBAR, the honest subset (SheetToolbar.tsx; the DS half via PR.6).
1. UNITS: the three chips print "N columns" for three different quantities and none is the number the chip computed — a chip count CONTRACT where the producer declares the unit and the renderer never re-derives it. A test currently pins "3 columns": post `QUESTION FOR THE OWNER` with both readings BEFORE changing it (D23).
2. TYPED STATUS SLOT: `trailing` is documented "status only, never a control" and both callers put a modal-opening verb there (ChannelSheet passes SchemaStatus, which renders a Button; the same schema fact is a dead Pill on master) — type the slot so a control is a type error; the ChannelSheet hunk is PR.7's (`REQUEST TO: PR.7` with the exact change: the control moves into the one ⋯ or becomes a Pill).
3. PADDING SPECIFICITY: the audit's literals (`0 6px` / `14px 16px`) do not occur in any studio stylesheet — MEASURE the real competing pair with the computed style at 1440 before touching a rule; then one rule, one owner; also delete the dead `styles.tabStrip` / `styles.mergedRow` blocks in studio.module.css (0 references; positive control styles.shell 6) and fix the route skeleton to the LIVE frame's bands (loading.tsx + studio.module.css draw 129px; the live frame draws 89–90 — measure it on screen, then match).
4. PORTALLED VIEWS PROMPT: saving a view replaces the Views trigger with an Input and two buttons inside the no-wrap bar — move it into a DS popover/portal (PR.6 if a piece is missing); the DS already ships MenuProps.selectedId (no hand-rolled tick). The overflow's force-disable during a pending write gains PR.6's `description` sentence.
5. Post `PR-GATE P-TOOLBAR DONE` with before/after screen numbers (chip text, trailing contents, padding pair, prompt geometry) at 1440×900 both themes.

WAVE 3 — the Listings surface. After `PR-GATE W2-READ DONE`, `W2-DS DONE`, `W2-WIRE DONE` and `W3-API DONE`.
6. `tab=listings`, label "Listings" (D2): mounted as the FIRST item of each per-channel nav group above "Listing information" and one product-wide roll-up under THIS PRODUCT (ASSUMED position: after Matrix if it is mounted, else after Variants — post it). The nav/tab-host hunks follow the Matrix tab's mount pattern exactly; `navigationHref` tests extended, never weakened; URL contract (`tab`, `scope`, `market`, `account`, `chip`) unchanged. The legacy Listing Hub on the legacy route is NOT touched (D3).
7. The surface, `_studio/listings/**`, on GridSheet + NexusGrid with the studio's frame bands (band budget from the Variants spec §2; measure, then match): rows are FIVE-LEVEL coordinates (channel · market · account · alias, with the external id and the seller SKU in the identity band); columns: Intent (PresenceMark) · Fact + AsOf · Verdict · Gate mode · Identity · Last change · ⋯ verbs. On a channel scope: that channel's coordinates for the current market with a chip per market; on master: the roll-up — a DISTRIBUTION per channel, confidence = weakest member, as-of = oldest, never "mostly live" — from PR.7's meta module. When there is NO coordinate (a channel with no listing, a market the channel does not serve, a revoked grant): a DS EmptyState NAMING the missing dimension and the way to create one — never a coordinate-less grid (the ambient-targeting hazard the design exists to kill). A one-line note at the top says what `verify` does and that "not checked · never" is the honest default (D20's cost, said out loud).
8. VERBS — local-reach only, through the DS action registry with `reach` and `reversal` typed (PR.6) and PR.1's writers: hold / resume; exclude child / include child; discontinue / undiscontinue (with the reason field and the effectiveFrom/until dates rendered as "a note, not an alarm"); verify (a channel READ, rate-capped by the server — render the cap sentence); alias archive / restore (held with the PES.5-ii sentence from the server). Every channel-reaching verb is ABSENT (not disabled) in Wave 3 — the sheet's channelActions preview-only refusal stays (D14). Each verb takes its target from the ROW, never the scope bar; the confirmation (ActionConfirm from components/, PR.6) restates channel · market · account · alias · external id, renders the reversal sentence from the type and the refusal from the server, and refuses when the row's version drifted (409 → repaint + refetch). A held verb shows its reason in `description` (keyboard-reachable). Selection: the DS BulkActionBar (the grid one, not the /products one) on THIS grid — in Wave 3 it shows the count and offers only the local verbs that apply to every selected row; plural intents are Wave 4.
9. Live pipe: the surface refetches on the studio's listing events (the sheet lost its live refresh — do not inherit that) and shows a staleness signal, never a silent stale grid.
10. GATES: rows for the listings surfaces in scripts/check-control-census.mjs (master roll-up · Amazon·IT · eBay·IT · the EmptyState · the confirm open) and the band budget in scripts/check-layout-v2.mjs — each assertion returns the measured value; check-editor-open (no editor opens on this grid; fill handle off); census / layout / editor-open / grid-chrome green on one clean run, announced per §A.7. 1440×900 both themes, signed in, zero console errors; screenshots into docs/audits/2026-09-13-presence/w3/listings/ with the numbers (band heights, row 36, controls 28, identity 380, copy verbatim from PR.7's meta).
11. Post `PR-GATE W3-SURFACE DONE`, then `PR-GATE PR.8 AT-WAVE-4` with web tsc 0, vitest for the surface (render via react-dom/server for the contract; node-only), the gates, the screen table, and every ASSUMED/QUESTION/REQUEST line.

DONE = items 1–11 measured green on live data (GALE-JACKET). STOP and ask the Owner only for: the "3 columns" test (D23), the roll-up's nav position if the Matrix tab is absent, any verb whose reach is not local, any write outside XAVIA, a conflict with a live claim.
```

### PR.F — the closing verification (Fable 5.1; launch only when all eight `PR-GATE PR.n AT-WAVE-4` lines are in the ledger)

```
You are PR.F (the closing verification) on the Presence programme. Eight lanes have posted AT-WAVE-4. Your job is to establish, with symmetric scepticism, whether Waves 0–3 and lane P are actually done to the AAA bar — and to leave the Owner one record that says exactly what is true, what is not, and what Wave 4 needs.

READ FIRST: (1) docs/pr-prompts-2026-09-13.md whole (§A–§E are the contract every lane was held to; §C's gate lines are your checklist); (2) docs/2026-09-13-presence-proposal.md — "Before anything else" (all ten), "The sequence" Waves 0–3 + lane P (every bullet is an acceptance item), "Deliberately not in this programme"; (3) docs/audits/2026-09-13-presence/audit-index.md and audit-full.md — every VERIFIED finding gets a disposition; (4) every PR.n ledger section (`/usr/bin/grep -n "^### PR\." docs/pes-claims.md`) and every `PR-GATE`, `ASSUMED:`, `QUESTION FOR THE OWNER:`, `REQUEST TO:` line; (5) docs/audits/2026-09-13-presence/baseline/** (PR.1's census — your BEFORE), measurements/** (PR.5), w3/** (PR.7/PR.8 screens). Memory traps that are the whole job: feedback_scepticism_must_be_symmetric, reference_claims_must_match_their_measurement, reference_could_not_measure_vs_measured_empty, reference_a_list_of_members_is_a_set_claim, reference_a_fixture_pins_a_dimension, reference_relayed_claim_converts_in_one_hop, reference_control_must_target_the_branch, reference_stale_measurement_looks_like_a_missing_one, reference_zero_change_round_trip_cannot_test_the_write, reference_read_before_the_write_arrived, reference_establish_attribution_before_reverting, reference_bare_reexport_broke_every_gate, reference_hmr_remount_doubles_the_header_set, feedback_read_the_screen_not_the_diff, feedback_restore_must_be_exercised.
RULES: §A verbatim. You may run everything; you edit only to fix a defect you found, each fix disclosed in the owning lane's section AND yours before saving. Prod writes: none. Channel writes: none. Nothing committed. Every reading names the host, the load, the time and the exit code by redirect.

PRODUCE, under docs/audits/2026-09-13-presence/final/, and summarise in your ledger section `### PR.F — <session> — closing verification — <date>`:
1. THE TREE. `uptime`; web tsc and api tsc (scoped, private build-info); packages/shared build + tests; the FULL pre-push gate list from .githooks/pre-push run bare, one clean run, serialised browser gates announced per §A.7 (nobody else saves during it — post the hold, wait for eight acknowledgements or the Owner's word); the studio suite hermetic (NEXT_PUBLIC_API_URL=http://127.0.0.1:1 PES3_API=http://127.0.0.1:1) and the whole apps/web suite; apps/api vitest FROM apps/api with the host printed. A table: instrument · baseline (PR.1's) · now · exit · attributed owner of any residual red (dirty∧listed, never a stash). Expected: grid-kit ≤ 33 with the last importer attributed by name; wire-null-defaults ≤ 17; ds-dts-fresh green; swatch gate green and PROVEN to convict (seed it red in a scratch copy); route-prisma attributed; the six baseline studio failures gone; the new gates (push-lock, recreate-guard, focus-visible, token-role, presence-vocabulary) green WITH their positive controls witnessed by you (run each seeded-red arm yourself).
2. THE MEASUREMENTS. M1–M8 re-read (not relayed): re-run M1 and M3 yourself on both DBs; confirm the migration on both (columns, policy, grants, unique index) or record prod NOT APPLIED; confirm no backfill happened (`SELECT count(*) FROM "ChannelListing" WHERE "presenceIntent" IS NOT NULL` must be the count of Wave 3 writes on XAVIA only, or 0).
3. THE WIRE. GET /studio/presence on GALE-JACKET (local API; name the DB): every coordinate's fact is UNKNOWN with as-of null unless a verify ran; intents null with a provisional intent carried WITH provenance; verdicts derived; the Openings listed; per-verb availability with reach/reversal/refusal from the server; the ended-eBay-row arm reads UNKNOWN never "never published" (seed one on the LOCAL DB under XAVIA, clean by value). POST /presence/verify on GALE's live eBay·IT coordinate (a READ): the fact lands with as-of and via after ≤ 8 s; the cap and ceiling refuse a second burst. GET /presence/one without an alias → 400 naming `alias`. POST /api/products/operational-impact with an unavailable check → status 'unavailable' in `checks`, verb not refused.
4. THE WRITERS. Each Wave 3 local verb rehearsed on a disposable DRAFT XAVIA row on the LOCAL DB with `fetch` stubbed (zero outbound calls asserted): hold → resume (syncPaused round trip + a push attempt refused by assertPushAllowed with its code); exclude → include (both directions; the re-included child is pushable — read the raw flag); discontinue → undiscontinue (intent + At/By/Reason; ProductEvent emitted with the coordinate; a verify enqueued); alias archive → restore (or the honest PES.5-ii refusal, read from the body). The recreate guard: a RELEASED identity seeded on the LOCAL DB refuses a create at that coordinate without the override. expectedVersion drift → 409 with `current`. The audit row's actor is the session user, not 'default-user' (grep the literal repo-wide with a positive control: the count must be the sites the lanes listed as requested-and-open, or 0).
5. THE DELIST PATH. The four PR.2 rehearsal arms re-run by you (transport failure → UNKNOWN + holdUntil + errorCode; "item cannot be accessed" → UNKNOWN/REFUSED; Amazon unpublish → refused, deleteListingsItem NEVER called; cancel → CANCELLED). dispatchSync given a lifecycle syncType → refused, never the update path (the Woo containment). The gate inversion: master flag off + AMAZON_PUBLISH_MODE=live → deleteListingsItem sends nothing. The narrowing: two rows sharing (product, channel, market) → the remove takes ONE (seed, count, clean). Dry-run close → no offerClosedAt written.
6. THE SCREEN, at 1440×900 both themes, signed in, zero console errors, hydration checked first, browser origin named: /products/[id]/edit/studio on GALE-JACKET — the Listings roll-up under THIS PRODUCT; Listings on Amazon·IT, eBay·IT, Shopify; the EmptyState on a market the channel does not serve; a held verb's description reachable by keyboard; ActionConfirm for `discontinue` (restated target, reversal sentence from the type — do not confirm it); the ⋯ menu with a danger-toned item apart; the toolbar's chips and `trailing`; the drawer's ListingsPane marks and as-of; the /products hard-delete modal's three radio bodies (do NOT confirm); the primary and danger buttons' focus ring; the provenance marks announced (read the accessibility tree). Every band height, row height, control height, identity width and copy string against the Variants spec §2 and PR.7's meta. Screenshots + a table.
7. THE AUDIT DISPOSITION. For every VERIFIED finding in audit-index.md (163): FIXED (by which lane, proven how — cite the measurement, not the ledger claim), DEFERRED (Wave 4 / Wave 5 — by design, cite the sequence bullet), OUT OF SCOPE (cite "Deliberately not in this programme"), or OPEN (nobody owns it — this is a finding of yours). A fixed count that you did not measure is not a fixed count.
8. THE LEDGER LINES. Every `QUESTION FOR THE OWNER:` (with the lane's recommendation), `ASSUMED:` and `REQUEST TO:` line across all eight sections in ONE table, each marked answered / applied / open. Every §C gate present with its time; every `PR-GATE-RUN STARTING` with its FINISHED.
9. THE HONEST COST, measured: on GALE-JACKET and two other XAVIA products, the count of coordinates that read "not checked · never" versus the count that read green "Live" before (from the baseline screens or the old readiness verdict) — the Owner must see this number.
10. COMMIT ATTRIBUTION (nothing committed): docs/audits/2026-09-13-presence/commit-groups.md — every file changed or created by this programme, its owning lane (from the claims, never from mtimes), mtime, and proposed commit groups in dependency order (packages/shared → schema → api → DS → studio), with the untracked probe scripts and scratch files that must NOT ride along. The Owner commits, not you.
11. WAVE 4 READINESS. What Wave 4 needs before its approval line: the six Owner decisions restated with what Waves 0–3 measured (D6 both numbers on one eBay family; D8 M2's count; D11 M5's answer; D13's second writer; D14 the running gate flags from M8; D19), the pre-push state, and any OPEN row from item 7. One paragraph the Owner can act on.

Say "verified" only about what you ran; say "relayed" about what you read; say "open" about what nobody proved. Numbers, never adjectives. Post `PR-GATE W3 VERIFIED — <session> — <time>` only if items 1–6 are green; otherwise post `PR-GATE W3 NOT VERIFIED` with the list, route each item to its owning lane's section as a `REQUEST TO:`, and stop.
```

---

## §F — Part II: Waves 4 and 5 (do NOT paste until the Owner writes `PR-OWNER WAVE-4 APPROVED — "<words>" — <time>` in the ledger, after Wave 3 has run in production with local verbs only)

These are the same sessions continuing under the same rules, the same §D ownership plus the new files named, and the
same gate discipline (`PR-GATE W4-…`, `PR-GATE W5-…`). Wave 4 is the first time the studio may write to a live
marketplace, so every lane's rehearsal moves from "fetch stubbed" to "the gate in dry-run on a disposable listing
the Owner names" — and NO lane flips a publish-mode flag. Read the six Owner decisions (D6, D8, D11, D13, D14, D19)
as recorded in §B, PR.F's Wave 4 readiness paragraph, and the full adapter specs before starting.

**PR.1 → W4-PLAN.** POST /studio/presence/plan (server-side, writes nothing: per-coordinate consequences, reach,
reversal{verb,fidelity}, gate mode, server-computed fan-out as NAMED coordinates per verb × channel × market — the
Amazon EU shared-quantity pool is NOT a fan-out for a per-market offer close but IS for any quantity-based hold and
for reopen, refusals each NAMING the verb that resolves them, the operational preflight for every local-destructive
verb, a planId and a short-TTL stateless-HMAC preflightToken binding the enumerated five-level set and each target's
version — D19) and POST /studio/presence/apply (refuses a missing, expired or set-mismatched token; 409 per target on
drift; per-target results in the shipped MarketOfferResult shape with NO aggregate success; THE ONLY WRITER — a gate
asserts plan/apply is not a fourth ChannelListing removal path). Plural typed-confirm REFUSED (the per-coordinate verb
offered). PD-2 (D6): widen the liveness predicate at BOTH the preflight and the cascade enqueue in ONE write with
PR.2, showing both numbers ("11 hold a marketplace id; Nexus will try to reach 7"). Delete the preview-only refusal in
channelActions.ts and runAction's short-circuit — recorded as its own approval line in the ledger (D14). Then
**W5-RELEASE**: the ladder walked and re-checked server-side (intent ENDED or DISCONTINUED; a fact of ABSENT or
NOT_SELLING from a real verify inside a freshness window; the operational preflight run and clear; a ListingIdentity
tombstone in the same transaction; confirmPhrase = the real external id; holdUntil +5 min with the cancel route);
remove-variation; `/recover` folded into History (D18); `release` gated on the RBAC enforce flip (D4).

**PR.2 → W4-EBAY.** ebay-presence-adapter + ebay-presence-guards: the reversible hold DELEGATES to the shipped
`deactivate` (qty 0, ItemID kept) and ZERO_PIN — never rebuilt, never greyed out as missing — named as its own verb
per D11 with M5's answer written into its copy; the shared-listing guards extracted WITH an exclusion set (Guard 2 is
correct only AFTER the target rows are deleted — extracted verbatim it fails shut on every coordinate and reports
"shared"); ENDED stops nulling externalListingId, in the SAME wave as sellingRisk's consumers and the delete-confirm
copy; the fact-stamp joins on ItemID, never SKU; an eBay end is permanent for that ItemID — "publish again as a new
listing" records lineage in ListingIdentity, never writes EbayListingIndex (D12). Then **W5-STOPSALE** with PR.5's
schema: one verb that stops selling on every coordinate while destroying nothing — SaleStop with a control snapshot,
the COALESCE partial unique index, a recall quarantine term in the published-quantity function (a REQUIRED field on all
15 call sites; the gate rejects a literal `quarantined: 0`), 'LIFECYCLE' on the existing default-on scheduled-changes
cron, never through the master status cascade; proposed on LotRecall, not automatic; two-step lift (D16).

**PR.3 → W4-AMZ.** amazon-presence.adapter.ts with its four resolvers (seller SKU; marketplace id from
Marketplace.marketplaceId — delete both literal maps and all eight `?? IT` defaults; connection; ASIN ownership as a pure
function whose EMPTY result is "unverifiable", never "rejected"); withdraw-offer / restore-offer via purchasable_offer
op:'delete' / restore with a REVALIDATED price (never last season's figure without a margin/floor check); honest silence
where the client has no method (no patchListingsItem: Amazon has a reversible offer close and a permanent delete and
nothing between — D14); FBA fail-closed with the Seller Central hand-off, offerScope:'FBM' only if M2 showed Offer
populated (D8); no lifecycle DB write on dryRun. Then **W5-IDENTITY**: RELAUNCH-SKU, MOVE-ACCOUNT, ATTRIBUTE on the
presence grid with a per-target strategy table where an unset row blocks the confirm; MOVE-ACCOUNT's source is normally
an INACTIVE grant (the active-account unique index) so the candidate query reads the full population; eBay cosmetic
until MAP.4; RELAUNCH-SKU's recreate carries fidelity 'none' until a fixture-ASIN rehearsal proves a second seller SKU
can be created; `sku` removed from writerAcceptsField (D17).

**PR.4 → W4-SHOP.** services/shopify/presence.service.ts — every verb through shopifyAdmin(accountId), never
ShopifyService; ARCHIVED the reversible takedown, publication removal the narrower one; withdraw/restore through
applyInformationPublications with a FRESH read as `value` and the captured set as `nextValue` (or it 409s every time);
end/relist through applyNativeEdit({field:'status'}); availability gated on read_publications/write_publications
(otherwise the verb is disabled with the sentence, and verify maps the throw to REFUSED); the intent WRITTEN into the
stored status/salesChannels cells so the sheet and presence cannot contradict (D13(a)) with the Wave 1 guard kept;
one sentence that this connection is one webstore; the /products bin cascade downgrades to `end` for Shopify and says
so; the four-part capture into ListingIdentity.retiredSnapshot before any release. Then **W5-ETSY**: the preflight
partition FIRST (cascadeCovered vs cascadeUnsupported from one exported constant), then in ONE write the SyncChannel
ETSY enum value + the dispatchChannelDelist Etsy branch + the dispatchSync refusal (the enum alone converts today's
loud Prisma rejection into three retries and a dead letter per row); BUILD the ingest (etsy-sync.service.ts queries a
Product.etsyListingId column that does not exist, through `prisma as any`, and throws at runtime); a first Etsy write is
a production experiment on a real listing — the Owner names the listing (D9).

**PR.5 → Wave 5 schema** on request: SaleStop (with the raw-SQL COALESCE partial unique index and its RLS policy and
grants), the ETSY enum, Product.lifecycleState — each staged, applied to both DBs on the Owner's word, verified, then
declared. **PR.6** ships the Wave 4 confirmation pieces the adapters need (a toned SummaryTable row via Pill; the
Disclosure tone in use; the plural COLLECT → PLAN → CONFIRM → APPLY flow's DS states). **PR.7** lands the Wave 4
copy WITH the wire fields (the per-verb reversal/refusal sentences are derived; the relist review as a MODE in History
with the diff read-only and edits landing locally through the ordinary mutator BEFORE the channel call — D18; the
"Unpublish" default flips to `none` with PD-2 — D22). **PR.8 → W4-UI**: the plural COLLECT → PLAN → CONFIRM → APPLY
flow on the presence grid's own selection bar — the plan rendered per coordinate with its fan-out named, the token
bound, the per-target results rendered with no green summary the server did not give, the queue rows draining in the
same panel; the legacy Listing Hub retired in the SAME write that lands the plural plan (D3). **PR.F** runs again at
the end of each wave, with a real dry-run rehearsal on the listing the Owner names.

---

*Written 2026-09-13 by the session that read both artifacts end to end. The proposal is the design authority; this file
settles the 24 decisions at its recommendation, splits the work by FILE so nine writers can share one uncommitted tree,
and gives the sessions a way to wait on each other without a hub. Nothing in it is built.*

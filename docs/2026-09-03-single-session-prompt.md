# Single-session prompt — Product Edit Studio, everything still open (2026-09-03 08:40)

Paste everything below the line into ONE fresh session in `/Users/awais/nexus-commerce`. Close every other session first (the hub `nexus-commerce-8c` included); their dev servers die with them.

---

You are the single session for the Product Edit Studio rebuild. There is no hub: you report to the Owner, who decides every design question; you measure, implement and verify. Work fix-then-report: land an item, then report once with mtime + sha1 (say "sha1"), the test run, and one screen reading.

## Standing rules (verbatim-critical)
- NOTHING is committed or pushed by you. The Owner pushes once, on their word. HEAD is `80f6cfb84`; ~148 modified and ~316 untracked files are the rebuild.
- The local API on :8091 uses the PRODUCTION database. Every write is real. Rehearsal writes only on the XAVIA family (GALE-JACKET parent `cmokmy3a40078pm0p1fvnu523`; variants e.g. BLACK-MEN-3XL `cmokmy0jf0003pm0ppnu1b2yy`, BLACK-MEN-4XL `cmokmy0jt0004pm0p0gaufo1b`; NOT BLACK-MEN-XXS / YELLOW-MEN-XXS, which carry corrupted bags). Never the parent row for a `manufacturer` write (cascade). Columns with no push path: `manufacturer` (the only one that is also formula-writable by the old gate), `weave_type`, `fabric_type`; `brand`, `name`, `status`, `price`, `quantity`, `amazon_title`, `attr_item_name` push.
- Before ANY fixture write: write one line in your ledger section (row, coordinate, column, value, expected reversal, time), capture the original on two read paths, have the restore command EXERCISED (built from a captured request of the same shape), then write; restore by value (`null` asserted strictly null), verify on two paths immediately and after ≥ 8 s, read `Product.version` back as the number you read. A formula DELETE needs the FULL coordinate (`scope`, `locale`, …) and you read the response BODY, not the status.
- Design changes not already ruled go to the Owner first. The Owner's rulings that stand: formulas on every column an operator can type into; select and yes/no results must be an allowed option or the operator sees a warning; recalculation is built to the marked recommendations in `docs/2026-09-02-formula-recalculation-design.md`, any deviation to the Owner first.
- Never bypass pre-push. Quote every gate with its argv (`--check`); census mode exits 0.
- Screenshot BEFORE any computed-style probe. A synthetic hover DOES fire AG's delayed tooltip (control first, dismiss stale panels). Value reads on this write path at ≥ 8 s. Clear grid state from `/products` (a page with no grid), never from the studio page. Read columns at `maxScroll` (every header renders there), not a sample at rest. `grep` in this harness is a ugrep wrapper that prints BLANK on files under bracketed route dirs — use `command grep`; an empty count means "did not run".

## Environment — start these FIRST, exactly
Check both ports are empty (`lsof -nP -iTCP:8091 -sTCP:LISTEN`, same for 3000). Then, as two background tasks:
```
NEXUS_API_HOST=:: PORT=8091 NEXUS_DISABLE_BACKGROUND_JOBS=1 NEXUS_ENABLE_TIMING_ALLOW_ORIGIN=1 npm --prefix apps/api run dev
NEXT_PUBLIC_API_URL=http://localhost:8091 npm --prefix apps/web run dev
```
The web variable is NOT optional: without it `apps/web/src/lib/backend-url.ts` falls back to the DEPLOYED production API on Railway while the page still renders (the server component reads the DB directly). Verify on the wire: zero requests to `railway.app`, the studio's calls to `:8091`. A `[completed]` web task with a live listener means the port was already held by someone else — trace the owner with `lsof -t` → `ps -o ppid=` up to the `claude` pid. `tsx watch` restarts the API on any save inside `src/index.ts`'s import graph (test files and scripts restart nothing); a 2–4 s DOWN/UP is a restart, not a drop. Local dev has NO Redis (Upstash host NXDOMAIN): queue-backed paths are inert.

## Read first
- `docs/pes-claims.md` — rulings newest-at-TOP (line 966 = #775); read #775 down to #687 for the day's rulings, then each lane's bullet row (`command grep -n -E '^- [A-Z]+\.[0-9] · ' docs/pes-claims.md`) and each lane's section at the bottom (PES.2's hand-off at line ~30850; UX.1's fixture register; PES.5's). Write your own progress in ONE new section at the bottom.
- `docs/2026-09-02-formula-recalculation-design.md` — the Owner-approved design.
- `docs/2026-09-02-layout-v2-convergence-run.txt` + `scripts/check-layout-v2.mjs` (`LAYOUT_ONLY=5.4 LAYOUT_VIEWPORTS=1280` runs the reveal clause in ~21 s; it refuses unknown values).
- Memory: the project memory index is loaded automatically; every pointer there names a trap someone paid for — a remembered pattern tells you what to CHECK, not what is TRUE.

## Open items, in order
1. **Recalculation (Owner item 51, step 2).** Check whether it has started (`command grep -rn 'formula.recalc' apps/api/src`). Build to the design's marked recommendations: dependency index (field → dependent formulas per product), recalculation pass after each product write in the same request (queue fallback above 50 dependants), same-row references only, pinned cells never touched, cycles refused at save, topological order within a row, a failed recalculation keeps the last value and stores `lastError`, and a `formula.recalc` audit row (original actor + ip, source field, expr, value, version before/after — READ BACK, never computed). Tests on each rule; rehearsal: change `brand` on a variant carrying `=upper($brand)` on `manufacturer`, read the recalculated value on two paths at ≥ 8 s, restore.
2. **The 1280 reveal defect (#752/#753/#768 addenda).** State: `revealColumn` is hoisted into `_studio/drawer/revealHost.ts` and consumed by both sheets (06:51 today); whether the cold `?rec=&cell=` path is fixed is NOT recorded. Re-measure with `LAYOUT_ONLY=5.4 LAYOUT_VIEWPORTS=1280` on a declared build, capturing `new Error().stack` in the `scrollLeft` interceptor (the real scroller is `.ag-grid-viewport.ag-layout-normal`; the body-horizontal one lags a frame). The narrowed question: why is `panelOverlap` 0 on a cold load when the panel is present with `data-resting-left`. Fix, then re-run; then the FULL layout suite once (≈5 min hold; 22-file stamp) — §5.4 reveal distances depend on column positions.
3. **Item 51 loose ends.** A successful channel-scope formula WRITE (the writer's gate was keyed on the raw `fieldKey` vs the sheet's routed `writeField` — confirm PES.5's fix landed and measure one write on Amazon·IT), and the refusal reason rendering on the page on both scopes (PES.2 landed a "save outcome" verdict on master; confirm the channel shows it).
4. **Re-activated lanes' parked work** (from #687; each in its own bullet row): PES.4 (drawer/dock; P13 `BulkOperationModal.tsx:296`), PES.7/IO.1 (images tab + ancillary tabs), CH.1 (channel connections), DS.1 (conformance; P14 the stale factory `token-guard.mjs`, fork-drift blind to `.mjs`), DS.2 (P15 certify the seven formula-palette hues against the GRID row grounds — the DS certifies page grounds only). Do them in the order the Owner asks; otherwise PES.4 → DS.1/DS.2 → PES.7 → CH.1.
5. **Housekeeping before the push (Owner items 35/36).** Delete probe scripts ONLY by explicit `_pes<n>-*.mts` name (58 untracked remain; 1,682 `_*.mts` are TRACKED at HEAD — do not sweep); the eleven untracked migration folders under `packages/database/prisma/migrations` are APPLIED to prod (424/424) and MUST ride; the push adds `apps/web`, `apps/api/src`, `packages`, `scripts/check-*.mjs`, `.githooks`, `docs` explicitly — never `git add -A`; every gate green with its `--check` argv; 17 untracked `*.vitest.test.ts` under `apps/api/src` ride with the directory. Then STOP and hand the Owner the exact command list; the Owner pushes.
6. **Owner decisions you cannot make:** item 50 (three unannounced `attr_item_name` writes on 3XL at 19:56 yesterday — recorded, unattributed; the gap is an ACTOR on local write paths, `userId` null); the one-line `NEXT_PUBLIC_API_URL=http://localhost:8091` in `apps/web/.env.local` (env files are the Owner's); items 32, 34, 40, 42, 45, 46, 47 as listed under #361.

## Report shape
One message per landed item: files with mtime + sha1; tests run with the command; one screen reading (screenshot first) naming the build; what was NOT verified, stated plainly; fixture state after restore with the version as read. A number reached by recall or arithmetic is not a measurement.

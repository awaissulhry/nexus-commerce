Product experience sign-off is in progress. Items are verified individually; passing one item does not certify the entire workspace.

| Item | Status | Required evidence |
| --- | --- | --- |
| Category IDs → names | Amazon cached names verified; current eBay breadcrumb observed; fresh provider authentication still unverified | Correct market-specific names in API and browser, stored IDs preserved, lookup failure/retry and cache tests |
| Other reference names: policies, themes, shipping, fulfillment, markets | Named selectors, metadata and server validation verified; live seller names blocked | Exact account/market labels, usable pickers and recovery, no ambiguous name writes |
| English headings and consistent grid chrome | Verified for the current supported cache and live scope sample | 93 active schemas inspected; 52 supported cached coordinates and 957 distinct English labels; live Master/eBay/Amazon reads; DS loading/empty/error and keyboard recovery |
| Parent/Child and alias lifecycle | Information family actions and alias safeguards verified within the boundaries below | Production-schema route tests, reviewed membership checks, alias/account isolation, keyboard selection and cancellation |
| Database-backed import/edit/reset/concurrency | Audited family/import cache persistence and Information save/reload recovery verified; remaining boundaries below | Production-schema persistence/rollback, old/new parent projections, scoped lost-acknowledgement recovery and queued edits, no outbound publication |
| Product workspace tasks and navigation | Pending | Information, Media, Needs attention, Performance, Activity, Listing information, eBay themes/order; scope isolation and keyboard/recovery checks |
| Shared design-system guards | Current reference and information-state passes pass | Web/Factory token guards and generated-token checks; types, primitive/conformance and CSS checks; mirrored shared changes |
| Final integrated gate | Pending | Production build, relevant suites, browser desktop/narrow light/dark and documented limits |

Category pass, 7 September:

- Amazon browse-node `2420941031` now resolves to `Auto e Moto > Moto, accessori e componenti > Abbigliamento protettivo > Giacche` in the OUTERWEAR grid. The verified source is an existing COAT schema for the same Italian marketplace. The lookup reads only the latest active browse-node subtree per product type, rejects conflicting names, shares concurrent requests, and preserves stored IDs and allowed values.
- The live API returned that path under both `recommended_browse_nodes` and `browseNodeId`; all 21 GALE-JACKET rows displayed it. Find matched the category name. Provider category values retain their marketplace language; the heading remains English.
- Category search now composes Nexus Field, Input, ListboxPanel and Button. Browser checks covered search by name, ArrowDown with focus and `aria-activedescendant`, scrolling the active option into view, Escape/Cancel, and accepting the unchanged category without a pending save. The popup fits a 600×900 viewport in both light and dark themes; the viewport and light theme were restored afterward.
- eBay app authentication accepts complete canonical `EBAY_CLIENT_ID` / `EBAY_CLIENT_SECRET` or legacy credential pairs without mixing them. Concurrent taxonomy requests share token/tree work. Unknown or unavailable markets never borrow UK names; cached wildcard eBay paths are also rejected. UK condition lookups use eBay's GB identifier.
- Amazon catalog detection now reads the documented marketplace wrapper and top-level productTypes. Separate category branches are not concatenated, ancestor IDs are not treated as assigned categories, and wrong-ASIN/wrong-market results are rejected. Browse-path and suggestion routes require the requested effective product type and verify the live type; untyped reconciliation ASINs cannot supply unrelated breadcrumbs. Contract checked against the [official Catalog Items reference](https://developer-docs.amazon.com/sp-api/docs/catalog-items-api-v2022-04-01-reference).
- Initial grid errors in shared and channel scopes use the same Nexus EmptyState/Button composition, recovery guidance and retry. A real temporary API disconnect exercised the channel error and successful retry. Raw fetch text and internal deployment instructions are removed from these states. Existing saved-layout recovery remains in the footer.
- Policy lookup errors now identify connection recovery, network retry, and malformed responses. Seller/market changes hide previous choices immediately; the editor has explicit Cancel/Escape and only commits a chosen value. Tests retain the account/market cache boundary. The live Shipping policy editor showed safe connection guidance, disabled unavailable choices and usable Refresh/Cancel controls.

Verification for this pass:

- 29 focused API tests across category authentication, cache labels, classification parsing and route scope isolation passed. A wider category/browse-node regression run passed 51 tests (overlapping coverage, not an additional 51 unique tests).
- 457 Web sheet tests passed. The 13 read-only live eBay tests initially encountered a dropped API connection; an unrestricted rerun passed all 13. No skipped test is counted as a pass.
- Web and API type checks passed. The Web production build passed using `.next-product-grid-audit`; the generated `next-env.d.ts` reference was restored to its prior value afterward.
- Web token guard, conformance ratchet, raw-primitives ratchet and token generation check passed. These guards do not certify every untracked file; the changed feature controls were inspected directly. No shared design-system component was changed in this pass, so no Factory mirror was needed. The prior Factory violations remain open.
- No product/listing data or saved grid layout was intentionally changed, and no listing was published. Live name diagnostics used reads; the unchanged category selection produced no pending save.

External blockers, verified independently of the code fixes:

- eBay seller credentials fail authentication and the local app credentials are placeholders. All 221 inspected Italian eBay listings with category `177104` lack a cached category name/path. The breadcrumb endpoint now reports 503 with connection/retry guidance. Live category and business-policy names need the eBay connection and application configuration restored; no name is guessed.
- Amazon's seller-specific PTD request returns `invalid_grant`. Its SP-API connection is managed by the local environment, so seller authorization must be restored there before shipping-template names can be verified. The normal reference-label endpoint reports `shippingTemplate` unavailable while cached browse-node names continue working.
- The final browser read also showed mapping/validation errors and unavailable scope summaries. These are recorded for the separate readiness/mapping pass; the category work does not establish that the real listing data is publication-ready.

Reference-editor pass, 7 September:

- Description themes and Amazon shipping templates always open a named selector in Master and channel grids, including while names load or the provider is unavailable. They cannot fall back to a free-text editor during those states. The selector commits an explicitly chosen stored ID, preserves null/default semantics, cancels unchanged values and discards stale results after scope changes.
- Theme choices distinguish the default-following mode, explicit “No theme”, and active theme IDs. Inactive themes retain labels for existing values but do not appear as new choices. Searching an explicit theme name excludes the default-following row; searching “Default theme” still finds it. The names endpoint selects only id/name/active/default metadata and does not seed or fetch full HTML templates.
- Reference requests share in-flight work, including concurrent refreshes, and cache names briefly. Shipping lookup boundaries include account, marketplace and product type. Failure removes the cache entry so a retry can recover. Provider response details are not exposed as UI errors.
- Marketplace display metadata now covers internal database IDs, provider IDs, country codes and GB/UK aliases. Names stay within channel scope; conflicting labels for the same ID are rejected. Standard fulfillment labels take precedence over localized provider metadata. Declared Amazon variation-theme codes receive English display labels while stored codes remain unchanged.
- The shared `AsyncListboxPanel` uses Nexus Field, Input, ListboxPanel and Button with standard small controls. Categories now consume the same component. Loading, error, no matches, retry and cancel states use one composition. Arrow keys skip disabled options; Enter cannot commit during loading; Tab moves to the actions; Escape cancels. Grouped option order is shared with the Listbox model. The component, Listbox focus option, CSS, documentation and catalog specimen are mirrored in Factory.
- Browser checks verified live theme labels, search, keyboard selection, retry/empty/cancel behavior, and 480×376-pixel popup bounds within a 600×900 viewport in both themes. Desktop light/dark presentation was also inspected. The initial name-search check exposed an ambiguous default-row match and performed a reversible local theme change; the original explicit `Xavia Modernist` assignment was restored and verified after a full page reload. After the fix, accepting that same searched theme produced no save. No listing was published.
- The live Amazon Merchant Shipping Group editor showed the expected connection guidance, Try again and Cancel. Retry returned to the loading state; Escape closed without a save and retained the original ID. Temporary viewport overrides and light theme were restored. No saved column view was changed.
- Verification: 477 Web sheet tests passed, including all 13 live channel tests; 20 API theme-route tests passed. One additional request-deduplication test was then added and the 15 focused reference tests passed. These focused tests overlap the broader run. Web/API/Factory types passed; production build passed. Web and Factory token guards and generated-token checks, Web conformance/primitive ratchets and CSS parsing passed. Shared component/specimen parity was checked directly. The earlier Factory token gap was closed by the separate mapping-quality work and is no longer an open failure.

Reference-write pass, 7 September:

- Grid edits and channel-override imports now share authoritative reference resolution for description themes, eBay shipping/payment/return policies, and both Amazon shipping-template field keys. Exact IDs take precedence; a trimmed, case-insensitive display name must identify exactly one choice. Non-text values, ambiguous names, inactive themes, unknown IDs and references from another scope are refused before persistence. Opaque IDs remain case-sensitive. The stored value is the canonical ID, never the submitted display name.
- The grid preserves pasted names until server validation, even for select-shaped columns or while labels are loading. Server responses carry normalized IDs back to the submitted cells without replacing newer local edits. Name lengths are checked after resolution against the ID field's limits, so a long valid display name is not rejected as an oversized ID.
- Existing unchanged IDs remain valid no-ops even if retired or currently unavailable; this does not authorize new assignments of those IDs. Clear and inherit do not need provider credentials. Theme default-following, explicit “No theme” and explicit theme IDs retain their existing meanings. Parent/Child/Standalone roles, alias identity and completeness percentages are unchanged.
- Policy assignments require a fresh, complete response for the selected account and exact marketplace, including UK → EBAY_GB. Strict reads reject incomplete responses and provider failures, omit unrelated location calls, and preserve the normal snapshot cache. Amazon assignments use fresh seller-specific schema choices, exclude deprecated templates and retain the existing primary-seller credential boundary. Choices are shared within a request/import batch, including both shipping field aliases; later preview/apply batches obtain fresh choices. Bulk listing lookup is indexed by product rather than scanned for every cell.
- Import apply revalidates the reviewed reference and compares the canonical patch with preview. Disabled/deleted themes and a name reassigned to a different ID are rejected. These checks do not claim that external provider configuration is transactionally locked throughout a batch.
- Verification: 173 API tests passed across 11 files, including real-route validation, strict provider responses, preview/apply and production SQL persistence. One intentionally disabled manual browser-fixture server test remains skipped and is not counted as a pass. The import-job tests use the isolated Prisma-shaped fixture store; nine persistence tests run against PGlite/PostgreSQL, including writing different template IDs for the same name in two accounts and refusing a foreign account's ID without spending a version. Six shared resolver tests and 485 Web sheet tests passed. Web/API type checks, the shared package build and Web production build passed. The build's generated `next-env.d.ts` path was restored afterward. The touched-file whitespace check passed.
- Live verification used five `dryRun: true` requests against the real eBay Italian GALE-JACKET listing: unique name, unknown name, non-text value, clear and inherit. The valid name resolved to the expected ID; invalid inputs reported per-field errors. A second sheet read confirmed the stored theme and listing version were unchanged. This pass made no live catalog writes or outbound publication.
- No shared design-system component, control markup or CSS changed in this pass. Existing selector, refusal and save-state surfaces remain in use; no Factory mirror was required. The earlier browser and design-system checks remain recorded above. Live policy/shipping names still require the seller connections to be restored. Automatic channel recovery after a lost save acknowledgement remains part of the separate database/concurrency item; this pass verifies acknowledged normalization and does not certify that recovery flow.

English headings and information-state pass, 7 September:

- Rechecked the current heading resolver against 93 active Amazon/eBay schema records, producing 52 supported channel/market/category coordinates and 957 distinct cached headings. The distinct labels were reviewed for English; no further translation change was needed. Six live sheet reads succeeded: Master IT (230 columns), eBay IT (51), and Amazon IT/DE/FR/ES (164/166/169/169). The [heading audit snapshot](./2026-09-07-product-grid-heading-audit.json) records the labels and scope coverage. Counts describe this snapshot, not every future provider category. Stored field keys and marketplace-language product/category values are unchanged.
- Master and channel grids retain the same toolbar during initial load and failure. Search, saved views, classification and data actions cannot operate on an unavailable read. Counts no longer describe a failed request as an empty catalog. Reload and the standard Nexus error state provide recovery; raw provider/fetch details stay out of the error presentation. The redundant channel content-language text and source legend remain absent.
- Loading and empty results now use the existing Nexus grid overlays. An empty search offers Clear filters; an empty successful read offers Reload. Clearing filters resets the search, selected issue chip and grid filters without replacing the saved column view. Master reload hides old rows until the read completes; the channel hook already clears the old response.
- The Web grid loading overlay now supports single-line thumbnail rows in all three densities, retaining the compact 36px product row. Browser measurement caught a shrink-to-content wrapper that reduced the skeleton to 76px and hid its text; the shared grid wrapper now fills the available width. The Web catalog includes a product-row loading specimen. Grid adapters are explicitly Web-only in both platform READMEs; Factory has no runtime adapter to mirror. Both catalogs/changelogs and DS-GAPS document this boundary. Existing shared controls and token values are unchanged by this pass.
- Initial sheet reads stop after 30 seconds and abort on scope cleanup. Master uses its legacy fallback only for a real 404; a network failure no longer starts a second catalog request. Channel reads and quiet refreshes reject incomplete response envelopes before replacing the view. This is minimum sheet-envelope validation, not a claim of exhaustive row validation or lost-save-acknowledgement recovery.
- Browser checks covered healthy Master/eBay/Amazon sheets, live Master search with no matches and Clear filters, Master failure and retry, eBay loading/empty/malformed response and keyboard recovery, and Amazon failure/retry. Desktop and 600×900 light/dark presentations were inspected. Loading skeletons measured 1660px wide at a 1728px viewport and 532px wide at 600px, with 36px rows and no page overflow. Browser presentation preferences and the build-generated `next-env.d.ts` reference were restored afterward. The separate navigation/scope-bar audit remains open.
- Failure testing used an isolated local proxy that refused all non-GET requests; it also refused the formula batch read because that endpoint uses POST, so formula availability in this fixture is not sign-off evidence. A held Master read reached the timeout and recovered through retry. No catalog writes or outbound publication were performed in this pass.
- Verification: the final complete Web suite passed **3,486 tests across 257 files**, with no skipped cases. The separate Amazon live run passed all 13 scope tests (additional scope coverage, not 13 new unique tests). All 99 API heading/schema tests passed. Web type checking and the production build passed. Web and Factory generated-token checks, Web token/conformance/primitive guards and CSS parsing passed. Guards that scan tracked files do not certify every untracked workspace file; changed feature controls were inspected directly.
- The current live eBay grid now displays the Italian category breadcrumb. This updates the earlier unavailable-cache observation above; this pass did not test renewed provider credentials or certify fresh policy/shipping-template names. Existing mapping/validation findings describe listing data and remain outside the heading/chrome sign-off.

Parent/Child and alias lifecycle pass, 7 September:

- Information-grid Attach and Move now have searchable product pickers showing SKU and product name. Choices exclude archived products, nested children and the current parent; standalone products with protected alias relationships are disabled with an explanation. Reads select only identity metadata, use batched alias checks and cap results at 50 with a search hint. Cancelling collection stops before confirmation or execution. No new writable role field was introduced, and completeness percentages and Amazon listing-role fields remain separate.
- Family writes revalidate membership inside serializable transactions. Attach requires an existing Parent and preserves child axis values. Move and unlink reject a changed parent when the UI submits its reviewed parent ID. Moving or unlinking the last child preserves the explicit Parent role. Forced demotion requires the exact reviewed child IDs and atomically detaches those children, clears the variation theme and demotes the parent; it previously left children attached while clearing the flag. Serialization conflicts return reload/review guidance without silently retrying an action against a different family.
- Alias protections now cover the Information family endpoints as well as Parent SKU import. Active and archived aliases, owned alias records and legacy listing rows with only an alias foreign key prevent incompatible family changes. Alias creation reads family membership inside the same transaction as its draft listings. Rename/archive validate the family and account; archive is idempotent and retains listing records. Parent SKU apply also refuses a product that gained children after preview without changing its own snapshot. Imports retain per-target checkpoints and transactions; this is not whole-file rollback.
- Adding a child creates a DRAFT product with channel sync disabled. Optional copied listing content stays in the exact account and alias and has publication disabled. Creation and copies commit together; an invalid copy source rolls back the new child. Local child deletion refuses marketplace records and alias relationships, and removes the child plus its entry in the parent listing payload atomically. The legacy parent-delete route uses the same protections. These actions do not claim to end remote marketplace listings.
- Browser checks covered search, empty results, keyboard selection, named Attach/Move confirmations, Cancel, Escape and restored trigger focus. Typed demotion remained disabled for an incorrect SKU and enabled only for the exact SKU; it was cancelled. A 20-child confirmation measured 560×738 pixels inside a 600×900 viewport, with no document overflow. Light and dark picker presentation was inspected. The live child-delete preflight refused a marketplace-linked child before confirmation. A saved-layout read failure recovered through Retry layout. Browser tests did not apply any family or alias mutation; light theme and the desktop viewport were restored.
- The shared `AsyncListboxPanel` search input now carries the Nexus modal autofocus marker; this fixed initial focus landing on Close instead of search. The component is mirrored in Factory, and both catalogs/changelogs document the behavior. The Web grid action runner supports cancelled collection, with a catalog specimen and regression test. Grid adapters remain Web-only, as documented in both platform READMEs. No control size, token or feature stylesheet change was needed.
- Verification: **3,492 Web tests across 257 files** passed, with no skips. The subsequent focused family/action run passed 73 overlapping tests after adding the reviewed-parent protocol. The final API relationship/alias/import/account regression passed **141 tests across nine files**, with one existing opt-in manual browser-fixture test skipped and excluded from the pass count. This includes **31 production-schema route/service tests against disposable PGlite/PostgreSQL**, including rollback on a database failure, draft/account/alias persistence, archived alias guards, stale selections and deletion refusal. Web, API and Factory types passed. The Web production build, both token guards and generation checks, Web conformance/primitive ratchets and CSS parsing passed. Shared component parity and whitespace in changed lines and untracked source files passed. Four pre-existing trailing-whitespace lines elsewhere in `catalog.routes.ts` were left unchanged. Existing build warnings concern workspace-root lockfile selection and future PostgreSQL SSL-option semantics; neither was a failed gate in this pass.
- The read-only live audit again found 338 active products with no child marked Parent and no unmarked root with children. Master and Amazon relationship columns remained consistent. No live catalog records were written or listings published. Build-generated `next-env.d.ts` was restored to its prior path.

Cache and interrupted-save follow-up, 7 September:

- Family/alias creation and import paths now refresh affected cached products and both former/current parents transactionally. The projector batches reads and refreshes counts, coverage, thumbnails and the existing category/fulfillment projection. Bulk grid edits use one awaited post-commit refresh batch. A broader drift detector backs up missed cache updates.
- Shared product and channel saves now recover interrupted or malformed acknowledgements through a scoped read, including alias identity, concurrency versions, reference names and reset/pin state. Newer typing remains queued and visible. Missing fields/version tokens remain unconfirmed. Both Reload actions review local edits; channel conflicts keep refused values available for correction.
- Browser verification recovered lost acknowledgements on Shared product and eBay, verified the next eBay write used the refreshed listing version, and exercised a non-stored edit plus Reload cancellation/confirmation. The confirmation fit 600×900 in light and dark themes; Escape restored focus. All four writes were simulated in memory and never forwarded to the live API.
- The full Web suite passed **3,510 tests across 258 files**. The API regression passed **174 tests across 13 files**, with one existing manual-fixture skip excluded. Real production-schema SQL tests include cache rollback, imported Parent SKU, old/new parent rollups and deletion. Web/API/Factory types, production build and design-system gates passed. See the [cache and recovery evidence](./2026-09-07-product-cache-recovery.md) for test boundaries and browser outcomes.

Remaining boundaries: grid PATCH cache refresh is post-commit and can fail independently; the reconciliation backstop defaults to 15 minutes, and product-list KPI statistics retain a 15-second cache. Provider ingestion, external search propagation and other legacy bulk paths have not all moved onto the shared transaction safeguards. Disposable SQL tests do not prove simultaneous multi-connection contention, and browser closure does not preserve durable local drafts. Broader workspace tasks and fresh seller-reference authentication remain open.

Next item: product workspace navigation and scope switching, including pending-edit handling when leaving Information. The entire product experience is not yet signed off.

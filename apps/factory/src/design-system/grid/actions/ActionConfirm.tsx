/** Compatibility path; the shared confirmation is a component, independent of grid CSS. */
export { ActionConfirm, canConfirmAction, useActionConfirm, type ActionConfirmProps, type ActionConfirmApi } from '../../components/ActionConfirm'

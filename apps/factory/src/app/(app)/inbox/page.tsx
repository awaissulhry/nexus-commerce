/** FP1 — the Inbox is LIVE: the factory's front door (spec: docs/factory/FP1-SPEC.md). */
import { InboxClient } from "./_components/InboxClient";

export default function InboxPage() {
  return <InboxClient />;
}

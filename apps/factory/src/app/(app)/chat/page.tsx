/** FC2 — /chat is LIVE: Order Spaces, the Google-Chat shell on the FC1 substrate (spec: docs/factory/FS-FC-PROPOSAL.md §3). */
import { ChatClient } from "./_components/ChatClient";

export default function ChatPage() {
  return <ChatClient />;
}

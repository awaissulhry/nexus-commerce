// Re-export shim — canonical location is now the design system.
// Kept so any existing imports from the old path continue to resolve.
export { ColumnGroupModal, type ColumnGroupModalProps } from '@/design-system/components/ColumnGroupModal'
export type { ColumnGroup } from '@/design-system/components/ColumnGroupModal'

// FP.1 — full project-detail page (replaces the old cramped dark drawer),
// rebuilt on the app's design system with tabs. Room to edit specs, size
// charts, images, and (FP.7) the factory pack.

export const dynamic = 'force-dynamic'

import ProjectDetailClient from './ProjectDetailClient'

export default function DevelopmentProjectPage() {
  return <ProjectDetailClient />
}

/**
 * STO.4 — editable per-weekday send-time windows.
 */
import SendTimesClient from './SendTimesClient'

export const dynamic = 'force-dynamic'

export default function SendTimesPage() {
  return <SendTimesClient />
}

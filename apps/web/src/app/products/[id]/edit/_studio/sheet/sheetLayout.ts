// Compatibility path for studio sheet callers; the layout rules are shared by every grid host.
export { preferencesFromLayout, layoutFromPreferences, visibleLayoutKeys } from '@/design-system/grid/views/columnLayout'

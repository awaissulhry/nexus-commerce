/** Campaign Builder (type chooser) — Helium 10 Ads match. */
import { CampaignBuilder } from './CampaignBuilder'

export const dynamic = 'force-dynamic'
export default function CampaignBuilderPage() {
  return <CampaignBuilder />
}

/** Campaign Builder · Quick (Helium 10 Ads match). */
import { QuickBuilder } from './QuickBuilder'

export const dynamic = 'force-dynamic'
export default function QuickBuilderPage() {
  return <QuickBuilder />
}

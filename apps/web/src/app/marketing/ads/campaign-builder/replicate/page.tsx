/** AX3.2 — Campaign Builder · Replicate Structure: copy a working structure onto another product. */
import { ReplicateBuilder } from './ReplicateBuilder'

export const dynamic = 'force-dynamic'
export default function ReplicatePage() {
  return <ReplicateBuilder />
}

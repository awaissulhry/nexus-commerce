/**
 * NEG.8 — the record and the notifications. NOT BUILT YET.
 *
 * Evidence on create and on retire, and the notification that a negation happened. 0 of 856
 * create logs carry evidence today, so "why does this negative exist" is unanswerable for the whole
 * base — 1,225 rows have no log at all and never will.
 *
 * Renders null. Hidden, not disabled: the section does not exist, so the page shows nothing rather
 * than a control that cannot work. Its props are already the shared contract, so NEG.8 is one file
 * and one import line — nobody restructures the client.
 */
import type { NegSlotProps } from './slot-contract'

export function NegRecord(_props: NegSlotProps) {
  return null
}

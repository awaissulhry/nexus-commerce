/** WG.3e — moved into the design system; re-exported so its 13 call sites are unchanged. */
export { AdsFilterBar, stripServerKeys, isServerKey } from '@/design-system/patterns/workspace-grid/AdsFilterBar'

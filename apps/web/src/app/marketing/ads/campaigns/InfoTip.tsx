/**
 * W6 (2026-08-20) — `InfoTip` moved into the design system
 * (`@/design-system/primitives/InfoTip`); this file re-exports it so the 27 existing relative
 * imports keep working unchanged. New code should import from `@/design-system/primitives`.
 */
export { InfoTip } from '@/design-system/primitives/InfoTip'

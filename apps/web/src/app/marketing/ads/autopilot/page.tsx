/** AC P-D — Autopilot control room (AI Control). Plan canvas + real-time decision feed. */
import { AutopilotControlRoom } from './AutopilotControlRoom'

export const dynamic = 'force-dynamic'

export default function Page() {
  return <AutopilotControlRoom />
}

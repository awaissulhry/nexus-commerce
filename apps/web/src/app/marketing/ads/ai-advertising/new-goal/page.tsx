/** AI Advertising · New Product Goal — the "AI Goal" campaign builder (Helium 10 match). */
import { AiGoalBuilder } from './AiGoalBuilder'

export const dynamic = 'force-dynamic'
export default function NewProductGoalPage() {
  return <AiGoalBuilder />
}

'use client'

// Keep the existing settings entry point on the canonical listing-preset UI.
export { default } from '@/app/channels/listing-presets/ListingPresetsClient'
export type { WizardTemplateRow } from '@/app/channels/listing-presets/ListingPresetsClient'

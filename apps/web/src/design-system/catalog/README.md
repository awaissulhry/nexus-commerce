# catalog/

The living style guide — one screen that renders every token and every component
in all relevant states, at native resolution. It is both the **documentation**
surface and the **verification harness**: each phase's screenshot-diff and
numeric alignment checks run against this page.

Evolves from the existing `apps/web/src/app/design/page.tsx`. Light-first; dark
toggle where supported.

> Empty until **Phase 2**.

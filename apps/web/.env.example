# Database
DATABASE_URL=postgresql://user:password@localhost:5432/nexus_commerce
# Rebuild the web app with this enabled when enabling business profiles in API/workers.
NEXT_PUBLIC_WORKSPACES_ENABLED=0
# Server-only API origin for the same-origin /backend proxy.
NEXUS_API_PROXY_TARGET=http://localhost:4000
